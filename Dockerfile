# Dockerfile — صورةٌ واحدةٌ عامّةٌ لكلِّ خدمةٍ ورُبوتٍ قابلٍ للتشغيل. (M2-01)
#
# ── العيبُ المقيسُ الذي عولِجَ هنا ─────────────────────────────────────────
# لم يكنْ للمستودعِ صورةٌ **قابلةٌ للبناءِ** أصلاً: المحاولةُ السابقةُ (PR #205)
# كانتْ تنسخُ `services/*/dist` بنجومٍ، والخدماتُ لا تعملُ من `dist`: أمرُ
# `start` فيها كلِّها هوَ `node --import tsx src/…`، وصادراتُ حِزَمِ العملِ
# تُشيرُ إلى `src/*.ts`. فصورةٌ بلا مصدرٍ ولا `tsx` لا تُقلِعُ، وبناءٌ لم
# يُشغَّلْ في CI مرّةً ليسَ إثباتاً بل نصٌّ.
#
# ── قراراتٌ مُلزِمةٌ (يفرضُها الفحصُ 19) ───────────────────────────────────
#   • الأساسُ مُثبَّتٌ بالبصمةِ (digest) لا بالوسمِ وحدَه: وسمٌ متحرّكٌ يجعلُ
#     «نفسَ البناءِ» شيئَينِ مختلفَينِ بينَ يومَينِ.
#   • رقمُ نسخةِ العقدةِ = `NODE_VERSION` في `.github/workflows/ci.yml`
#     حرفاً بحرفٍ: ما يُختبَرُ هوَ ما يُشحَنُ، ومصدرُ الحقيقةِ واحدٌ.
#   • لا نسخةَ pnpm مكتوبةً هنا: `corepack` يستنبطُها من `packageManager`
#     في `package.json` — مصدرٌ واحدٌ لا نسختانِ تفترقانِ بصمتٍ.
#   • المستخدمُ النهائيُّ غيرُ جِذرٍ (`node`)، والطبقةُ الأخيرةُ هيَ التي
#     تحملُ `USER`: جِذرٌ في صورةِ خدمةٍ عيبٌ أمنيٌّ لا تفصيلُ ذوقٍ.
#   • تبعيّاتُ التطويرِ حاضرةٌ في الصورةِ **بقصدٍ مُسجَّلٍ**: `tsx` شرطُ
#     تشغيلٍ لا أداةُ تطويرٍ في هذا المستودعِ (RISK-0043)، وتقليمُها يستلزمُ
#     ترجمةً حقيقيّةً — دَينٌ مُسمّىً في RISK-0047، لا ادّعاءُ صورةٍ نحيفةٍ.
#
# المرجع: docs/08-infrastructure/CONTAINER_IMAGES.md · ADR-033 · docs/12-testing/M2-01_GATE.md

FROM node:20.20.1-alpine@sha256:b88333c42c23fbd91596ebd7fd10de239cedab9617de04142dde7315e3bc0afa AS base

# يُمرِّرُهُ `build-image.sh` من زمنِ الالتزامِ لا من ساعةِ الجدارِ. ويُعلَنُ هنا
# لأنَّ وسيطَ بناءٍ غيرَ مُعلَنٍ **يُتجاهَلُ بتحذيرٍ** — فيُقرأُ «مُثبَّتٌ» وهوَ مُهمَلٌ.
ARG SOURCE_DATE_EPOCH=0

ENV PNPM_HOME=/pnpm \
    PATH=/pnpm:$PATH \
    CI=1 \
    COREPACK_ENABLE_DOWNLOAD_PROMPT=0 \
    COREPACK_HOME=/corepack \
    SOURCE_DATE_EPOCH=${SOURCE_DATE_EPOCH}
WORKDIR /app

# `COREPACK_HOME` مُصرَّحٌ بموضعٍ مُشتَرَكٍ بقصدٍ: تنزيلُ `corepack` يذهبُ افتراضاً
# إلى بيتِ المستخدمِ الذي بنى (الجِذرِ)، والطبقةُ الأخيرةُ تعملُ بـ`node` — فلولا
# هذا لاحتاجَ أوّلُ `pnpm` في التشغيلِ إلى **الشبكةِ**، وحاويةٌ تحتاجُ الشبكةَ
# لتُقلِعَ ليست قابلةً للشحنِ.
RUN corepack enable && mkdir -p /corepack

# ── طبقةُ التبعيّاتِ والمصدرِ ──────────────────────────────────────────────
# `COPY . .` مقصودٌ: حِزَمُ العملِ تُصدِّرُ `src/*.ts`، فلا يُفيدُ نسخُ
# البيانِ وحدَه ثمَّ المصدرِ لاحقاً — و`.dockerignore` هوَ ما يمنعُ دخولَ
# `node_modules` و`.git` والأسرارِ إلى السياقِ.
FROM base AS build
COPY . .
RUN corepack install \
 && pnpm install --frozen-lockfile \
 && pnpm store prune

# ── طبقةُ التشغيلِ ────────────────────────────────────────────────────────
FROM base AS runtime
ENV NODE_ENV=production
COPY --from=build --chown=node:node /app /app
COPY --from=build --chown=node:node /corepack /corepack
USER node
ENTRYPOINT ["/app/scripts/container/entrypoint.sh"]
