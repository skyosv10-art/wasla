# Status Reconciliation — M0-23 / M0-34 / RISK-0020 / M2-04..10

> **CLM-0257** · 2026-09-20 · `@uxxxu (agent:perplexity-computer)`
> **Base commit:** `07ba94c20f1d9ba9c0d0e0e604850d83413a69dc` (main)
> **Scope:** قياس معاد من الشجرة الحالية لا من تقارير سابقة. لا عمل برمجي جديد.

---

## 1. القياس المعاد من الشجرة

### 1.1 الخدمات/الحزم والـ Journals

| القياس | النتيجة | الدليل |
|---|---|---|
| خدمات بـ `contracts/schema.sql` | **13** | `find services -name schema.sql -path '*/contracts/*'` |
| `_journal.json` (services + packages) | **14** | 13 خدمة + `packages/channel-postgres` |
| خدمات بـ `migrate.ts` + `migrate-cli.ts` | **13** | كل خدمة منتظمة لديها runner |
| ملفات `0000_*.sql` (ترحيلات أساس) | **28** | `find services packages -name '0000_*.sql' -path '*/drizzle/*'` |
| ملفات بعلامة `@wasla-upgrade-proof` | **63** | ترحيلات + اختبارات ترقية |
| ملفات `migration-upgrade-with-data.integration.test.ts` | **13** | خدمة لكل خدمة منتظمة |

### 1.2 برهان الترقية على قاعدة ببيانات

- **13 ملف اختبار `migration-upgrade-with-data.integration.test.ts`** موجودة في كل خدمة منتظمة.
- `validate-migrations.sh` يؤكد: كل خدمة منتظمة لديها برهان ترقية يُعلن تغطية كل ترحيل غير أساس.
- الاختبارات تُشغَّل في CI عبر وظائف `db-integration` لكل خدمة على PostgreSQL حقيقي.
- حكم CI الحالي (القسم 1.4) يُثبت أن وظائف `db-integration` الـ14 خضراء.

### 1.3 validate-migrations وحالات الطفرة

- `scripts/checks/validate-migrations.sh` (25KB) موجود ويُشغَّل في `governance-guard`.
- `scripts/checks/test-governance.sh` يُشغِّل حالات طفرة تُثبت أن الحارس يرفض ويعطي فعلاً.
- حكم CI: وظيفة `governance-guard` خضراء في الشوط الحالي (القسم 1.4).
- `verify-governance.sh` المحلي: **23 فحصاً ناجحاً** (الفحص 8: CI مانع — تخطّي مُعلَن: لقطة مؤرخة لا قراءة حيّة؛ الفحص 15: تدقيق أحكام CI — جزئي مُعلَن).

### 1.4 حكم CI المرتبط بالالتزام الحالي

| البيان | القيمة |
|---|---|
| SHA main الحالي | `07ba94c20f1d9ba9c0d0e0e604850d83413a69dc` |
| Run ID (WASLA CI) | [35525855417](https://github.com/skyosv10-art/wasla/actions/runs/35525855417) |
| الحكم | **success** |
| عدد الوظائف | **33** (typecheck · test · verify · governance-guard · 14 × db-integration · 10 × exit-gate-e2e · image-supply-chain · doc-coverage · repo-structure) |
| Run ID (Roadmap freshness) | [35525855396](https://github.com/skyosv10-art/wasla/actions/runs/35525855396) |
| الحكم | **success** |

**هذا حكم CI حقيقي على main اليوم، لا حكم تاريخي.**

---

## 2. إعادة تقييم RISK-0020

### 2.1 السبب المسجَّل لبقائه مفتوحاً

نص RISK-0020 يقول:

> «الخطرُ يبقى مفتوحاً بسببٍ واحدٍ مُعلَنٍ لا بسببِ فجوةٍ فنّيّةٍ: لا حكمَ CI في المستودعِ كلِّهِ (`RISK-0039`)، فلا بوّابةَ تُقفِلُه»

### 2.2 القياس الحالي

- **RISK-0039 (حجب فوترة CI):** ارتفع الحجب في 2026-09-14. RISK-0039 نفسه يوثِّق في تحديثه أنَّ «الحجبُ ارتفعَ فعلاً، وأوّلُ حكمٍ حقيقيٍّ صدرَ» وأنَّ «الشرطُ الأوّلُ استُوفيَ وأُتمِتَ الثاني» (حكم أخضر على main مرّتين متتاليتين).
- **حكم CI اليوم:** أخضر بالكامل على main عند `07ba94c` — 33 وظيفة + Roadmap freshness، كلها `success`.
- **الفجوة الفنّيّة:** مغلقة بالكامل — 14 خدمة/حزمة منتظمة، 13 ملف برهان ترقية، `validate-migrations.sh` بـ5 أبواب يعمل في CI.

### 2.3 الاستنتاج

السبب المسجَّل لبقاء RISK-0020 مفتوحاً («لا حكمَ CI») **لم يعد صادقاً**. حكم CI أخضر ومقيس اليوم. الفجوة الفنّيّة (غياب الترحيلات) مغلقة بالكامل.

**لكن:** RISK-0020 هو خطر `high` على مستوى المخاطر، وإقفاله قرار مالك البرنامج. ما يُكتب هنا هو **القياس**، لا الإقفال. المالك يقرأ هذا القياس ويقرر.

---

## 3. M0-23 و M0-34 — الحالة الصحيحة وفق STATUS_MODEL

### 3.1 M0-23 (نظام ترحيلات عكوسة)

| الطبقة | القياس | الحكم |
|---|---|---|
| Implementation | 14 خدمة/حزمة بـ journal + ترحيل أساس + رفيق ترجع | ✅ الكود مكتوب والنطاق مغطّى |
| Verification | `validate-migrations.sh` أخضر محلياً + في CI | ✅ مُثبَت محلياً |
| Gate (CI) | `governance-guard` أخضر في CI على `07ba94c` | ✅ CI بدأ وأنتج دليلاً |
| GATE.md | **غير موجودة** | ❌ بوّابة العنصر لم تُنشأ |

**الحالة الصحيحة وفق STATUS_MODEL:** الأدلة الثلاثة (Implementation + Verification + Gate-CI) **مُجتمعة**. لكن M0-23 ليس له ملف `M0-23_GATE.md`، فالحالة `GATED` لا تُمنح بلا بوّابة العنصر. الحالة الصحيحة: **`READY FOR GATE`** — VERIFIED محلياً وبوّابته لم تُنشأ بعد.

**قرار `COMPLETED`:** للمالك وحده (§9 + STATUS_MODEL §2.8).

### 3.2 M0-34 (برهان الترقية على قاعدة ببيانات)

| الطبقة | القياس | الحكم |
|---|---|---|
| Implementation | 13 ملف `migration-upgrade-with-data.integration.test.ts` + الباب 4-ج في `validate-migrations.sh` | ✅ الكود مكتوب والنطاق مغطّى |
| Verification | `validate-migrations.sh` يؤكد تغطية كل ترحيل غير أساس | ✅ مُثبَت محلياً |
| Gate (CI) | `governance-guard` أخضر في CI على `07ba94c` | ✅ CI بدأ وأنتج دليلاً |
| GATE.md | **غير موجودة** | ❌ بوّابة العنصر لم تُنشأ |

**الحالة الصحيحة وفق STATUS_MODEL:** نفس M0-23 — **`READY FOR GATE`**.

**ملاحظة:** اللوحة تقول لحظة القياس إنّ CI = `NOT VERIFIED · account_billing_blocked (RISK-0039)`. **هذا القول قديم:** حكم CI اليوم أخضر، والحجب ارتفع في 2026-09-14. القول الصادق اليوم: CI أخضر على `07ba94c`.

---

## 4. مراجعة M2-04 / M2-05 / M2-06 / M2-07 / M2-10 — Status Drift

### 4.1 M2-04 (config schema وenv examples)

| البعد | القياس |
|---|---|
| العمل البرمجي المتبقّي | **لا يوجد** — الشرطان مُنفَّذان (`CLM-0220`/`CLM-0221`) |
| الدليل/البوّابة المتبقّية | `M2-04_GATE.md` موجودة |
| قرار المالك | مطلوب للنقل إلى `Completed` (§9) |
| اعتماد خارجي | لا يوجد |
| الحالة على اللوحة | `In Progress` |
| Status drift | **العمل مكتمل والبوّابة موجودة** — الحالة الصحيحة: `Ready for Gate` |

### 4.2 M2-05 (migrations موحدة) — التناقض الدائري

| البعد | القياس |
|---|---|
| العمل البرمجي المتبقّي | **لا يوجد** — 13 خدمة بـ schema.sql + journal + migrate.ts، upgrade/repair drill مُنجَز (`CLM-0232`، 13/13 ضد Supabase pooler PostgreSQL 17.6) |
| الدليل/البوّابة المتبقّية | **لا توجد `M2-05_GATE.md`** |
| قرار المالك | مطلوب للنقل إلى `Completed` (§9) |
| اعتماد خارجي | لا يوجد |
| الحالة على اللوحة | `In Progress` — السبب المسجَّل: «M2-06 (backup/restore) غير منجز» |
| Status drift | **السبب قديم:** M2-06 drill مُنجَز (`CLM-0234`) — الحالة الصحيحة: `Ready for Gate` |

### 4.3 M2-06 (backup/restore/RPO-RTO) — التناقض الدائري

| البعد | القياس |
|---|---|
| العمل البرمجي المتبقّي | **لا يوجد** — backup/restore drill مُنجَز (`CLM-0234`، 13/13 ضد Supabase pooler، RPO/RTO مُعرَّفة، `ADR-040`) |
| الدليل/البوّابة المتبقّية | **لا توجد `M2-06_GATE.md`** |
| قرار المالك | مطلوب للنقل إلى `Completed` (§9) |
| اعتماد خارجي | لا يوجد |
| الحالة على اللوحة | `In Progress` — السبب المسجَّل: «M2-05 migrations لا تزال In Progress (M2-06 يعتمد عليها)» |
| Status drift | **تناقض دائري:** M2-05 يقول «أبقى In Progress لأن M2-06 غير منجز» وM2-06 يقول «أبقى In Progress لأن M2-05 In Progress» — وكلاهما منجز فعلاً |

### 4.4 التناقض الدائري M2-05 ↔ M2-06 — التشخيص

```
M2-05 → "أبقى In Progress لأن M2-06 غير منجز"
             ↓
M2-06 → "أبقى In Progress لأن M2-05 In Progress"
             ↓
  (حلقة لا تنتهي)
```

**التمييز المطلوب:**

1. **عمل برمجي متبقٍ:** لا يوجد في أيٍّ منهما. كلاهما أنجز drill كاملاً (13/13 خدمة).
2. **دليل/بوّابة متبقّية:** لا توجد `M2-05_GATE.md` ولا `M2-06_GATE.md`.
3. **قرار مالك:** مطلوب للنقل إلى `Completed` لكليهما (§9).
4. **اعتماد خارجي:** لا يوجد. (على عكس M2-07 المحجوب على `RENDER_API_KEY`/`RENDER_OWNER_ID`).

**الحالة الصحيحة لكليهما:** `Ready for Gate` — العمل مكتمل والدليل مرفق، لكن بوّابة العنصر لم تُنشأ وقرار المالك لم يصدر.

### 4.5 M2-07 (workers/outbox/ticks/DLQ)

| البعد | القياس |
|---|---|
| العمل البرمجي المتبقّي | G1–G7 مُغلقة، G8 مفتوحة (`Expected` — لا مُجدوِل نبضة، شأن نشر في نطاق M2-02/M2-09) |
| الدليل/البوّابة المتبقّية | `M2-07_GATE.md` موجودة وتقول `NOT PASSED` — البند 7 محجوب |
| قرار المالك | مطلوب للنقل (§9) |
| اعتماد خارجي | **موجود** — `RENDER_API_KEY`/`RENDER_OWNER_ID` (M2-02 `BLOCKED`) |
| الحالة على اللوحة | `In Progress` |
| Status drift | **لا يوجد drift** — الحالة صادقة: اعتماد محجوب والبوّابة تقول `NOT PASSED` |

### 4.6 M2-10 (P0 Executive Visual Roadmap)

| البعد | القياس |
|---|---|
| العمل البرمجي المتبقّي | `P0_EXECUTIVE_ROADMAP.md` + `.svg` موجودان |
| الدليل/البوّابة المتبقّية | معيار القبول: `owner review + CI green` |
| قرار المالك | مطلوب للمراجعة |
| الحالة على اللوحة | `Ready for Gate` |
| Status drift | **لا يوجد drift** — الحالة صادقة: بانتظار مراجعة المالك |

---

## 5. ملخص التسوية

| العنصر | الحالة على اللوحة | الحالة الصحيحة وفق القياس | السبب |
|---|---|---|---|
| M0-23 | In Progress | **Ready for Gate** | الأدلة الثلاثة مجتمعة، لكن لا `M0-23_GATE.md` |
| M0-34 | In Progress | **Ready for Gate** | الأدلة الثلاثة مجتمعة، لكن لا `M0-34_GATE.md` |
| RISK-0020 | open (سبب: لا CI) | **سبب البقاء قديم** — CI أخضر اليوم | الفجوة الفنّيّة مغلقة، CI أخضر على `07ba94c` |
| M2-04 | In Progress | **Ready for Gate** | العمل مكتمل + `M2-04_GATE.md` موجودة |
| M2-05 | In Progress (سبب: M2-06) | **Ready for Gate** | العمل مكتمل، السبب قديم |
| M2-06 | In Progress (سبب: M2-05) | **Ready for Gate** | العمل مكتمل، تناقض دائري |
| M2-07 | In Progress | **In Progress (صادقة)** | اعتماد محجوب على M2-02 |
| M2-10 | Ready for Gate | **Ready for Gate (صادقة)** | بانتظار مراجعة المالك |

**قاعدة الحوكمة:** لا تُغيَّر أي حالة إلى `Completed` تلقائياً. كل تغيير مسجَّل هنا مربوط بقياس قابل لإعادة التشغيل من الشجرة الحالية. قرار `Completed` للمالك وحده (§9).

---

## 6. ما لا يُدَّعى

- لا يُدَّعى أنَّ M0-23 أو M0-34 أو M2-04 أو M2-05 أو M2-06 `Completed` — هذا قرار المالك.
- لا يُدَّعى أنَّ RISK-0020 مُغلَق — هذا قرار المالك. ما يُقال: السبب المسجَّل لبقائه مفتوحاً لم يعد صادقاً.
- لا يُدَّعى أنَّ `verify-governance.sh` المحلي بديل عن حكم CI — الفحص 8 (CI مانع) تخطّي مُعلَن: لقطة مؤرخة لا قراءة حيّة. حكم CI الفعلي مقروء من GitHub Actions في القسم 1.4.
- لا يُدَّعى أنَّ `test-governance.sh` شُغِّل محلياً في هذه الجلسة — الوقت تجاوز حدّ الـsandbox. حكم CI (القسم 1.4) يُثبت أنَّ `governance-guard` أخضر، وهو يُشغِّل `test-governance.sh` في CI.
