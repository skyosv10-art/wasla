# بوّابةُ M0-05 — تفعيلُ بروتوكولِ الحوكمةِ ونظامِ منعِ تكرارِ العملِ

> **العنصر:** M0-05 · **الاعتماديّة:** — · **Last Updated:** 2026-09-02 · **الحالةُ المُعلَنة:** `GATED` — **لا** `COMPLETED`

---

## 0. الحالةُ بثلاثِ طبقاتٍ

```
LOCAL      ✅ VERIFIED     — بوّابةُ الحوكمةِ 12 فحصاً · وحزمةُ إثباتٍ 153/0
CI         ✅ VERIFIED     — governance-guard نجحت في خمسةِ تشغيلاتٍ
PRODUCTION ⚪ NOT VERIFIED — لا طبقةَ نشرٍ (وهي غيرُ ذاتِ صلةٍ بعنصرِ حوكمةٍ)
```

**الدليلُ المشتركُ للطبقةِ الوسطى:** [`ci-evidence/2026-09-02T050000Z-m0-governance-gates/`](ci-evidence/2026-09-02T050000Z-m0-governance-gates/README.md).

---

## 1. المعيارُ المُعلَنُ وسببُ تعليقِه — نصّاً

> **معيارُ اللوحةِ:** وظيفةُ `governance-guard` خضراءُ على MR الدمجِ + `ADR-017`

> **وما كُتبَ في خانةِ الدليلِ حرفاً:** «**الدليلُ غيرُ مستوفى: البوّابةُ لم تُنفَّذ في MR !81 لنقصِ رصيدِ CI (`ci_quota_exceeded`، 0/21 وظيفةً بدأت) لا لعيبٍ في التغييرِ.** التحقُّقُ محلّيٌّ: 5/5 بوّاباتٍ و14/14 حالةَ إثباتٍ. **يبقى `Ready for Gate` حتى تركضَ البوّابةُ فعلاً**.»

فالشرطُ الذي وضعَه العنصرُ على نفسِه شرطٌ **واحدٌ محدَّدٌ**: أن **تركضَ** الوظيفةُ. وهذه البوّابةُ لا تُثبِتُ غيرَه.

---

## 2. بنودُ البوّابةِ

| # | البند | النتيجة | الدليل |
|---|---|---|---|
| 1 | **الوظيفةُ رَكَضت فعلاً — لا مرّةً بل خمساً** | ✅ | `governance-guard` → `success` في [`33566652310`](https://github.com/skyosv10-art/wasla/actions/runs/33566652310) · [`33567256847`](https://github.com/skyosv10-art/wasla/actions/runs/33567256847) · [`33569807797`](https://github.com/skyosv10-art/wasla/actions/runs/33569807797) (`push` · `main`) · [`33573216083`](https://github.com/skyosv10-art/wasla/actions/runs/33573216083) · [`33577596261`](https://github.com/skyosv10-art/wasla/actions/runs/33577596261). الحقولُ الخامُ في `jobs-*.json` |
| 2 | **`started_at ≠ null` — وهو الفرقُ الذي كانَ كلَّ الفرقِ** | ✅ | في التشغيلاتِ الخمسةِ: **26/26 وظيفةً لها `started_at`**. وسجلُّ `M0-05` كانَ يقولُ «0/21 وظيفةً بدأت»؛ **فالمقابلةُ بينَ الرقمَينِ هي الدليلُ**، لا خُضرةُ اليومِ وحدَها |
| 3 | **البوّاباتُ نمت من 5 إلى 12 وكلُّها تُنفَّذُ** | ✅ | سجلُّ CI المُلتزَمُ [`verify.raw.txt`](ci-evidence/2026-09-01T104148Z-m0-21-github-actions/job-logs/verify.raw.txt) ينتهي بـ`✓ نجح 11) الأساسُ الآليُّ` · `✓ نجح 12) تغطيةُ إنفاذِ هويّةِ الخدمة` · `✓ الحوكمة: كل الفحوص المُنفَّذة نجحت` |
| 4 | **حزمةُ الإثباتِ نمت من 14 إلى 153 حالةً** | ✅ | سجلُّ CI [`governance-guard.raw.txt`](ci-evidence/2026-09-01T104148Z-m0-21-github-actions/job-logs/governance-guard.raw.txt) ينتهي بـ`═══ النتيجة: 153 ناجح · 0 فاشل ═══`، ونُفِّذت اليومَ محلّيّاً فأجابت العددَ نفسَه |
| 5 | **الحزمةُ تُثبِتُ الرفضَ لا القَبولَ** | ✅ | أكثرُ حالاتِها موسومةٌ `(كما هو متوقع: fail)` — أي **طفراتٌ**: «سجلٌّ بلا علامةِ بدايةٍ يُسقِط» · «شجرةٌ مُعدَّلةٌ بلا سببٍ مكتوبٍ تُسقِط» · «شفرةٌ توقِّعُ وسجلٌّ يقول مؤجَّل يُسقِط» |
| 6 | **وعندَ الجهلِ تُعلِنُ التخطّيَ ولا تدَّعي** | ✅ | حالتانِ صريحتانِ في سجلِّ CI: `✓ عند الجهل يُعلن التخطّي ولا يدّعي نجاحاً كاملاً` · `✓ غيابُ pnpm يُعلَن تخطّياً بالرمزِ 2 لا نجاحاً` — وهذا **جوهرُ العنصرِ** لا تفصيلٌ فيه |
| 7 | **`ADR-017` موجودٌ لا مذكورٌ** | ✅ | [`ADR-017-unified-roadmap-governance-and-work-claim-system.md`](../15-decisions/ADR-017-unified-roadmap-governance-and-work-claim-system.md) |
| 8 | **نظامُ منعِ التكرارِ يعملُ ويرفضُ** | ✅ | `validate-work-claims.sh` رفضَ دفعاتٍ حقيقيّةً مُسجَّلةً في [`WORK_CLAIMS.md`](../16-progress/WORK_CLAIMS.md): «عنصرُ العملِ M1-02 محجوز مرتين في وقتٍ واحد» · و«✗ رفض حجز العمل: لا حجز نشط للفرع feat/m0-21-ci-evidence» في التشغيلِ [`33499994547`](https://github.com/skyosv10-art/wasla/actions/runs/33499994547). **وهذا رفضٌ في الخطِّ لا محلّيّاً** |
| 9 | **وهو يرفضُ كاتبَه أيضاً — مقيسٌ في هذه الدفعةِ** | ✅ | مُنِعَ في هذه الدفعةِ نفسِها من العملِ على `M0-02` لأنّ `CLM-0004` نشطٌ لمالكٍ بشريٍّ؛ ومُنِعَ من العملِ على `M0-08` و`M1-03` لحجزَينِ نشطَينِ. **فالحارسُ ليس مانعاً للغيرِ وحدَه** |
| 10 | **عيوبُه مُسجَّلةً لا مطويّةً** | ✅ | `M0-20` عنصرُ عملٍ قائمٌ: فحصُ التكرارِ يقرأُ عنصرَ العملِ **ولا يقرأُ عمودَ المالكِ** فيُعامِلُ جهةً واحدةً على فرعَينِ معاملةَ جهتَينِ. **العيبُ معروفٌ ومُسمّىً وله عنصرٌ** — وهذا شرطُ الحوكمةِ لا نقضُها |

**المحصّلة: 10 من 10 ✅ — البوّابةُ `PASSED`.**

---

## 3. حدودٌ مُعلَنةٌ

1. **الفحصُ الثامنُ في البوّابةِ تخطٍّ مُعلَنٌ** (رفضُ الدمجِ غيرُ مُنفَذٍ · `M0-22A` محجوبٌ · `RISK-0002`). فالحوكمةُ **تقيسُ وتُعلِنُ** ولا **تُغلِقُ زرَّ الدمجِ** اليومَ. وهذا حدُّ إنفاذٍ لا حدُّ قياسٍ، وهو **بابُ `M0-22A` لا بابُ هذا العنصرِ**.
2. **الخُضرةُ في تشغيلاتٍ دفعاتُها وثائقُ في أكثرِها** — والوظيفةُ نفسُها تُقاسُ بها الشفرةُ والوثائقُ سواءً، لكنّ **قوّةَ الدليلِ ليست واحدةً**: أقواها التشغيلُ `33569807797` على `main` والتشغيلانِ على فرعِ `M0-22B` (شفرةُ خطٍّ حقيقيّةٌ).
3. **الحارسُ لا يقرأُ `.github/workflows/ci.yml` بأبوابِ `validate-ci-mandatory.sh`** — حدٌّ مُعلَنٌ في [ADR-023](../15-decisions/ADR-023-ci-migration-to-github-actions.md) §5 ونطاقُه `M0-22`.
4. **`M0-20` مفتوحٌ** — عيبُ عمودِ المالكِ لم يُصلَح، ولم يُصلَح **قصداً** في دفعةٍ يُقاسُ فيها الحارسُ نفسُه.

---

## 4. القرارُ والسلطةُ

**البوّابةُ `PASSED` · الحالةُ `GATED`.**

**والشرطُ الذي وضعَه العنصرُ على نفسِه — «حتى تركضَ البوّابةُ فعلاً» — استُوفيَ حرفاً:** رَكَضت خمسَ مرّاتٍ بـ26/26 وظيفةً بدأت، بعدَ أن كانت 0/21.

**و`COMPLETED` سلطةُ مالكِ البرنامجِ وحدَه** ([البروتوكول](../16-progress/ROADMAP_OPERATING_PROTOCOL.md) §9) · وعمودُ اللوحةِ يبقى `Ready for Gate`.
