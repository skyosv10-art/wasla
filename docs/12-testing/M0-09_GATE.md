# بوّابةُ M0-09 — تصحيحُ تأكيداتِ القيودِ بعدَ ترقيةِ Drizzle

> **العنصر:** M0-09 · **الاعتماديّة:** M0-02 · **Last Updated:** 2026-09-02 · **الحالةُ المُعلَنة:** `GATED` — **لا** `COMPLETED`

---

## 0. الحالةُ بثلاثِ طبقاتٍ

```
LOCAL      ✅ VERIFIED     — PostgreSQL 18.6 حقيقيّةٌ · 79/79 · وطفرةٌ تُثبِتُ الحساسيّةَ
CI         ✅ VERIFIED     — db-integration/drivers على postgres:15 · success · 79/79
PRODUCTION ⚪ NOT VERIFIED — لا طبقةَ نشرٍ في المستودعِ
```

**الدليلُ المشتركُ:** [`ci-evidence/2026-09-02T060000Z-m0-09-m0-10-drivers-db/`](ci-evidence/2026-09-02T060000Z-m0-09-m0-10-drivers-db/README.md).

---

## 1. المعيارُ المُعلَنُ وحدُّه التاريخيُّ — نصّاً

> **معيارُ اللوحةِ:** `drivers-db-integration` **بلا فشلٍ من هذا السبب**

> **وما كُتبَ في خانةِ الدليلِ حرفاً:** «**⚠ حدُّ دليلي: لا Postgres في بيئتي، فلم أُشغِّل `drivers-db-integration` ولا أُصادق على رقمِ `79/79` الواردِ في سجلِّ الفرعِ الأصليّ — رقمُ كاتبِه لا رقمي.** ما تحقّقتُ منه بنفسي: `typecheck` نظيفٌ، و`vitest` **195/195** … **البوّابةُ هي التي تُصادق على تشغيلِ قاعدةِ البيانات**.»

فالحدُّ كانَ **حدَّ بيئةٍ لا حدَّ شفرةٍ**، والشرطُ الذي وضعَه العنصرُ على نفسِه شرطٌ واحدٌ: أن تُشغَّلَ الوظيفةُ على قاعدةٍ حقيقيّةٍ. **وقد زالَ الحدُّ في الطبقتَينِ معاً** — لا نُقِلَ الرقمُ بل أُعيدَ قياسُه.

---

## 2. بنودُ البوّابةِ

| # | البند | النتيجة | الدليل |
|---|---|---|---|
| 1 | **الوظيفةُ رَكَضت على قاعدةٍ حقيقيّةٍ في الخطِّ** | ✅ | `db-integration` · ساقُ `drivers` · `postgres:15` · **`success`** في التشغيلِ [`33566652310`](https://github.com/skyosv10-art/wasla/actions/runs/33566652310)، و`started_at ≠ null` |
| 2 | **«بلا فشلٍ من هذا السبب» — مقيسٌ لا مُقدَّرٌ** | ✅ | السجلُّ الخامُّ المُلتزَمُ [`drivers-db.raw.txt`](ci-evidence/2026-09-02T000000Z-m0-22b-remaining-jobs/job-logs/drivers-db.raw.txt): `Test Files 3 passed (3)` · `Tests 79 passed (79)` — **0 إخفاقٍ**، فلا إخفاقَ من هذا السببِ ولا من غيرِه |
| 3 | **الرقمُ الذي رُفضَتِ المصادقةُ عليه صار مقيساً بيدي** | ✅ | محلّيّاً على PostgreSQL **18.6**: `Tests 79 passed (79)` — [`restored-79.raw.txt`](ci-evidence/2026-09-02T060000Z-m0-09-m0-10-drivers-db/local-measurements/restored-79.raw.txt). **مصدرانِ مستقلّانِ، إصدارانِ مختلفانِ، والعددُ واحدٌ** |
| 4 | **السببُ الأصليُّ للعيبِ صحيحٌ كما وُصف** | ✅ | `drizzle-orm@0.45` يلفُّ خطأَ Postgres في `DrizzleQueryError`، فالتأكيدُ على `constraint` يلزمُه النزولُ إلى `cause`. والدالّةُ المُصلَحةُ `expectRefusedBy` تعملُ فعلاً: هي التي أخفقت وحدَها في اختبارِ الطفرةِ (البند 5) |
| 5 | **التأكيداتُ المُصلَحةُ تُشخِّصُ ولا تُجامِلُ — بالطفرةِ** | ✅ | أُعيدَ القيدُ إلى صورتِه المعطوبةِ فأخفقَ **اختبارٌ واحدٌ بعينِه** لا الحزمةُ: `1 failed | 78 passed (79)` بنصٍّ `promise resolved … instead of rejecting` — [`mutation-array_length.raw.txt`](ci-evidence/2026-09-02T060000Z-m0-09-m0-10-drivers-db/local-measurements/mutation-array_length.raw.txt). **فالتأكيدُ يقرأُ القيدَ فعلاً، ولو كان تجميليّاً لبقيَ أخضرَ** |
| 6 | **القيدُ رفضَ فعلاً — بقلمِ الخادمِ لا بقلمِ الاختبارِ** | ✅ | سجلُّ CI السطرُ 449: `ERROR: new row for relation "driver_eligibility_log" violates check constraint "ck_eligibility_log_reasons"` مع `DETAIL: Failing row contains (… ineligible, {}, …)` |
| 7 | **الخطأُ الحسابيُّ المُعلَنُ (151 → 150) مُصحَّحٌ** | ✅ | `8+13+1+128 = 150`، وحدُّ العمودِ 192 — والاختبارُ يمرُّ في الخطِّ ومحلّيّاً معاً (البندانِ 2 و3) |
| 8 | **العقدُ لم ينحرف عن الشفرةِ** | ✅ | `contract-drift.test.ts` و`schema-drift.test.ts` ضمنَ المجموعةِ الخضراءِ؛ والطفرةُ عُدِّلت في **الملفَّينِ معاً** لأنّ الحارسَ يرفضُ تعديلَ أحدِهما وحدَه |
| 9 | **التكرارُ لا الصدفةُ** | ✅ | ساقُ `drivers` نفسُها `success` في التشغيلاتِ الخمسةِ المُوثَّقةِ في [بوّاباتِ الحوكمةِ](ci-evidence/2026-09-02T050000Z-m0-governance-gates/README.md) — 26/26 في كلٍّ منها |
| 10 | **ما لا يُثبِتُه هذا البابُ مُعلَنٌ** | ✅ | خُضرةُ اليومِ **إخباريّةٌ لا مانعةٌ**: `RISK-0002` مفتوحٌ و`M0-22A` محجوبٌ، فدمجُ طلبٍ أحمرَ ما زال ممكناً. **والبوّابةُ تُعلِنُ هذا ولا تطويه** |

**المحصّلة: 10 من 10 ✅ — البوّابةُ `PASSED`.**

---

## 3. حدودٌ مُعلَنةٌ

1. **فرقُ الإصدارِ:** المحلّيُّ **18.6** والخطُّ **`postgres:15`**. والحُجّةُ الحاكمةُ سجلُّ CI؛ والمحلّيُّ يُضيفُ ما لا يُضيفُه الخطُّ: **الطفرةَ**.
2. **`nproc = 2`** في بيئةِ القياسِ المحلّيِّ.
3. **الرقمُ التاريخيُّ `79/79` (2026-08-25) لا يُصادَقُ عليه بأثرٍ رجعيٍّ.** ما يُصادَقُ عليه قياسُ اليومِ وحدَه — والنصُّ التاريخيُّ في اللوحةِ **لم يُحرَّر**، فالخطأُ يُصحَّحُ ولا يُمحى.
4. **هذا البابُ يحكمُ على `M0-09` وحدَه.** بندُ «ترحيلٌ عكوس» بندُ `M0-10`، وحكمُه هناك **ليس `PASSED`** — فلا يُقرأُ خُضرةُ هذه الوثيقةِ خُضرةً للعنصرَينِ.

---

## 4. القرارُ والسلطةُ

**البوّابةُ `PASSED` · الحالةُ `GATED`.**

**والشرطُ الذي وضعَه العنصرُ على نفسِه — «البوّابةُ هي التي تُصادق على تشغيلِ قاعدةِ البيانات» — استُوفيَ حرفاً**، وزِيدَ عليه ما لم يشترطه: طفرةٌ تُثبِتُ أنّ التأكيداتِ تكشفُ العيبَ لو عادَ.

**و`COMPLETED` سلطةُ مالكِ البرنامجِ وحدَه** ([البروتوكول](../16-progress/ROADMAP_OPERATING_PROTOCOL.md) §9) · وعمودُ اللوحةِ يبقى `Ready for Gate`.
