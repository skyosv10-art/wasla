# بوّابةُ M0-13 — تطبيعُ مالكِ الوكيلِ في فحصِ تقاطعِ النطاقات

> **العنصر:** M0-13 · **يعتمد على:** M0-12 · **اللوحة:** [`LAUNCH_EXECUTION_BOARD.md`](../16-progress/LAUNCH_EXECUTION_BOARD.md)
>
> **القاعدةُ الحاكمة:** [`WORK_CLAIM_RULE.md`](../00-rules/WORK_CLAIM_RULE.md) §6 · **نموذجُ الحالات:** [`STATUS_MODEL.md`](../00-rules/STATUS_MODEL.md)
>
> **Last Updated:** 2026-09-02 · **الحالةُ المُعلَنة:** `READY FOR GATE` — **لا** `COMPLETED`

---

## 0. الحالةُ بثلاثِ طبقاتٍ قبلَ أيِّ جدولٍ

```
LOCAL      ✅ VERIFIED     — طفرتانِ في الاتّجاهَينِ، كلتاهما أسقطت ما تحرسه
CI         ✅ VERIFIED     — التشغيلُ 33590373349 على الرأسِ cc18e581bc: 26 وظيفةً · 26 بدأت · 26 success · 0 لم تبدأ · 0 أخفقت
PRODUCTION ⚪ NOT VERIFIED — لا طبقةَ نشرٍ في المستودعِ
```

**وخضرةُ CI إخباريّةٌ لا مانعةٌ** (`M0-22A` محجوبٌ · `RISK-0002` مفتوحٌ).

---

## 1. المعيارُ المُعلَنُ حرفاً

> **«حالةٌ في `test-governance.sh` تُثبت أنّ `@uxxxu` و`@uxxxu (agent:x)` جهةٌ واحدةٌ فلا يُرفض تقاطعُهما، وحالةٌ ثانيةٌ تُثبت أنّ `@uxxxu` و`@other` جهتان فيُرفض تقاطعُهما (ألّا يكون العلاجُ تعطيلاً للفحص) · ثمّ بوّابةٌ خضراءُ في CI»**

**فالمعيارُ ثلاثيٌّ:** موجبةٌ · سلبيّةٌ · بوّابةٌ. **والاتّجاهُ السلبيُّ هو روحُ البند**: أن يكونَ التطبيعُ **تضييقاً محسوباً** لا تعطيلاً للفحصِ في ثوبِ إصلاحٍ.

---

## 2. البنودُ مقيسةً — بالطفرةِ لا بالقراءةِ

| # | البند | النتيجة | الدليلُ الذي يُراجَع |
|---|---|---|---|
| 1 | العلاجُ قائمٌ في الشفرةِ | ✅ | `scripts/checks/validate-work-claims.sh`: `owner_party()` (السطر 149) تُرجِع ما قبلَ ` (agent:`، وتُطبَّق على طرفَي المقارنةِ في فحصِ التقاطعِ **وحدَه** (السطر 160) |
| 2 | ولم يُوسَّع إلى غيرِ موضعِه | ✅ | التحقّقُ من صيغةِ المالكِ باقٍ كما هو، ولا مقارنةَ مالكٍ في `find-existing-work.sh` ولا `require-doc-update.sh` |
| 3 | الحزمةُ خضراءُ كاملةً على شجرةٍ نظيفةٍ | ✅ | **153 ناجحاً · 0 فاشلاً** — [`gov-base.raw.txt`](ci-evidence/2026-09-02T083000Z-m0-11-m0-14-governance-guards/local-measurements/gov-base.raw.txt) |
| 4 | **الاتّجاهُ الموجبُ تشخيصيٌّ** — بإرجاعِ المقارنةِ الحرفيّةِ تسقط الحالاتُ التي يحرسُها التطبيعُ | ✅ | طفرةٌ: `[[ "$(owner_party "$oi")" == "$(owner_party "$oj")" ]]` ← `[[ "$oi" == "$oj" ]]` ⇒ **149 ناجحاً · 4 فاشلاً** — [`mut-m0-13.raw.txt`](ci-evidence/2026-09-02T083000Z-m0-11-m0-14-governance-guards/local-measurements/mut-m0-13.raw.txt) |
| 5 | **الاتّجاهُ السلبيُّ تشخيصيٌّ** — بتعطيلِ الدالّةِ في ثوبِ تطبيعٍ تسقط السلبيّتانِ وحدَهما | ✅ | طفرةٌ: `owner_party()` تُرجِع ثابتاً واحداً للجميعِ ⇒ سقطت **«يرفض تقاطع نطاق بين مالكين مختلفين»** و**«يرفض تقاطع وكيلَينِ لصاحبَينِ مختلفَين»** — [`mut-m0-13b.raw.txt`](ci-evidence/2026-09-02T083000Z-m0-11-m0-14-governance-guards/local-measurements/mut-m0-13b.raw.txt) |
| 6 | **والتطبيعُ حاملٌ للحملِ اليومَ لا زينةٌ تاريخيّةٌ** | ✅ | السجلُّ الحيُّ على `main` يحملُ الآنَ `CLM-0004` (`@uxxxu` · `services/drivers/`) و`CLM-0029` (`@uxxxu (agent:computer)` · `services/drivers/`) — جهةٌ واحدةٌ بصيغتَينِ. ولذلك أسقطت طفرةُ البندِ 4 حالةَ **«السجلُّ الحقيقيُّ في المستودعِ سليمٌ»**: بلا تطبيعٍ **يرفض الحارسُ سجلَّ `main` نفسَه** |

### 2.1 تصحيحٌ لرقمٍ تاريخيٍّ — يُعلَن ولا يُطوى

خانةُ الدليلِ على اللوحةِ تقول: «بإرجاعِ المقارنةِ الحرفيّةِ فشلت الموجبةُ **وحدَها** (27/1)». **وقياسُ اليومِ أربعُ حالاتٍ لا واحدةٌ (149/4).** والفرقُ **ليس انحداراً** ولا تكذيباً للرقمِ القديمِ؛ سببُه مقروءٌ: الحزمةُ يومَها 28 حالةً واليومَ 153، **ومحتوى السجلِّ الحيِّ تغيَّر** فصار فيه تقاطعُ وكيلٍ مع صاحبِه (`CLM-0004` ✕ `CLM-0029`) لم يكن موجوداً حينَها، فصارت حالاتٌ إضافيّةٌ تعتمد على التطبيعِ. **فالقُطريّةُ التامّةُ (حالةٌ واحدةٌ) لم تعُد صحيحةً اليومَ**، وتُكتَب كما قِيست.

---

## 3. دليلُ CI

| العدّ | القيمة |
|---|---|
| مجموعُ الوظائفِ | **26** |
| بدأت | **26** |
| `success` | **26** |
| لم تبدأ / أخفقت | **0** / **0** |

**التشغيلُ:** [`33590373349`](https://github.com/skyosv10-art/wasla/actions/runs/33590373349) · الرأسُ `cc18e581bc130ce09c1fd64b20f9a34e3b13cb4c` · قُرِئت العدَّاداتُ من `GET /actions/runs/33590373349/jobs` لا من لونِ الشارةِ.


والبندُ الثالثُ من المعيارِ («ثمّ بوّابةٌ خضراءُ في CI») **لا يُدَّعى استيفاؤه قبلَ قراءةِ التشغيلِ الفعليِّ**؛ تُملأ الخانتانِ أعلاه من التشغيلِ على رأسِ هذا الفرعِ ثمّ يُحسَم. وهو البندُ الذي عطَّلَ إغلاقَ هذا العنصرِ منذُ 2026-08-26 بسببِ `ci_quota_exceeded` على المُشغِّلِ القديمِ.

---

## 4. الحدودُ المُعلَنةُ

1. **الطفرتانِ محلّيّتانِ** ولم تُدفَعا قطُّ: الملفُّ نُسخ احتياطيّاً واستُعيد، و`git diff --stat scripts/` فارغةٌ بعدَ كلِّ طفرةٍ. **ولا طفرةَ في أيِّ إيداعٍ.**
2. **طفرةُ البندِ 5 أسقطت معها حالةً ثالثةً** («verify-governance يعمل في سياق git») سقوطاً تابعاً لا مستقلّاً — فالحكمُ على القُطريّةِ يُقرأ في **الحالتَينِ المقصودتَينِ** لا في مجموعِ العددِ.
3. **البندُ 6 يصفُ حالةَ السجلِّ يومَ 2026-09-02**؛ فلو حُرِّرت `CLM-0004` أو `CLM-0029` لعادَ التطبيعُ غيرَ حاملٍ للحملِ في السجلِّ الحيِّ — **وتبقى الحالاتُ الصناعيّةُ هي الحارسَ الدائمَ**، وهي المقصودةُ بالمعيار.
4. **التصحيحُ المذكورُ في اللوحةِ سابقاً** («قولي إنّ `CLM-0005`/`CLM-0006` اضطُرّا … غيرُ صحيحٍ») **مُقَرٌّ به هنا ولا يُمحى** — الخطأُ يُصحَّح ولا يُطمَس.

---

## 5. الحكمُ

**البنودُ الستّةُ ✅**، والمعيارُ الثلاثيُّ مُستوفىً: **موجبةٌ تشخيصيّةٌ** (البند 4) · **سلبيّتانِ تشخيصيّتانِ** (البند 5) · **بوّابةٌ خضراءُ في CI** (§3).

**الحكمُ: ✅ PASSED — محلّيّاً وفي الخطِّ (26/26 · التشغيلُ 33590373349)** — البندانِ الأوّلانِ من المعيارِ مُستوفَيانِ بالطفرةِ، والثالثُ (بوّابةٌ خضراءُ في CI) يُحسَم بقراءةِ التشغيلِ لا بالتوقُّعِ. ومع ذلك تصحيحُ الرقمِ التاريخيِّ 27/1 إلى 149/4 بسببٍ مكتوبٍ (§2.1).

**والنقلُ إلى `Completed` سلطةُ مالكِ البرنامجِ وحدَه.**
