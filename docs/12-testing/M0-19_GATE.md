# بوّابةُ M0-19 — الحارسُ يقرأ حجوزاتِ الفرعِ كلَّها لا أوّلَها

> **العنصر:** M0-19 · **يعتمد على:** M0-14 · **اللوحة:** [`LAUNCH_EXECUTION_BOARD.md`](../16-progress/LAUNCH_EXECUTION_BOARD.md)
>
> **القاعدةُ الحاكمة:** [`WORK_CLAIM_RULE.md`](../00-rules/WORK_CLAIM_RULE.md) §6 · **نموذجُ الحالات:** [`STATUS_MODEL.md`](../00-rules/STATUS_MODEL.md)
>
> **Last Updated:** 2026-09-02 · **الحالةُ المُعلَنة:** `READY FOR GATE` — **لا** `COMPLETED`

---

## 0. الحالةُ بثلاثِ طبقاتٍ قبلَ أيِّ جدولٍ

```
LOCAL      ✅ VERIFIED     — طفرةٌ قُطريّةٌ تامّةٌ: إعادةُ `break` أسقطت الحالةَ الموجبةَ وحدَها (152 · 1)
CI         ✅ VERIFIED     — 26 وظيفةً · 26 بدأت · 26 success · 0 أخفقت (التشغيل 33596056706 على e31b2ca)
PRODUCTION ⚪ NOT VERIFIED — لا طبقةَ نشرٍ في المستودعِ
```

**وخضرةُ CI إخباريّةٌ لا مانعةٌ** (`M0-22A` محجوبٌ · `RISK-0002` مفتوحٌ).

---

## 1. المعيارُ المُعلَنُ حرفاً

> **«اتّحادُ نطاقاتِ كلِّ حجزٍ نشطٍ على الفرعِ الواحدِ، وحالتانِ في `test-governance.sh`: حجزانِ على فرعٍ واحدٍ يُغطّيانِ ملفَّينِ في نطاقَينِ مختلفَينِ → قبولٌ · وملفٌّ خارجَ الاتّحادِ كلِّه → رفضٌ · وطفرةٌ تُسقط الحالةَ الأولى وحدَها»**

**والحالتانِ متلازمتانِ بقصدٍ:** الأولى وحدَها يُرضيها اتّحادٌ يقبل كلَّ شيءٍ، والثانيةُ تُثبت أنّ الاتّحادَ **حدٌّ لا إلغاءٌ للحدِّ**.

---

## 2. البنودُ مقيسةً — بالطفرةِ لا بالقراءةِ

| # | البند | النتيجة | الدليلُ الذي يُراجَع |
|---|---|---|---|
| 1 | العلاجُ قائمٌ في الشفرةِ | ✅ | [`validate-work-claims.sh`](../../scripts/checks/validate-work-claims.sh) السطور 220-225: حلقةٌ تجمع `MY_SCOPES` و`MY_CID` من **كلِّ** صفٍّ يطابق الفرعَ — **بلا `break`** |
| 2 | والمعرِّفاتُ تُطبَع كلُّها لا أوّلُها | ✅ | `MY_CID="${MY_CID:+$MY_CID + }$cid"` — فرسالةُ الرفضِ تُسمّي الحجوزاتِ المعتبَرةَ جميعاً (`CLM-…​ + CLM-…`) |
| 3 | والاتّحادُ لا يُرخِّص شيئاً جديداً | ✅ | كلُّ صفٍّ مرَّ قبلَه على فحصِ البنيةِ (عنصرُ عملٍ موجودٌ · مالكٌ · مهلةٌ ≤ 14 يوماً) وفحصِ التقاطعِ بينَ المالكينَ — فالمجموعُ نطاقٌ **مُعلَنٌ مسبقاً في السجلِّ** لا توسيعٌ صامتٌ بعدَ الكتابةِ |
| 4 | الحزمةُ خضراءُ كاملةً على شجرةٍ نظيفةٍ | ✅ | **153 ناجحاً · 0 فاشلاً** — [`base.raw.txt`](ci-evidence/2026-09-02T130000Z-m0-15-m0-19-source-and-target-guards/local-measurements/base.raw.txt) |
| 5 | **الحالةُ الموجبةُ تشخيصيّةٌ (طفرةٌ قُطريّةٌ)** | ✅ | بإعادةِ `break` إلى الحلقةِ سقطت **«يقبل ملفَّينِ يغطّيهما حجزانِ نشطانِ على الفرعِ نفسِه (M0-19)» وحدَها**: **152 · 1**، ورسالةُ الرفضِ حرفيّاً «رفض حجز العمل: ملفات مُعدّلة خارج نطاق الحجز CLM-8001» — [`mut-m0-19.raw.txt`](ci-evidence/2026-09-02T130000Z-m0-15-m0-19-source-and-target-guards/local-measurements/mut-m0-19.raw.txt) |
| 6 | **والحالةُ السالبةُ بقيت خضراءَ تحتَ الطفرةِ نفسِها** | ✅ | «يرفض ملفّاً خارجَ اتّحادِ الحجزَينِ معاً» لم تسقط — وهذا **هو الدليلُ على أنّ الاتّحادَ حدٌّ**: لو كان العلاجُ «اقبل كلَّ شيءٍ» لسقطت هي أوّلاً على الشجرةِ النظيفةِ |
| 7 | والقُطريّةُ تامّةٌ | ✅ | طفرةٌ واحدةٌ · حالةٌ واحدةٌ ساقطةٌ · 152 ناجحاً — فالحالةُ تشخيصيّةٌ لا تجميليّةٌ |
| 8 | وشجرةُ الشفرةِ سليمةٌ بعدَ القياسِ | ✅ | `git diff --stat scripts/` **فارغةٌ** — [`measure.sh`](ci-evidence/2026-09-02T130000Z-m0-15-m0-19-source-and-target-guards/local-measurements/measure.sh) |

---

## 3. دليلُ CI — يُقرأُ عدَّينِ منفصلَينِ لا لوناً واحداً

| العدّ | القيمة |
|---|---|
| التشغيل | [`33596056706`](https://github.com/skyosv10-art/wasla/actions/runs/33596056706) على الرأسِ `e31b2ca` (حدثُ `pull_request`) |
| مجموعُ الوظائفِ | **26** |
| بدأت | **26** (`started_at` غيرُ فارغٍ في كلِّها) |
| `success` | **26** |
| لم تبدأ / أخفقت | **0 / 0** |

**و«لم تبدأ» ≠ «أخفقت» ≠ «الكودُ أخفق»**.

---

## 4. الحدودُ المُعلَنةُ

1. **الرقمُ التاريخيُّ «143/0 و142/1» لا يُصادَقُ عليه بأثرٍ رجعيٍّ ولا يُنكَر.** قياسُ اليومِ **153/0** و**152/1**، والفرقُ نموُّ الحزمةِ لا انحدارٌ. والمقيسُ هنا قياسُ اليومِ وحدَه.
2. **الاتّحادُ يُقاس على الحجوزاتِ النشطةِ فقط** — وحداثةُ تلك الحجوزاتِ (أفرعُها قائمةٌ؟) حراسةُ عنصرٍ آخرَ (`M0-16`)، ولا يُدَّعى أنّها من نطاقِ هذا العنصرِ.
3. **ولا يُدَّعى أنّ الاتّحادَ يمنع تجزئةَ العملِ**: مالكٌ واحدٌ يستطيع أن يفتح حجزَينِ بنطاقَينِ واسعَينِ على فرعٍ واحدٍ فيتّسع الاتّحادُ. الحدُّ الحقيقيُّ هناك **مهلةُ الأربعةَ عشرَ يوماً وفحصُ التقاطعِ بينَ المالكينَ**، لا هذا العنصرُ.
4. **الطبقةُ محلّيّةٌ** (bash 5.3.9 · nproc=2)؛ والطفرةُ لا تُقاس إلّا محلّيّاً بطبيعتِها.

---

## 5. الحكمُ

**البنودُ الثمانيةُ ✅**، والمعيارُ **مُستوفىً بحالتَينِ متلازمتَينِ وطفرةٍ قُطريّةٍ تامّةٍ**: إعادةُ `break` أسقطت الموجبةَ وحدَها، وبقاءُ السالبةِ خضراءَ يُثبت أنّ العلاجَ **توسيعُ قراءةٍ لا إلغاءُ حدٍّ**.

**الحكمُ: ✅ PASSED محلّيّاً · ✅ مُثبَتٌ في CI (26/26 · 26 بدأت · 0 أخفقت)**

**والنقلُ إلى `Completed` سلطةُ مالكِ البرنامجِ وحدَه.**
