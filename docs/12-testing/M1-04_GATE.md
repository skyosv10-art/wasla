# بوّابةُ M1-04 — الوسيطُ المركزيُّ لإنفاذِ هويّةِ الخدمة

> **العنصر:** M1-04 · **القرارات:** [ADR-020](../15-decisions/ADR-020-service-to-service-identity.md) §6.1 (سلَّمُ الحدودِ) · [ADR-021](../15-decisions/ADR-021-service-token-replay-policy.md) · [ADR-022](../15-decisions/ADR-022-service-auth-key-rotation.md)
>
> **السجلُّ الحاكمُ:** [`SERVICE_AUTH_ENFORCEMENT.md`](../07-security/SERVICE_AUTH_ENFORCEMENT.md) — بوّابتُه (الفحصُ 12) تُثبِتُ كلَّ صفٍّ في سجلِّه
>
> **نموذجُ الحالات:** [`STATUS_MODEL.md`](../00-rules/STATUS_MODEL.md)
>
> **Last Updated:** 2026-09-13 · **الحالةُ المُعلَنة:** `READY FOR GATE` — البوّابةُ نفسُها `PASSED` (بنودُها ✅ كلُّها)، **ونقلُ `M1-04` إلى `Completed` سلطةُ مالكِ البرنامجِ وحدَه** ([ROADMAP_OPERATING_PROTOCOL](../16-progress/ROADMAP_OPERATING_PROTOCOL.md) §9)، ومدخلاتُ قرارِه (`AUD-004`/`AUD-005`) مفتوحةٌ لا تُحسمُ من هنا.

---

## 0. الحالةُ بثلاثِ طبقاتٍ قبلَ أيِّ جدولٍ

```
LOCAL      ✅ VERIFIED        — verify.sh كاملٌ أخضرُ (2026-09-13 · 4447 اختباراً في 272 ملفاً · typecheck رمزُ 0)
CI (بوّابةُ الموجةِ 5) ✅ VERIFIED — التشغيلُ 34065473979 · 27 وظيفةً · 27 بدأت · 27 success (2026-09-07)
CI (اليومَ) 🔴 UNAVAILABLE   — لا حكمَ خطٍّ على ما بعدَ 2026-09-12T11:15Z: كلُّ وظيفةٍ `failure` بـ`steps` صفراً (عطبُ فواتيرِ الحساب · CI_RUNNER_UNBLOCK)
PRODUCTION ⚪ NOT VERIFIED    — لا طبقةَ نشرٍ في المستودع (infra/* فيها .gitkeep وحدَها)
```

**والطبقةُ الأخيرةُ لا تُقرَأُ خُضرةً على أنّها إنتاجٌ ولا يُدَّعى ذلك.** وسطرُ
بوّابةِ الموجةِ 5 قياسٌ على رأسِ دفعتِها لا انتسابٌ عامٌّ لخُضرةِ `main`: التشغيلُ
[34065473979](https://github.com/skyosv10-art/wasla/actions/runs/34065473979) جرى
على رأسِ [PR #47](https://github.com/skyosv10-art/wasla/pull/47) (`c020c8c`) —
وهو اليومَ **سلفٌ لـ`main`** (التزامُ الدمجِ `d2bf46f`، وفحصُ التضمينِ `git
merge-base --is-ancestor` ناجحٌ)، فالشجرةُ التي قِيسَت هي الشجرةُ المدموجة.

> **وسطرُ «CI (اليومَ)» أُضيفَ بالإضافةِ لا بتعديلِ السطرِ الأخضرِ ولا بمحوِه.**
> الخُضرةُ المقيسةُ في 2026-09-07 حقيقةٌ تاريخيّةٌ على شجرةٍ مدموجةٍ ولا تُمحى؛
> لكنَّها **لا تُنسَبُ إلى ما بعدَها**. ومنذُ 2026-09-12T11:15Z يُخفِقُ كلُّ
> تشغيلٍ على `main` وعلى كلِّ فرعٍ بـ31/31 `failure` و`steps` طولُها صفرٌ —
> **عطبُ حسابٍ لا عطبُ شفرةٍ** (آخرُ أخضرَ على `main`:
> [34675601381](https://github.com/skyosv10-art/wasla/actions/runs/34675601381)،
> 2026-09-12T05:26:48Z)، وعلاجُهُ في
> [`CI_RUNNER_UNBLOCK.md`](../14-runbooks/CI_RUNNER_UNBLOCK.md). فالموجةُ السادسةُ
> وما بعدَها **لا حكمَ خطٍّ لها**، والأخضرُ المحلّيُّ لا يُقدَّمُ بديلاً عنه.

---

## 1. بنودُ البوّابةِ

| # | البند | النتيجة | الدليلُ الذي يُراجَع |
|---|---|---|---|
| 1 | **الوسيطُ المركزيُّ واحدٌ لا خمسة** | ✅ | `packages/service-auth/src/fastify.ts` — كلُّ الربطِ في الحزمةِ، ولا يبقى في الخدمةِ إلّا جمهورُها وصلاحيّاتُها ومغلَّفُ خطئِها؛ و`audience` و`denialBody` إلزاميّانِ بلا افتراضٍ (الموجةُ 1 · [PR #38](https://github.com/skyosv10-art/wasla/pull/38)) |
| 2 | **حدودُ المحرِّكاتِ الخمسةُ مفروضةٌ كلُّها، ومعَها حدُّ التوصيلِ (ستةٌ)** | ✅ | `matching` (M1-03) · `orders` (موجةُ 2) · `identity` (موجةُ 3) · `dispatch` (موجةُ 4) · `geography` (موجةُ 5 · [PR #47](https://github.com/skyosv10-art/wasla/pull/47)) · **`delivery`** (موجةُ 6 · [PR #117](https://github.com/skyosv10-art/wasla/pull/117) · مدموجٌ 2026-09-12) — والفحصُ 12 (`validate-service-auth-coverage.sh`) يُثبِتُ الحدَّ في الشفرةِ لا في الوعدِ؛ و`delivery` خارجَ المحرِّكاتِ فُرِضَ **قبلَ أن يوجدَ لهُ مُنادٍ إنتاجيٌّ** ([السجلُّ §2.7](../07-security/SERVICE_AUTH_ENFORCEMENT.md)) |
| 3 | **بلا هويّةٍ ⇒ 401 قبلَ 404** | ✅ | المصفوفةُ الأربعُ في اختباراتِ الإثباتِ **الستةِ** (`service-identity.test.ts` لكلِّ حدٍّ، وحدُّ التوصيلِ **24 حالةً** بعدَ 22/N)؛ ومسارٌ مجهولٌ بلا رمزٍ ⇒ 401 قبلَ 404 في كلِّ حدٍّ |
| 4 | **رسالةُ الرفضِ لا تُسرِّبُ سرَّها** | ✅ | اختبارٌ صريحٌ في كلِّ حدٍّ: رسالةُ 401 لا تُطابِقُ `توقيع\|منته\|kid\|صلاحي` — فالخارجُ يعرفُ أنّه مرفوعَةً لا لماذا |
| 5 | **الصلاحيّاتُ أدقُّ من حدٍّ واحدَ** | ✅ | `geography` بأربعِ صلاحيّاتٍ (`hierarchy:read` · `zone:read` · `location:read` · `location:write`) و`dispatch` بسبعٍ و**`delivery` بإحدى عشرةَ** (الكتلةُ المُعلَّمةُ في §5 أدناهُ) — ورمزُ قراءةٍ لا يفتحُ كتابةً (403) ورمزُ منطقةٍ لا يفتحُ منطقةً أخرى (401 · ADR-021 §4) |
| 6 | **منعُ الإعادةِ حيٌّ على كلِّ حدٍّ** | ✅ | رمزٌ مُعادٌ ⇒ 401 في كلِّ حدٍّ؛ وتعذُّرُ متجرِ الإعادةِ ⇒ **503 لا مرورَ** (`SERVICE_AUTH_REPLAY_STORE_UNAVAILABLE`) |
| 7 | **مسارٌ بلا تصنيفٍ لا يمرُّ صامتاً** | ✅ | حارسُ الإقلاعِ (`onRoute`) يُسقِطُ الخدمةَ عندَ بدئِها — اختبارٌ صريحٌ في كلِّ حدٍّ بمسارٍ بلا `config.serviceIdentity` |
| 8 | **`/health` مفتوحٌ بتصنيفٍ صريحٍ لا بسكوتٍ** | ✅ | `config.serviceIdentity: "open"` في كلِّ حدٍّ — الافتراضُ منعٌ لا سماحٌ |
| 9 | **كلُّ عميلٍ صادرٍ في `services/` موقَّعٌ** | ✅ | [السجلُّ §4](../07-security/SERVICE_AUTH_ENFORCEMENT.md): **11 موقِّعاً من 11 عميلاً صادراً — لا مؤجَّلَ**، والحارسُ (الفحصُ 12) يُثبِتُه في الشفرةِ بعشرِ اختباراتِ طفرةٍ |
| 10 | **الرمزُ المولَّدُ يحملُ الوجهةَ والصلاحيّةَ المطلوبتَين** | ✅ | اختباراتُ العملاءِ تفكُّ حِملَ الرمزِ وتقرأُ `aud` و`scp`: `customers`/`matching`/`drivers` إلى `geography` بصلاحيّةِ `zone:read` وحدَها |
| 11 | **بوّاباتُ الخروجِ تُثبِتُ السلوكَ بعدَ النشرِ** | ✅ | الحزمُ الثماني (`exit-gate-e2e`) تُشغِّلُ الخدماتِ المُنفَّذةَ كحدودٍ **مفروضةٍ** — ومنها الخمسُ التي تُشغِّلُ الجغرافيا ووقَّعت نداءاتِها (11 · 16 · 13 · 14 · 8 اختباراتٍ) |
| 12 | **مُنادو الحدودِ خارجَ `services/` موقَّعونَ** | ⚠️ جزئيٌّ | `bots/customer-bot/src/customer-core.ts` وُقِّعَ (كشفَه فحصُ الأنواعِ في الموجةِ 5) · و`packages/bot-runtime/src/identity-bootstrap.ts` موقَّعٌ منذُ الموجةِ 3 — لكنَّ **حارسَ التغطيةِ لا يرى `bots/` ولا `packages/` أصلاً** (**`RISK-0027` مفتوحٌ**): سلامتُهم بقياسٍ لا بحارسٍ |
| 13 | **دليلُ CI** | ✅ **للموجاتِ 1–5 وحدَها** | التشغيلُ [34065473979](https://github.com/skyosv10-art/wasla/actions/runs/34065473979) على رأسِ دفعةِ الموجةِ 5: **27 وظيفةً · 27 `started_at ≠ null` · 27 `success`** — والخامُّ والحكمُ في [`ci-evidence/2026-09-06T231308Z-m1-04-ci-gate/`](ci-evidence/2026-09-06T231308Z-m1-04-ci-gate/README.md). **أمّا الموجةُ 6 وما بعدَها فلا حكمَ خطٍّ لها** (§0 أعلاه) — وهذا يُقالُ ولا يُلفَّفُ |
| 14 | **تحديثُ الوثائقِ معَ الدفعاتِ** | ✅ | [السجلُّ الحاكمُ](../07-security/SERVICE_AUTH_ENFORCEMENT.md) (بعدَّ كلِّ موجةٍ) · اللوحةُ · سجلُّ المهامِّ · الأساسُ الآليُّ (`BASELINE.json` مُعادٌ من سجلٍّ أخضرَ في المستودعِ) · خارطةُ P0 |
| 15 | **ما لا يُدَّعى صريحاً** | ✅ | §2 أدناه — الحدودُ الـ14 الباقيةُ تطبيقاتٌ وأطرٌ لم تُبنَ · `AUD-004`/`AUD-005` مفتوحانِ · `RISK-0015` و`RISK-0027` مفتوحانِ · لا مصفوفةَ تفويضٍ (`M1-05`) · ولا يُدَّعى أنّ متغيّراتِ البيئةِ مُعَدّةٌ في أيِّ نشرٍ |
| 16 | **وثيقتا الحدِّ لا تنحرفانِ عن الشفرةِ بصمتٍ** | ✅ | حرسٌ يقرأُ `DELIVERY_SCOPES` من الشفرةِ ويُطابِقُها بالكتلتَينِ المُعلَّمتَينِ في هذهِ البوّابةِ وفي السجلِّ §2.7 (`service-auth-docs-drift.test.ts` · 7 حالاتٍ) — أُنشِئَ لأنَّ الجدولَينِ بقيا يُعلِنانِ **تسعَ** صلاحيّاتٍ والحدُّ يفرضُ **إحدى عشرةَ** (المراجعةُ 22/N) |

**المحصّلة: 15 بنداً ✅ · بندٌ واحدٌ ⚠️ جزئيٌّ (12) — البوّابةُ `PASSED` بحدِّها المُعلَن، والبندُ 16 أُضيفَ في 22/N ولم يُرفَعْ بندٌ من موضِعِه.**

> **البندُ 12 جزئيٌّ لا فاشلٌ:** المُنادونَ الموجودونَ موقَّعونَ كلُّهم اليومَ
> (مقيساً، لا وعداً)، لكنَّ **الحارسَ الذي يمنعُ انحدارَهم أعمى عن نصفِ
> المستودعِ** — فسلامتُهم اليومَ تقومُ على قياسِ لحظةٍ لا على منعٍ دائمٍ. وهذا
> عيبٌ بنيويٌّ مُسجَّلٌ خطراً (`RISK-0027` · فتحَهُ `M0-25`) لا يُطوى بإعلانٍ،
> وعلاجُهُ توسيعُ مسارِ الحارسِ — عنصرٌ لاحقٌ بحجزٍ مستقلٍّ.

---

## 2. ما لا تُدَّعيهُ هذهُ البوّابةُ

1. **«التغطيةُ الكاملةُ»** لا تُقالُ: حدودُ HTTP الـ14 المذكورةُ في خارطةِ P0 أوسعُ
   من الحدودِ المفروضةِ — البقيّةُ تطبيقاتٌ (`apps/` كلُّها `placeholder`) وأطرٌ
   وقنواتٌ **لم تُنفَّذْ أصلاً**، فلا معنى لفرضِ حدٍّ على خدمةٍ لا وجودَ لها. الذي
   يُدَّعى بدقّةٍ: **كلُّ محرِّكٍ مُنفَّذٍ يفرضُ الهويّةَ**، ومعَهُ **حدُّ التوصيلِ**
   (الموجةُ 6) وهوَ خارجُ المحرِّكاتِ. و**`services/marketplace` مُنفَّذٌ وغيرُ
   مفروضٍ** — يُقالُ صريحاً هنا لا في حاشيةٍ: هوَ الحدُّ المُنفَّذُ الوحيدُ الباقي
   بلا فرضٍ، وفرضُهُ عنصرٌ لاحقٌ بحجزٍ مستقلٍّ.
2. **`AUD-004`/`AUD-005`** تدقيقانِ مفتوحانِ على البيئةِ المُنشَرةِ (متغيّراتُ
   البيئةِ · التدويرُ في نشرٍ حقيقيٍّ) — **لم يُقاسا لأنّ لا نشرَ يُقاسُ عليه**،
   وحسمُهما قرارُ المالكِ لا قرارَ وكيلٍ.
3. **مخزنُ الإعادةِ في الذاكرةِ** (`RISK-0015`) — يصلحُ لنسخةٍ واحدةَ ولا يصلحُ
   لتعدُّدِ النسخِ؛ والدَّينُ موثَّقٌ لا منسيٌّ.
4. **مصفوفةُ التفويضِ** (`M1-05`) لم تبدأْ — التوثيقُ ليسَ التفويضَ، والصلاحيّاتُ
   المفروضةُ أدقُّ من `authenticated` وحدَها لكنها ليست مصفوفةَ أدوارٍ.
5. **ولا يُدَّعى أنّ متغيّراتِ البيئةِ مُعَدّةٌ في أيِّ نشرٍ** — لم يُقَسْ ذلك،
   ولم يوجدْ نشرٌ يقيسُ عليه.

---

## 3. الدليلُ المقيسُ للموجةِ 5

**محلّيّاً** (سجلٌّ خامٌّ محفوظٌ في المستودعِ:
[`ci-evidence/2026-09-07T010000Z-m1-04-wave5-geography-enforcement/`](ci-evidence/2026-09-07T010000Z-m1-04-wave5-geography-enforcement/README.md)):

- `pnpm -r typecheck` رمزُ 0 · `pnpm -r test` **3816 ناجحاً · 0 فاشلٍ** (226 ملفاً).
- `geography` 54/54 (منها 13 حالةَ إثباتٍ جديدةً) · `matching` 172/172 ·
  `customers` 117/117 · `drivers` 196/196.
- بوّابةُ الحوكمةِ (12 فحصاً) وحزمةُ اختبارِها (174 حالةً) أخضرانِ.

**على الخطِّ** (غيرُ محلّيٍّ · [دليلُ البوّابة](ci-evidence/2026-09-06T231308Z-m1-04-ci-gate/VERDICT.md)):

- التشغيلُ [34065473979](https://github.com/skyosv10-art/wasla/actions/runs/34065473979)
  على رأسِ [PR #47](https://github.com/skyosv10-art/wasla/pull/47): **27 وظيفةً · 27 بدأت · 27 نجحت · 0 أخفقت**.
- وفيها `exit-gate-e2e` الثماني (الخمسُ التي تُشغِّلُ الجغرافيا بحدٍّ مفروضٍ) و`db-integration` الثلاثَ عشرةَ و`governance-guard` و`verify`.

---

## 3.1 الموجةُ 6 وما بعدَها — حدُّ التوصيلِ (2026-09-12 · 2026-09-13)

**ما أُضيفَ بعدَ آخرِ تحديثٍ لهذهِ البوّابةِ (2026-09-07)، مقيساً لا موصوفاً:**

| الدفعةُ | ما فُرِضَ | الدليلُ المقيسُ |
|---|---|---|
| **الموجةُ 6** · المراجعةُ 17/N · [PR #117](https://github.com/skyosv10-art/wasla/pull/117) (مدموجٌ 2026-09-12T05:13:59Z · `cbda5d6`) | حدُّ التوصيلِ كلُّه: **9 مساراتٍ مُغلَقةٍ بـ9 صلاحيّاتٍ**، و`/delivery/health` و`/delivery/ready` مفتوحانِ **بقرارٍ مكتوبٍ** | **20 حالةَ إثباتٍ** في `service-identity.test.ts` · ثلاثةُ أكوادِ رفضٍ (`AUTHN_EXPIRED` · `AUTHN_AUDIENCE_MISMATCH` · `AUTHN_UNAUTHENTICATED`) · بوّابةُ خروجِ الطورِ 13 تبني الحدَّ **مفروضاً** على مُستمعٍ حقيقيٍّ وتُوقِّعُ 18 موضعَ نداءٍ · [السجلُّ §2.7](../07-security/SERVICE_AUTH_ENFORCEMENT.md) · [ADR-026](../15-decisions/ADR-026-store-orders-and-delivery-boundary.md) §4.19 |
| المراجعةُ 18/N · [PR #119](https://github.com/skyosv10-art/wasla/pull/119) | **المسارُ الثاني عشرَ** (إقرارُ رايةِ التضاربِ) بالصلاحيّةِ **العاشرةِ** `delivery:ops:inventory-conflicts:acknowledge` — مفصولةً عن `:read` بقصدٍ | 10 حالاتٍ في `inventory-conflict-acknowledgement-http.test.ts`، ومنها `obo` في الرمزِ ⇒ الإنسانُ في العمودِ · ADR-026 §4.20 |
| المراجعةُ 21/N · [PR #125](https://github.com/skyosv10-art/wasla/pull/125) (مدموجٌ 2026-09-13T02:45:32Z · `516f9c1`) | **المسارُ الثالثَ عشرَ** (دفترُ الرسائلِ الميتةِ) بالصلاحيّةِ **الحاديةَ عشرةَ** `delivery:ops:relay-dead-letters:read` | 32 حالةً في ثلاثِ طبقاتٍ (14 وحدةً · 10 على HTTP · 8 تكامُلاً على PostgreSQL) · ADR-026 §4.23 · [الوثيقةُ](../13-observability/DELIVERY_RELAY_DEAD_LETTERS.md) |
| **المراجعةُ 22/N** (هذهِ الدفعةُ) | **رفعُ دَينِ قياسٍ ودَينِ وثيقةٍ** لا فرضٌ جديدٌ: المسارانِ 12 و13 كانا مُصنَّفَينِ بصلاحيّةٍ في `app.ts` **ولم يُقَسْ فرضُهما بلا توقيعٍ**، والجدولانِ في هذهِ البوّابةِ وفي السجلِّ بقيا يُعلِنانِ **تسعَ** صلاحيّاتٍ | `service-identity.test.ts` **20 ⇒ 24 حالةً**: كلُّ مسارٍ منهما بلا توقيعٍ ⇒ **401**، ورمزُ `inventory-conflicts:read` لا يُقِرُّ رايةً ⇒ **403** ولا يقرأُ دفترَ التوزيعِ الفاسدَ ⇒ **403** · وحرسُ الانحرافِ `service-auth-docs-drift.test.ts` (**7 حالاتٍ**) يجعلُ الشفرةَ مصدرَ الجدولَينِ |

**والمحصّلةُ المقيسةُ اليومَ على هذا الحدِّ:** **13 مساراً** — **11 مُغلَقاً بـ11
صلاحيّةً** (صلاحيّةٌ لكلِّ مسارٍ، انظرْ §5) و**2 مفتوحانِ بقصدٍ مُعلَنٍ**. وحزمةُ
`services/delivery` **508 ناجحاً · 0 فاشلٍ في 33 ملفاً**، وبوّابةُ خروجِ الطورِ 13
**19/19** (قِيسَت في 21/N).

**وما لا يُدَّعى في هذا القسمِ:**

1. **لا حكمَ CI لأيٍّ من هذهِ الدفعاتِ** — لا للموجةِ 6 ولا لِما بعدَها (§0):
   الحسابُ يُخفِقُ كلَّ وظيفةٍ قبلَ أن تبدأَ، والدمجُ جرى **بدليلٍ محلّيٍّ
   بإذنِ مالكِ البرنامجِ** وهذا مُسجَّلٌ في سجلِّ المهامِّ لا مطويٌّ.
2. **`services/marketplace` ما زالَ غيرَ مفروضٍ**، فتوقيعُ التوصيلِ الصادرُ
   إليهِ يُقبَلُ لأنَّهُ **لا يُفحَصُ** — والموجةُ 6 لا تُصلِحُ ذلكَ.
3. **`api.openapi.yml` لم يُمَسَّ** في أيٍّ من الموجاتِ الستِّ: العقدُ المنشورُ
   لا يُعلِنُ `401`/`403` ولا `securitySchemes` — **نقصٌ حقيقيٌّ** علاجُهُ عقدٌ
   لكلِّ الحدودِ معاً.
4. **ولا يُدَّعى أنَّ حدَّ التوصيلِ جُرِّبَ في نشرٍ**: لا طبقةَ نشرٍ، ولا
   مُنادٍ إنتاجيٌّ لهُ بعدُ — وهذا بالضبطِ ما جعلَ فرضَهُ الآنَ أرخصَ وقتٍ.

---

## 4. المدخلاتُ الباقيةُ لقرارِ المالكِ

| المدخلُ | حالتُهُ | ما يلزمُه |
|---|---|---|
| `AUD-004` (مصادقةٌ على الحدودِ غيرُ مكتملةٍ) | مفتوحٌ | حدودُ المحرِّكاتِ صارت مفروضةً كلُّها؛ البقيةُ تعريفاتٌ فردّيّةٌ للـAUD ونشرٌ يُقاسُ عليه |
| `AUD-005` (حمايةُ مساراتِ البوتِ) | مفتوحٌ | مُنادو البوتِ موقَّعونَ، ومساراتُهُ الداخليّةُ عنصرُ `M1-07` |
| `RISK-0027` | مفتوحٌ | توسيعُ حارسِ التغطيةِ ليُبصِرَ `bots/` و`packages/` — عنصرٌ لاحقٌ |
| `RISK-0015` | مفتوحٌ | مخزنُ إعادةٍ دائمٌ عندَ أوّلِ نشرٍ متعدِّدِ النسخِ |

**ونقلُ `M1-04` إلى `Completed` — إن رأى المالكُ — يقومُ على:** البوّابةِ أعلاه
(`PASSED` ببندٍ جزئيٍّ مُسجَّلٍ) + دليلِ الخطِّ 27/27 + إقفالِ `AUD-004`/`AUD-005`
أو قرارٍ مكتوبٍ بأنّهما تُقاسانِ على النشرِ (`M2-09`) لا على الشفرةِ.

---

## 5. صلاحيّاتُ حدِّ التوصيلِ — كتلةٌ يقرأُها حرسٌ لا عينٌ

الكتلةُ أدناهُ **عقدٌ آليٌّ** لا جدولُ زينةٍ:
[`service-auth-docs-drift.test.ts`](../../services/delivery/src/__tests__/service-auth-docs-drift.test.ts)
يقرأُ `DELIVERY_SCOPES` من الشفرةِ ويُطابِقُها بما بينَ العلامتَينِ هنا وفي
[السجلِّ §2.7](../07-security/SERVICE_AUTH_ENFORCEMENT.md) — فلا صلاحيّةٌ مفروضةٌ
تغيبُ عن هذهِ البوّابةِ، ولا صلاحيّةٌ مذكورةٌ هنا لا وجودَ لها في الشفرةِ. وحذفُ
العلامتَينِ **يُسقِطُ الحرسَ** فيُسقِطُ الاختبارَ، لا يُمرِّرُهُ.

<!-- delivery-scopes:begin -->

| # | الصلاحيّة | أُضيفت في |
|---|---|---|
| 1 | `delivery:store-order:write` | الموجةُ 6 (17/N) |
| 2 | `delivery:store-order:read` | الموجةُ 6 (17/N) |
| 3 | `delivery:store-order:cancel` | الموجةُ 6 (17/N) |
| 4 | `delivery:store-order:confirm` | الموجةُ 6 (17/N) |
| 5 | `delivery:payment-mirror:write` | الموجةُ 6 (17/N) |
| 6 | `delivery:fulfillment:transition` | الموجةُ 6 (17/N) |
| 7 | `delivery:delivery-task:read` | الموجةُ 6 (17/N) |
| 8 | `delivery:ops:idempotency-sweep` | الموجةُ 6 (17/N) |
| 9 | `delivery:ops:inventory-conflicts:read` | الموجةُ 6 (17/N) |
| 10 | `delivery:ops:inventory-conflicts:acknowledge` | 18/N (ADR-026 §4.20) |
| 11 | `delivery:ops:relay-dead-letters:read` | 21/N (ADR-026 §4.23) |

<!-- delivery-scopes:end -->

**وما لا يُثبِتُهُ هذا الحرسُ:** يُطابِقُ **الأسماءَ** لا صحّةَ تعليلِها ولا ربطَ
كلِّ صلاحيّةٍ بمسارِها — والربطُ يُثبَتُ على السلكِ في `service-identity.test.ts`
(401/403 بلا توقيعٍ) لا في جدولٍ. ولا يمسُّ حدوداً أخرى (`orders` · `dispatch` ·
`geography` …): توسيعُهُ إليها عملٌ مستقلٌّ بحجزٍ مستقلٍّ.
