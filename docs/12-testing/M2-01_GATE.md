# بوّابةُ M2-01 — صورةُ الحاويةِ وقائمةُ الموادِ وفحصُ الثغراتِ

> **العنصر:** `M2-01` · **الحجز:** `CLM-0195` · **القرارُ:** [ADR-033](../15-decisions/ADR-033-container-image-supply-chain.md)
>
> **السجلُّ الحاكمُ:** [`CONTAINER_IMAGES.md`](../08-infrastructure/CONTAINER_IMAGES.md) — ومصدرُ حقيقتِهِ الشجرةُ نفسُها (`services/*` · `bots/*` · `packageManager`)
>
> **نموذجُ الحالات:** [`STATUS_MODEL.md`](../00-rules/STATUS_MODEL.md)
>
> **Last Updated:** 2026-09-16 · **الحالةُ المُعلَنة:** `IMPLEMENTED` — **وحكمُ CI لم يُقرأْ بعدُ** (يُملأُ البندُ 10 والطبقةُ الثانيةُ من سجلِّ تشغيلٍ حقيقيٍّ بعدَ الدفعِ، ولا يُفترَضُ).

---

## 0. لماذا يوجد هذا الملف

لأنَّ هذا البندَ بالذاتِ **كانَ مكتوباً مرّةً وغيرَ قابلٍ للبناءِ**: أرتفاكتٌ
حاضرٌ في فرعٍ مفتوحٍ ([PR #205](https://github.com/skyosv10-art/wasla/pull/205))
يُقرأُ إنجازاً وهوَ لا يُبنى، ولا وظيفةَ في الخطِّ تُظهِرُ عطبَهُ. فالبوّابةُ هيَ
الموضعُ الذي **يُفصَلُ فيهِ ما قِيسَ عن ما يُدَّعى**، وحدودُ الحارسِ تُكتَبُ فيهِ
صريحةً قبلَ أن يقرأَها أحدٌ خُضرةً عامّةً.

**وأخصُّ ما يجبُ ألّا يُقرأَ خطأً:** أخضرُ الفحصِ 19 **إعدادٌ متّسقٌ** لا
**صورةٌ تُبنى**. البناءُ والعقدُ داخلَ الصورةِ والثغراتُ حكمُها وظيفةُ
`image-supply-chain` وحدَها. **ولا دوكر في بيئةِ التنفيذِ المحليّةِ أصلاً**، فما
لم يُقَسْ محليّاً يُكتَبُ «لم يُقَسْ» لا «نجحَ».

---

## 1. الحالةُ بثلاثِ طبقاتٍ قبلَ أيِّ جدولٍ

```
LOCAL      ✅ VERIFIED       — `scripts/verify.sh` أخضرُ بكاملِهِ: 19 فحصاً · 421 حالةَ طفرةٍ · 4737 اختباراً · الأنواعُ. **والصورةُ لم تُبْنَ محليّاً: لا دوكر في البيئةِ.**
CI         ⚪ NOT MEASURED   — يُملأُ من «WASLA CI» بعدَ الدفعِ · أوّلُ تشغيلٍ لوظيفةِ `image-supply-chain` في تاريخِ المستودعِ
PRODUCTION ⚪ NOT VERIFIED   — لا نشرَ ولا سجلَّ صورٍ ولا إقلاعَ خدمةٍ في حاويةٍ (نطاقُ `M2-02`)
```

**والطبقتانِ الثانيةُ والثالثةُ لا تُقرَآنِ خُضرةً ولا يُدَّعى ذلك.**

---

## 2. بنودُ البوّابةِ

| # | البندُ | الحالةُ | الدليلُ المقيسُ |
|---|---|---|---|
| 1 | ملفُّ بناءٍ واحدٌ لكلِّ الحِزَمِ القابلةِ للتشغيلِ | ✅ | [`Dockerfile`](../../Dockerfile) بثلاثِ طبقاتٍ · الحزمةُ تُختارُ زمنَ التشغيلِ بوسيطٍ أو `WASLA_SERVICE` |
| 2 | الأساسُ مُثبَّتٌ بالبصمةِ ونسختُهُ = نسخةُ CI | ✅ | `node:20.20.1-alpine@sha256:b88333c4…bc0afa` = `NODE_VERSION` في `ci.yml` · البابانِ 1 و2 من الفحصِ 19 |
| 3 | مديرُ الحِزَمِ من مصدرٍ واحدٍ | ✅ | `corepack` يقرأُ `packageManager` (pnpm 9.15.9) · لا رقمَ في Dockerfile · البابُ 4 |
| 4 | التشغيلُ غيرُ جِذرٍ ومُدخَلٌ مُعلَنٌ | ✅ | `USER node` + `ENTRYPOINT` · البابُ 3 · ويُقاسُ **داخلَ الصورةِ** بـ`id -u` في `verify-image-contract.sh` |
| 5 | عقدُ التشغيلِ يُطابقُ الشجرةَ في الاتّجاهَينِ | ✅ | 16 حزمةً (13 خدمةً + 3 روبوتاتٍ) · `RUNNABLE_PACKAGE_COUNT: 16` مقيسٌ لا مكتوبٌ · البابُ 8 |
| 6 | قائمةُ موادٍ CycloneDX بلا بديلٍ صامتٍ | ✅ | `generate-sbom.sh` بحدٍّ أدنى `SBOM_MIN_COMPONENTS=300` · لا `\|\| true` (بابُ 9 يُسقِطُهُ) |
| 7 | بناءانِ ومقارنةُ مجموعةِ purl | ✅ | `build-image.sh` ثمَّ `--no-cache` ثمَّ `compare-sbom.sh` — فرقٌ واحدٌ يُسقِطُ الوظيفةَ |
| 8 | بوّابةُ ثغراتٍ بحدودٍ مكتوبةٍ بلا ملفِّ استثناءاتٍ | ✅ | `scan-image.sh`: تقريرٌ كاملٌ يُنشَرُ + بوّابةُ `HIGH,CRITICAL --ignore-unfixed` |
| 9 | أدواتٌ مُثبَّتةٌ بنسخةٍ وبصمةٍ من موضعٍ واحدٍ | ✅ | `tool-pins.env`: syft 1.51.1 · trivy 0.74.0 ببصمتَي sha256 · بلا إجراءِ طرفٍ ثالثٍ · البابُ 9 |
| 10 | حارسٌ في المدخلِ الموحَّدِ وعضّتُهُ مُبرهَنةٌ بطفرةٍ | ✅ | الفحصُ **19** في `verify-governance.sh` · **18 حالةً** في [`gov-cases-container-image.sh`](../../scripts/checks/lib/gov-cases-container-image.sh): خطُّ أساسٍ + 16 طفرةً تعضُّ + إثباتُ استعادةٍ · وكلُّ طفرةٍ تُثبِتُ تغييرَها بـ`cmp` |
| 11 | سياقٌ حاجزٌ حيٌّ لا لقطةٌ محرَّرةٌ بيدٍ | ✅ | `image-supply-chain` سياقٌ 32 في الحمايةِ الحيّةِ · أُعيدَ **قياسُ** اللقطةِ من الواجهةِ ([الدليلُ الخامُّ](ci-evidence/2026-09-16T193237Z-m2-01-image-supply-chain-context/api-responses/01-protection-after.raw.json)) |
| 12 | الأساسُ الآليُّ مُحدَّثٌ بالقياسِ | ✅ | `BASELINE.json`: `governance_checks` 18→19 · `risks_not_closed` 33→35 · `governance_suite_cases` 403→421 · مقيسٌ من سجلِّ تشغيلٍ أخضرَ واحدٍ (`tests_passed` 4737 · `verify_overall: passed`) |
| 13 | حكمُ CI أخضرُ على الدفعةِ | ⚪ | **لم يُقَسْ بعدُ** — يُملأُ برقمِ تشغيلٍ وعددِ وظائفَ ناجحةٍ بعدَ الدفعِ. **ولا يُقرأُ الأخضرُ المحليُّ بديلاً عنهُ.** |

---

## 3. عطبانِ مقيسانِ لم يكونا في الخارطةِ — ودليلُهما لا يُمحى

**الأوّلُ (`RISK-0048`، عُولِجَ وحُرِسَ):** `services/delivery` و`services/search`
لهما `src/http/server.ts` إنتاجيٌّ وأربعُ سيقانٍ حاجزةٍ في CI **وليسَ لهما أمرُ
`start`**. لا `tsc` يراهُ ولا اختبارٌ ولا حارسٌ — كانَ سيظهرُ أوّلَ مرّةٍ عندَ
أوّلِ نشرٍ. أُضيفَ الأمرانِ بنمطِ الخدماتِ الأخرى، **وأُنشِئَ البابُ 8** ليمنعَ
عودتَهُ في الاتّجاهَينِ.

**الثاني (`RISK-0047`، دَينٌ مفتوحٌ لا مطويٌّ):** صادراتُ حِزَمِ العملِ تُشيرُ
إلى `src/*.ts` وأمرُ التشغيلِ `node --import tsx`، فالصورةُ **تحملُ تبعيّاتِ
التطويرِ بالضرورةِ**. تقليمُها يقتضي مسارَ ترجمةٍ حقيقيّاً ونطاقاً أوسعَ من هذا
الحجزِ. **فلا يُقالُ إنَّ سطحَ الصورةِ مُقلَّمٌ.**

**ولا يُمحى دليلُ ما أُلغيَ:** PR #205 يُغلَقُ بسببٍ مكتوبٍ (Dockerfile ينسخُ
`services/*/dist` والخدماتُ لا تعملُ من `dist` · بديلٌ صامتٌ في توليدِ قائمةِ
الموادِ · لا وظيفةَ CI تبنيها · حجزٌ مملوكٌ لعنصرٍ آخرَ · ADR محجوزٌ لغيرِهِ)
ويُسجَّلُ في [`TASK_LOG.md`](../16-progress/TASK_LOG.md) — **يُصحَّحُ بالإضافةِ لا
بالطمسِ.**

---

## 4. حدودُ الحارسِ (مُعلَنةٌ لا مطويّةٌ)

- **الفحصُ 19 يقرأُ نصّاً وإعداداً لا يبني صورةً**: أخضرُهُ لا يُثبِتُ أنَّ
  `docker build` ينجحُ. وهذا بالضبطِ ما فاتَ المحاولةَ الأولى، فالجوابُ وظيفةُ
  CI لا وسمٌ في وثيقةٍ.
- **التشغيلُ التجريبيُّ ليسَ إقلاعاً**: `WASLA_ENTRYPOINT_DRYRUN=1` يُحمِّلُ مدخلَ
  الحزمةِ داخلَ الصورةِ بلا ربطِ منفذٍ ولا قاعدةِ بياناتٍ. «الخدمةُ تُقلِعُ
  وتُجيبُ» حكمُ `M2-02`.
- **لا سجلَّ صورٍ ولا نشرَ ولا `docker-compose`** في هذهِ الدورةِ.
- **قابليّةُ الإعادةِ مقيسةٌ على مجموعةِ purl لا على البايتاتِ**: بناءانِ
  متطابقانِ في المكوِّناتِ قد يختلفانِ في البصمةِ النهائيّةِ (طوابعُ زمنٍ)، وهذا
  حدٌّ مُعلَنٌ لا نجاحٌ مُدَّعىً.
- **بوّابةُ الثغراتِ لا تحجزُ غيرَ القابلِ للإصلاحِ** — يُقاسُ ويُنشَرُ في
  الأرتفاكتِ ولا يُخفى؛ وحجزٌ بلا علاجٍ يُعطَّلُ بعدَ أسبوعٍ، وتعطيلُ بوّابةٍ
  أسوأُ من حدٍّ مكتوبٍ.
- **بصمةُ الأساسِ تُثبَّتُ ولا تُجدَّدُ آليّاً**: تجديدُها عملٌ مقصودٌ يُقاسُ فيهِ
  الفارقُ، لا تحديثٌ صامتٌ.
