# M3-06 — بوّابةُ الخروجِ (جودةُ تجربةِ الواجهات: i18n/accessibility/error/offline)

> **Work Item:** `M3-06` — i18n/accessibility/error/offline UX · **كُتب بواسطة:** `CLM-0323` · **التاريخ:** 2026-09-23
>
> **معيارُ الإغلاقِ في اللوحة:** «UAT result · acceptance matrix». **المرجعُ الحاكم:** [`FRONTEND_UX_QUALITY_SPEC.md`](../01-product/FRONTEND_UX_QUALITY_SPEC.md) §2 — تسعةُ معاييرِ قبولٍ مقيسةٍ آليًّا لكلِّ تطبيق.
>
> **حدودُ الصدقِ (ما لا يُدَّعى):** لا يُدَّعى تغطيةُ قارئِ الشاشةِ اليدويِّ الكاملُ ولا الأوفلاين المُصرَّف (queued writes) — كلاهما مسجَّلٌ تأجيلًا في المواصفةِ §4.

---

## 1. الطبقاتُ الثلاث

```
LOCAL      ✅ VERIFIED  — وحدة: 7/7 لكلِّ تطبيق (locale override · offline banner · network/timeout mapping)
            E2E: 35 customer · 34 driver · 30 admin — كلُّها خضراء (بما فيها 3×3 مواصفاتُ M3-06 الجديدة)
            typecheck نظيفٌ للتطبيقاتِ الثلاثة
CI         ✅ VERIFIED  — run 35869845415 (PR #423، كلُّ الوظائف) · main 35871170596 (أخضرُ السياقِ المتطلبِّ بعدَ إصلاحِ بياتِ الحجوزات)
PRODUCTION ✅ VERIFIED  — 2026-09-23T19:05Z على النشرِ ac336a5 (dep-daq25rm7bikc73babqig · dep-daq25rp7lnhs739in4gg ·
            dep-daq25s60tbcc73dadtig) — متصفّحٌ حقيقيٌّ على العناوينِ العامةِ الثلاثة:
```

## 2. القياسُ المحلي (2026-09-23)

| المعيار (المواصفة §2) | customer | driver | admin |
|---|---|---|---|
| 1. تكافؤُ مفاتيحِ ar/en/ur | 110/110/110 ✅ | 121/121/121 ✅ | 139/139/139 ✅ |
| 2. `?locale=` يبدّلُ اللغةَ وlang/dir | E2E ✅ | E2E ✅ | E2E ✅ |
| 3. RTL افتراضيًّا | ✅ | ✅ | ✅ |
| 4. axe WCAG A/AA (0 مخالفات) | ✅ (موجودةٌ سابقًا) | ✅ | ✅ |
| 5. تمييزُ network_error/timeout مترجمًا | وحدة ✅ | وحدة ✅ | وحدة ✅ |
| 6. حالاتُ التحميل | ✅ (قائمة) | ✅ | ✅ |
| 7. لافتُ الأوفلاين (setOffline) | E2E ✅ | E2E ✅ | E2E ✅ |
| 8. صفرُ فائضٍ أفقيٍّ 375px | E2E ✅ | E2E ✅ | E2E ✅ |
| 9. E2E كاملةٌ على حزمةِ الإنتاج | 35 ✅ | 34 ✅ | 30 ✅ |

## 3. ما تغيّر (الفجواتُ المقيسةُ المُغلَقة)

1. **تمييزُ أخطاءِ الشبكة:** عملاءُ النداءِ الثلاثةُ كانوا يتركونَ رفضَ fetch خامًا يصلُ المخازنَ بلا تمييزٍ ولا ترجمة — صار `network_error` (status 0) و`timeout` (status 408) رمزينِ مستقرَّينِ بمفاتيحِ ترجمةٍ في اللغاتِ الثلاث.
2. **الأوفلاين:** لم يكنْ وجودٌ لأيِّ معالجةٍ (صفرُ `navigator.onLine`) — صار لافتُ «أنت غير متصل» (`role="status"`، تباينٌ AA) يظهرُ ويختفي بالأحداث، في حالةِ ما قبلَ الجلسةِ أيضًا (شاشةُ «جارٍ التحميل»).
3. **تشغيلُ اللغات:** كانت اللغاتُ الثلاثُ موجودةً بلا مسارٍ لتشغيلِها — صار `?locale=` يبدّلُ اللغةَ و`lang/dir` معًا.
4. **التجاوب:** كان صفرُ `@media` في التطبيقاتِ الثلاثة — صارت قواعدُ تجاوبٍ مقيسةً (صفرُ فائضٍ أفقيٍّ عندَ 375px؛ لوحةُ الإدارةِ تلتفُّ ≤768px).

## 4. الإنفاذُ الآلي

- كلُّ معيارٍ من التسعةِ مقيسٌ باختباراتٍ لا تُخفَّف: أيُّ انحدارٍ (مفتاحُ ترجمةٍ ناقص، لافتٌ لا يظهر، فائضٌ أفقي) يُسقِطُ وظيفةَ `test` أو `*-e2e` في CI — والبواباتُ الفرعيةُ مطلوبةٌ للدمج.
- أرقامُ القياسِ (المفاتیح، عددُ E2E) منشورةٌ في المواصفةِ §3 وتُقاسُ من الشجرة.

## 5. القياسُ الإنتاجي (PRODUCTION — التطبيقاتُ المنشورة)

قياسٌ في متصفّحٍ حقيقيٍّ على العناوينِ العامةِ بعدَ نشرِ ac336a5 (نُطلِقَ يدويًّا بتفويضِ المالك؛ autoDeploy لم يُطلِقْهُ):

| المعيار | customer | driver | admin |
|---|---|---|---|
| الافتراضيُّ `lang="ar" dir="rtl"` | ✅ | ✅ | ✅ |
| `?locale=en` → `lang="en" dir="ltr"` + نصٌّ إنجليزيٌّ مرئيٌّ ("Loading...") | ✅ | ✅ | ✅ |
| لافتُ الأوفلاين (متصفّحٌ بلا شبكةٍ): ظهورٌ بنصٍّ عربيٍّ كاملٍ و`role="status"` | ✅ | ✅ | ✅ |
| اختفاءُ اللافتِ عندَ عودةِ الشبكة | ✅ | ✅ | ✅ |
| فائضٌ أفقيٌّ عندَ 375px | **0** | **0** | **0** |

**حدودُ الصدقِ الإنتاجيِّ المُعلَنة:**
1. رحلةُ initData داخلَ Telegram (المصادقةُ الحقيقيةُ) **لم تُقَس هنا** — نفسُ حدِّ M3-09.
2. الأوفلاين الإنتاجيُّ قِيسَ بمحاكاةِ سقوطِ الشبكةِ في المتصفّحِ (emulateNetworkConditions) — وهي أداةُ قياسِ المتصفّحِ القياسيةُ للأوفلاين، لا انقطاعُ خادمٍ حقيقي.
3. نشرُ المواقعِ الثابتةِ نُطلِقَ يدويًّا عبرَ Render API بتفويضِ المالك (2026-09-23)؛ autoDeploy عبرَ push لم يُطلِقْ نشرًا (النشراتُ السابقةُ كانت من terraform عندَ الإنشاء) — **مسجَّلٌ كملاحظةِ تشغيلٍ** لا كخللٍ في البندِ (النشرُ يدويٌّ مقبولٌ في هذه المرحلة).

## 6. الحكم

**مكتمل** — LOCAL ✅ · CI ✅ · PRODUCTION ✅ (بحدودِ الصدقِ المُعلَنة). المصفوفةُ الكاملة: [`FRONTEND_UX_QUALITY_SPEC.md`](../01-product/FRONTEND_UX_QUALITY_SPEC.md).
