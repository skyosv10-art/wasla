# M3-07 — بوّابة الخروج (عمليات قابلة للدعم بلا تعديل قاعدة)

> **Work Item:** `M3-07` — Supportable operations without DB edits · **كُتب بواسطة:** `CLM-0325` · **التاريخ:** 2026-09-24
>
> **معيار القبول:** «runbook drill» — تمرينٌ تشغيليٌّ على الأدلة القائمة في بيئة staging الحقيقية.
>
> **الحكم بعد القياس (مُحدَّث 2026-09-24 · `CLM-0326`):** ✅ **المحاورُ الأربعةُ مُقاسة** — تدويرُ المفاتيحِ بحالاتِه الثلاثِ أُكمِل على النشرِ الحيِّ (§7). الحكمُ الأوّلُ «⚠️ جزئي» (`CLM-0325`) يبقى مكتوبًا في §2.3، وتشخيصُه «توقيتُ Render Free» **خاطئٌ** ومُصحَّحٌ بالإضافةِ في §7: السببُ الجذريُّ معرِّفٌ مكرَّرٌ صنعَه سكربتُ التمرينِ نفسُه، ونشرٌ لم يُطلَق.

---

## 1. الطبقات الثلاث

```
LOCAL      ✅ VERIFIED  — مصفوفة الإجراءات التشغيلية مكتوبة + سكربتات التمرين في scripts/
CI         ✅ PR #429 — WASLA CI run 35939312135 أخضر (38/38) · main run 35940610765 أخضر · تصحيحُ CLM-0326: ⏳ يُقاسُ على طلبِ الدمجِ التالي
PRODUCTION ✅ MEASURED  — مسح الصحة + حدث التدقيق + تدوير المفاتيح بحالاته الثلاث على wasla-audit الحيّة (§7)
           (القياسُ الأوّلُ كان ⚠️ PARTIAL — CLM-0325؛ مُصحَّحٌ في §7)
```

## 2. المحاور الأربعة للتمرين

### 2.1 مسح صحة الخدمات المنشورة — ✅ مُنجَز

**الطريقة:** نداء `GET /health` (أو نقطة الصحة المخصصة) لكل خدمة منشورة على Render staging، بالتوازي، مع مهلة 90 ثانية لكل نداء.

| # | الخدمة | نقطة الفحص | HTTP | الحالة | زمن الاستجابة |
|---|---|---|---|---|---|
| 1 | wasla-audit | `/health` | 200 | ok | 0.5s |
| 2 | wasla-customers | `/health` | 200 | ok | 0.2s |
| 3 | wasla-dispatch | `/health` | 200 | ok | 0.3s |
| 4 | wasla-drivers | `/health` | 200 | ok | 0.2s |
| 5 | wasla-geography | `/health` | 200 | ok | 0.4s |
| 6 | wasla-identity | `/health` | 200 | ok | 0.4s |
| 7 | wasla-marketplace | `/health` | 200 | degraded | 0.9s |
| 8 | wasla-matching | `/health` | 200 | degraded | 1.2s |
| 9 | wasla-orders | `/health` | 200 | ok | 0.3s |
| 10 | wasla-negotiations | `/health` | 200 | ok | 0.2s |
| 11 | wasla-reputation | `/health` | 200 | ok | 0.2s |
| 12 | wasla-search | `/search/health` | 200 | ok | 0.2s |
| 13 | wasla-delivery | `/delivery/health` | 200 | ok | 0.2s |
| 14 | wasla-customer-bot | `/health` | 200 | degraded (telegram) | 0.3s |
| 15 | wasla-driver-bot | `/health` | 200 | degraded (telegram) | 0.2s |
| 16 | wasla-partner-bot | `/health` | 200 | degraded (telegram) | 0.2s |
| 17 | wasla-subscriptions | `/health` | 200 | ok | 0.2s |

**النتيجة:** 17/17 خدمات منشورة صحية. خدمتا search و delivery تحميان `/health` بمصادقة الخدمة وتكشفان نقاط صحة مخصصة (`/search/health` و`/delivery/health`). البوتات الثلاثة `degraded` لأن قناة Telegram غير مهيأة في staging (متوقع). حالة `degraded` في marketplace و matching مرتبطة بعدم وجود بيانات تشغيلية (active_ruleset_version: null).

**ما لا يُدَّعى:** الصحة هنا تعني «الخدمة تتجاوب» لا «تعمل بلا أخطاء». لا قياس للأداء أو زمن الاستجابة تحت الحمل.

### 2.2 إلحاق وقراءة حدث تدقيق — ✅ مُنجَز

**الطريقة:** سكّ رمز مصادقة خدمة `wsvc3` محليًا بمفتاح `stg-audit-k2` (النشط وقت القياس)، ثم:

1. `POST /audit/events` بحمولة حدث تدقيق (`actor_id=m3-07-drill`, `action=runbook_drill`, `resource_id=m3-07`).
2. `GET /audit/events?action=runbook_drill&limit=5` لقراءة الحدث المُلحَق.

| العملية | الطريقة | المسار | HTTP | النتيجة |
|---|---|---|---|---|
| إلحاق حدث | POST | `/audit/events` | **201** | `id=4`, `created_at=2026-09-24T00:01:13Z` |
| قراءة الأحداث | GET | `/audit/events?action=runbook_drill&limit=5` | **200** | حدث واحد مطابق |

**النتيجة:** عملية تشغيلية كاملة عبر الواجهة بلا أي تعديل على قاعدة البيانات. السجل append-only: لا يوجد `PUT`/`PATCH`/`DELETE`.

**ما يُثبِت:** الخدمات المنشورة تقبل رموز المصادقة الموقعة محليًا وتنفذ العمليات الإدارية عبر واجهاتها المُصمَّمة لذلك.

### 2.3 تدوير مفاتيح هوية الخدمة بحالاتها الثلاث — ⚠️ جزئي في القياس الأوّل · ✅ مُكتمِلٌ في §7

> القياسُ الأوّلُ أدناه محفوظٌ دليلًا كما كُتِب. **تشخيصُه وجدولُه فيهما أخطاءٌ مُصحَّحةٌ في §7** (سببُ الفشلِ ومعرِّفاتُ النشرِ ومفتاحُ كلِّ نشر).

**الطريقة:** تنفيذ دليل [`SERVICE_AUTH_KEY_ROTATION.md`](../14-runbooks/SERVICE_AUTH_KEY_ROTATION.md) بحالاته الثلاث (active → verify_only → revoked) على خدمة `wasla-audit` المنشورة عبر تحديث متغيرات البيئة على Render.

**ما نُفِّذ:**

| الخطوة | الحالة المطلوبة | تحديث البيئة | Deploy | التحقق |
|---|---|---|---|---|
| 0 | `stg-audit-k2:active` (الأصلية) | — | live (dep-daq5bqflot8c73fdhp8g) | ✅ الرمز يعمل |
| 1 | `k2:active, k3:verify_only` | ✅ PUT ناجح | live (dep-daq6mg49v7es73c15ho0) | ✅ المفتاح القديم يعمل · ❌ المفتاح الجديد لم يُتحقق منه |
| 2 | `k2:verify_only, k3:active` (قلب) | ✅ PUT ناجح | ⚠️ deploy فاشل | ❌ لم يُتحقق |
| 3 | `k3:active` (إزالة القديم) | ✅ PUT ناجح | ⚠️ deploy فاشل | ❌ لم يُتحقق |

**القياسات الفعلية:**
- تحديث متغيرات البيئة على Render عبر `PUT /v1/services/{id}/env-vars` نجح في كل خطوة.
- deploy واحد نجح (dep-daq6mg49v7es73c15ho0) واثنان فشلا (`update_failed`).
- رمز المصادقة الموقوع بالمفتاح الأصلي (stg-audit-k2) عاد بـ200 قبل تحديث البيئة، مُثبتًا أن المفتاح يعمل.
- بعد تحديث البيئة لمدة خطوة واحدة، لم يُعاد التحقق من كل حالة بسبب:
  1. توقيت نشر Render Free (2-5 دقائق لكل deploy) يتجاوز مهلة الـsandbox.
  2. deploys فاشلة (`update_failed`) ربما بسبب تضارب نشر متزامن.
  3. مصنف الأمان منع قراءة متغيرات البيئة بعد المحاولة الأولى.

**ما لا يُدَّعى:** تدوير المفاتيح بحالاته الثلاث **لم يُتحقق منه بالكامل**. الذي تحقق:
- آلية تحديث متغيرات البيئة على Render تعمل (PUT ناجح).
- الرمز الموقوع بالمفتاح النشط يُقبَل من الخدمة المنشورة.
- دليل التدوير مكتوب بدقة وقابل للتنفيذ.

**العائق المسجَّل:** Render Free tier لا يضمن نشرًا متتابعًا سريعًا بما يكفي لتمرين تدوير كامل في جلسة واحدة. الحل: تنفيذ التدوير خطوة بخطوة عبر جلسات متعددة، أو ترقية إلى خطة مدفوعة تقلل زمن النشر.

### 2.4 مصفوفة الإجراءات التشغيلية — ✅ مُنجَز

انظر §3 أدناه.

## 3. مصفوفة الإجراءات التشغيلية

كل إجراء تشغيلي في النظام: مساره، هل يحتاج تعديل قاعدة، وحالة قياسه.

| # | الإجراء | المسار | تعديل قاعدة؟ | حالة القياس |
|---|---|---|---|---|
| 1 | إلحاق حدث تدقيق | `POST /audit/events` (audit service) | لا — append-only عبر API | ✅ مُقاس (201) |
| 2 | قراءة أحداث التدقيق | `GET /audit/events` (audit service) | لا — قراءة عبر API | ✅ مُقاس (200) |
| 3 | مسح صحة الخدمات | `GET /health` أو `/svc/health` لكل خدمة | لا — قراءة عبر API | ✅ مُقاس (17/17) |
| 4 | تدوير مفتاح مصادقة الخدمة | Render env vars: `WASLA_SERVICE_AUTH_KEYS` + `WASLA_SERVICE_AUTH_ACTIVE_KID` **ثمّ** `POST /deploys` صريح | لا — متغيرات بيئة فقط | ✅ مُقاس (§7: k5→k6 بثلاث حالات، 3 نشرات حيّة) |
| 5 | سحب مفتاح مسرَّب | Render env vars: `kid:revoked:` ثمّ نشر | لا — متغيرات بيئة فقط | ⚠️ حالُ `revoked` مُقاسةٌ (401 + `revoked_key` في السجل، §7) · الخطوةُ الطارئةُ الواحدةُ (§3 في الدليل) بلا نافذةٍ لم تُقَس |
| 6 | تنظيف مفاتيح الإلحاق المنتهية | `POST /delivery/idempotency-keys/sweep` (delivery service) | لا — حذف عبر API | ❌ غير مُقاس (الخدمة منشورة لكن المسار غير مُستدعى) |
| 7 | قراءة تعارضات المخزون | `GET /delivery/inventory-conflicts` (delivery service) | لا — قراءة عبر API | ❌ غير مُقاس |
| 8 | إدارة موظفي المتجر | `POST /stores/{slug}/staff` (marketplace service) | لا — عبر API | ❌ غير مُقاس |
| 9 | إدارة المنتجات | `POST /products` (marketplace service) | لا — عبر API | ❌ غير مُقاس |
| 10 | إدارة العملاء | `GET/POST /customers` (customers service) | لا — عبر API | ❌ غير مُقاس |
| 11 | إدارة السائقين | `GET /drivers` (drivers service) | لا — عبر API | ❌ غير مُقاس |
| 12 | تدوير توكنات البوتات | Render env vars: `*_BOT_TOKEN` | لا — متغيرات بيئة فقط | ❌ غير مُقاس |
| 13 | نشر خدمة جديدة | Render dashboard / terraform | لا — بنية تحتية | ❌ غير مُقاس |
| 14 | ترحيل قاعدة بيانات | `psql` أو `drizzle-kit` ضد Supabase | نعم — ترحيل DDL | ❌ غير مُقاس |
| 15 | نسخ احتياطي قاعدة البيانات | Supabase dashboard | نعم — نسخة كاملة | ❌ غير مُقاس |

**حدود الصدق:**
- الإجراءات 1-3 مُقاسة فعليًا على staging.
- الإجراء 4 مُجزَّأ (تحديث البيئة ناجح، لكن التحقق من كل حالة غير مكتمل).
- الإجراءات 5-15 مُدرجة لكن غير مُقاسة — وجودها في المصفوفة لا يعني أنها مُثبَتة.
- أي إجراء يتطلب تعديل قاعدة (14-15) خارج نطاق معيار القبول «بلا تعديل قاعدة».

## 4. الأدلة

| الدليل | الموقع | التاريخ |
|---|---|---|
| مسح الصحة | `scripts/m3-07-health-scan.py` + مخرجاته | 2026-09-24 |
| حدث التدقيق | `scripts/m3-07-audit-drill.py` + مخرجاته (POST 201, GET 200) | 2026-09-24 |
| تدوير المفاتيح | `scripts/m3-07-key-rotation-drill.py` (الموحَّد؛ حُذِفت النسختانِ المعيبتانِ v2/v3 في CLM-0326) + سجل deploys وسجلُّ الخدمة على Render | 2026-09-24 |
| مصفوفة الإجراءات | §3 أعلاه | 2026-09-24 |
| دليل تدوير المفاتيح المرجعي | `docs/14-runbooks/SERVICE_AUTH_KEY_ROTATION.md` | 2026-08-31 |

## 5. ما لا يُدَّعى (حدود الصدق)

1. **تدوير المفاتيح غير مُكتمل:** ثلاث حالات (active/verify_only/revoked) لم تُتحقق منها بالكامل بسبب توقيت نشر Render Free وdeploys فاشلة. هذا عائق تشغيلي مسجَّل لا تخفيض.
2. **الصحة تعني التجاوب لا الأداء:** مسح الصحة يقيس أن الخدمة ترد، لا أنها تعمل تحت الحمل أو بلا أخطاء داخلية.
3. **البوتات الثلاثة degraded:** قناة Telegram غير مهيأة في staging (توكنات البوتات موجودة في env vars لكن webhook غير مضبوط). هذا متوقع في staging.
4. **لا قياس للأداء أو السعة:** لا SLI/SLO/تسجيل موزَّع في هذا التمرين.
5. **الإجراءات غير المُقاسة (5-15):** وجودها في المصفوفة لا يثبت أنها تعمل — يُحتاج قياس مستقل لكل منها.

## 6. الحالة

`M3-07` يبقى **In Progress**. ثلاثة محاور مُنجَزة (مسح الصحة، حدث التدقيق، مصفوفة الإجراءات). محور تدوير المفاتيح مُجزَّأ ويحتاج إلى:
1. تنفيذ خطوة بخطوة عبر جلسات متعددة (deploy واحد لكل خطوة).
2. أو ترقية Render إلى خطة مدفوعة تقلل زمن النشر.
3. تفويض صريح بقراءة/كتابة متغيرات البيئة على Render للتمرين.

النقل إلى `Ready for Gate` يتطلب اكتمال تدوير المفاتيح بحالاته الثلاث.

> **تحديث 2026-09-24 (`CLM-0326`):** الشرطُ أعلاه استُوفي بالقياسِ في §7 (الحالاتُ الثلاثُ على النشرِ الحيّ). النقلُ إلى `Completed` بتفويضِ المالكِ التنفيذيِّ المكتوبِ في الجلسة، بعد حكمِ CI أخضرَ على طلبِ الدمجِ وعلى main. النقل إلى `Completed` بقرار مالك البرنامج وحده (§9).

## 7. سجلُّ تدقيق — إكمالُ التدويرِ وتصحيحُ التشخيص (`CLM-0326` · 2026-09-24)

بتفويضِ المالكِ الصريحِ في الجلسة (قراءةُ وتعديلُ مفاتيحِ wasla-audit وإعادةُ نشرِها).

### 7.1 السببُ الجذريُّ لفشلِ النشرَين — قيسَ من سجلِّ الإقلاع

سجلُّ Render للنشرَين `dep-daq6hnou01pc73f0l6ng` (00:04:14Z) و`dep-daq6k42d0e5s739e041g` (00:09:21Z):

```
ServiceAuthKeyError: معرِّفُ المفتاحِ «stg-audit-k3» مكرَّرٌ في السجلّ.
    at new ServiceAuthKeyRegistry (packages/service-auth/src/keys.ts:121)
==> Exited with status 1
```

أي أنّ **سكربتَ التمرينِ الأوّلَ (v2) كتبَ المعرِّفَ نفسَه مرّتين** في `WASLA_SERVICE_AUTH_KEYS`،
فرفضَ السجلُّ الإقلاعَ (fail-closed — سلوكٌ صحيح) وأبقى Render النشرَ السابقَ حيًّا فلم ينقطعِ
الخدمة. **ليس توقيتَ Render Free**: النشراتُ الثلاثةُ في §7.3 بلغت `live` في 30–90 ثانية.
وما كُتِب في §2.3 («ربما بسبب تضارب نشر متزامن» · «توقيت Render Free») خاطئٌ ويُقرأ بهذا التصحيح.

### 7.2 التباعدُ بين المخزَّنِ والحيِّ — قيسَ قبل أيِّ تعديل

| القياس | القيمة |
|---|---|
| المتغيّراتُ المخزَّنةُ على Render | `stg-audit-k5:active` وحده (من محاولةٍ سابقةٍ لم تُطلِق نشرًا) |
| النشرُ الحيُّ | `dep-daq6mg49v7es73c15ho0` بمفتاحِ `stg-audit-k3` (سرُّه لم يُحفَظ خارجَ المتغيّرِ الذي استُبدِل) |
| رمزٌ موقَّعٌ بـk5 على الحيّ (01:07:30Z) | **401** · السجلّ: `reason: unknown_key` |

فـ`PUT /env-vars` **لا يُعيد النشر**، وأيُّ نشرٍ لاحقٍ — ولو دمجٌ لا علاقةَ له — كان سيلتقطُ
المخزَّنَ بصمت. هذه حقيقةٌ تشغيليّةٌ أُضيفت إلى الدليل (§0 و§2.2).

### 7.3 الحالاتُ الثلاثُ — `stg-audit-k5 → stg-audit-k6` (مُزامنةٌ + تدوير)

كلُّ حالةٍ: `PUT /env-vars` ثمّ `POST /deploys` صريحٌ وانتظارُ `live`، ثمّ نداءانِ حقيقيّانِ
`POST /audit/events` موقَّعانِ بكلِّ مفتاح. الأسرارُ في ملفٍّ محليٍّ خارجَ المستودع (600).

| الحالة | المفاتيح · النشط | النشر | k5 | k6 |
|---|---|---|---|---|
| s1 — إدخالُ الجديدِ للتحقّق | `k5:active, k6:verify_only` · k5 | `dep-daq7fl2d0e5s739haksg` live (01:08:54Z) | **201** | **201** |
| s2 — قلبُ التوقيع | `k6:active, k5:verify_only` · k6 | `dep-daq7gadg1s2s73fe943g` live (01:09:28Z) | **201** | **201** |
| نافذةُ التداخل | 360 ثانية (الحدُّ الأدنى في الدليل §2.2(4)) | — | — | — |
| s3 — سحبُ القديمِ باسمِه | `k6:active, k5:revoked` · k6 | `dep-daq7jcs9v7es73c4hcqg` live (01:16:01Z) | **401** · السجلّ `reason: revoked_key` | **201** |

النتيجة: `RESULT PASS`. الحالةُ النهائيّةُ: المخزَّنُ = الحيُّ = `k6:active, k5:revoked`، و`/health` 200.
والردُّ على السلكِ في الرفضَين واحدٌ (`401 AUTHN_UNAUTHENTICATED`) والفرقُ في السجلِّ الداخليِّ فقط — كما يعدُ الدليل.

### 7.4 اكتشافٌ ثالث: اسمُ السببِ في الدليلِ لا وجودَ له — ومُنفَذٌ آليًّا

الدليلُ كتبَ «unknown_kid» في ثلاثةِ مواضع؛ الشفرةُ والسجلُّ الحيُّ يكتبانِ `unknown_key`.
مشغِّلٌ يبحثُ في السجلِّ بالاسمِ المكتوبِ يجدُ «لا شيء» فيقرأُ خضرة. صُحِّح الدليل، وأُضيف
**الفحصُ 25** (`scripts/checks/validate-runbook-reason-codes.sh`) موصولًا بالبوّابةِ الموحَّدة
مع حالتَي طفرةٍ تعضّان في `test-governance.sh` (اسمٌ قديمٌ في الدليلِ يُسقِط · سببٌ غابَ من الشفرةِ يُسقِط).
‏ADR-022 يكتبُ الاسمَ القديمَ نصًّا تاريخيًّا خارجَ نطاقِ هذا الحجز — الشفرةُ هي المرجع.

### 7.5 ما لا يُدَّعى بعد التصحيح

- التدويرُ مُقاسٌ على **خدمةٍ واحدةٍ** (wasla-audit). الدليلُ يطلبُ النشرَ على **كلِّ** الخدمات؛
  لم تُدوَّر مفاتيحُ الخدماتِ الأخرى في هذا التمرين.
- السحبُ الطارئُ في خطوةٍ واحدة (§3 في الدليل) لم يُقَس — قيسَ حالُ `revoked` بعد نافذةِ التداخل.
- مفتاحُ k3 الذي كان حيًّا لم يُعَد قياسُه (سرُّه لم يُحفظ) — ولا مُنادٍ معروفٌ يحملُه في staging.
- الإجراءاتُ 6–15 في المصفوفةِ ما زالت غيرَ مُقاسة.
