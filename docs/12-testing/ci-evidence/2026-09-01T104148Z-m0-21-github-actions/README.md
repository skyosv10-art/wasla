# دليلُ CI — M0-21 · أوّلُ خطٍّ حقيقيٍّ على `main`

> **قاعدةُ هذا المجلَّد:** كلُّ رقمٍ فيه **مقروءٌ من واجهةِ GitHub أو من سجلِّ التشغيلِ نفسِه**، لا من ذاكرةٍ ولا من تشغيلٍ محلّيٍّ. والملفّاتُ الخامُ محفوظةٌ بجوارِ هذا الملفِّ ليُعادَ التدقيقُ بلا ثقةٍ بأحد.

## 1. هُويّةُ التشغيل

| الحقل | القيمة |
|---|---|
| التشغيل | [`33498726052`](https://github.com/skyosv10-art/wasla/actions/runs/33498726052) |
| الحدث | `push` |
| الفرع | `main` |
| الالتزام | `fce1f054083965ff533be284948e4e1ecce6ec88` |
| النتيجة | `conclusion: success` |
| طلبُ الدمج | [PR #2](https://github.com/skyosv10-art/wasla/pull/2) |
| القرار | [ADR-023](../../../15-decisions/ADR-023-ci-migration-to-github-actions.md) · الحجز `CLM-0031` |

## 2. الوظائفُ الستُّ — `started_at` مقروءٌ لا مفترَض

| الوظيفة | `started_at` | `completed_at` | `conclusion` |
|---|---|---|---|
| `repo-structure` | 2026-09-01T10:41:52Z | 2026-09-01T10:42:00Z | `success` |
| `doc-coverage` | 2026-09-01T10:41:51Z | 2026-09-01T10:41:58Z | `success` |
| `governance-guard` | 2026-09-01T10:41:50Z | 2026-09-01T10:42:58Z | `success` |
| `verify` | 2026-09-01T10:41:51Z | 2026-09-01T10:43:28Z | `success` |
| `test` | 2026-09-01T10:41:51Z | 2026-09-01T10:43:42Z | `success` |
| `typecheck` | 2026-09-01T10:41:50Z | 2026-09-01T10:44:26Z | `success` |

**ولا وظيفةَ واحدةَ بـ`started_at: null`.** وهذا هو الفرقُ الجوهريُّ عن ثمانيةِ خطوطِ GitLab التي سبقت (`#2796336446` … `#2805165622`): هناك كان `started_at: null` في كلِّ وظيفةٍ و`failure_reason: ci_quota_exceeded` — **أحمرُ بلا تنفيذٍ**. هنا أخضرُ بتنفيذ.

## 3. الأرقامُ المقيسةُ داخلَ الخطِّ ومطابقتُها للأساسِ المحلّيّ

| المقياس | مقيسٌ في CI | مقيسٌ محلّيّاً (2026-09-01) | مطابق |
|---|---|---|---|
| اختباراتٌ ناجحة | **3748** | 3748 | ✅ |
| اختباراتٌ فاشلة | **0** | 0 | ✅ |
| ملفّاتُ اختبارٍ ناجحة | **221** | 221 | ✅ |
| اختباراتٌ متخطّاة | **42** | 42 | ✅ |
| أخطاءُ الأنواع | **0** | 0 | ✅ |
| حالاتُ حزمةِ الحوكمة | **153 ناجحاً · 0 فاشلاً** | 153 · 0 | ✅ |
| `verify.sh` | `passed` — 4 نجاح · 0 إخفاق · 2 تخطٍّ مُعلَن | مطابق | ✅ |

**والمطابقةُ التامّةُ هي دليلُ التكافؤِ السلوكيِّ المطلوبُ في شروطِ `M0-21`:** الخطُّ الجديدُ يقيسُ ما كان يُقاسُ محلّيّاً، لا تفسيراً مبسَّطاً له.

## 4. البوّابةُ العشريّةُ — قياسٌ لا ادّعاء

| # | البند | الحالة | الدليل |
|---|---|---|---|
| 1 | Workflow Started | ✅ | `run 33498726052` · `status: completed` |
| 2 | All 6 Jobs Started | ✅ | ستُّ قيمِ `started_at ≠ null` في §2 · `jobs.json` |
| 3 | All Required Jobs PASS | ✅ | ستُّ `conclusion: success` · لا `allow_failure` في أيٍّ منها |
| 4 | Canonical `verify.sh` PASS | ✅ | `artifacts/verify-report/verify-report.txt` → `الخلاصة: passed` |
| 5 | `typecheck` PASS | ✅ | `logs/typecheck.log` · 0 خطأ · 40 حزمة |
| 6 | `test` PASS | ✅ | `logs/test.log` · 3748 ناجحاً · 0 فاشلاً |
| 7 | `governance` PASS | ✅ | `logs/governance-guard.log` → `النتيجة: 153 ناجح · 0 فاشل` |
| 8 | `doc-coverage` PASS | ✅ | `logs/doc-coverage.log` · `conclusion: success` |
| 9 | Artifacts موجودة | ✅ | `verify-report` (1241B) · `test-log` (9625B) · `typecheck-log` (647B) — مُنزَّلةٌ ومفكوكةٌ في `artifacts/` |
| 10 | Logs قابلة للمراجعة | ✅ | ستُّ سجلّاتٍ كاملةٍ في `logs/` — 2560 سطراً |

**الحكم: `GATE PASSED` — عشرةٌ من عشرة، كلُّها من تشغيلٍ واحدٍ على `main`.**

## 5. ما لا يُغلَقُ بهذا الدليلِ وحدَه

- **`RISK-0002`** (`only_allow_merge_if_pipeline_succeeds=false`): **باقٍ**. الخطُّ يمرُّ، لكنّ المستودعَ **لا يزالُ لا يرفضُ دمجاً عندَ إخفاقِه** — لم تُفعَّل حمايةُ فرعٍ ولا فحوصٌ مطلوبة. وPR #2 نفسُه دُمج بقرارِ مالكٍ لا بإجبارِ بوّابة.
- **`RISK-0003`** (صفرُ عناصرَ `Completed`): **باقٍ**. مرورُ بوّابةِ `M0-21` شرطٌ لنقلِ عنصرٍ واحدٍ، والنقلُ قرارُ مالكِ البرنامجِ لا قرارُ الوكيل.
- **البندُ 13 من [بوّابةِ `M1-03`](../../M1-03_GATE.md)**: **لم يُمَسّ**، و`M1-03` لم تُفتَح. أصبحَ قابلاً للقياسِ لأوّلِ مرّةٍ — والقياسُ عملٌ لاحقٌ بأمرٍ صريح.
- **`ci.yml` غيرُ محروسٍ** بأبوابِ `validate-ci-mandatory.sh` الأربعةِ، و`scripts/ci-evidence.sh` ما زال يسألُ واجهةَ GitLab. حدّانِ مُعلَنانِ في [ADR-023 §5](../../../15-decisions/ADR-023-ci-migration-to-github-actions.md) ونطاقُهما `M0-22`.
- **العشرون وظيفةً** (`*-db-integration` · `*-exit-gate-e2e`) خارجَ هذا الخطِّ، باقيةٌ في `.gitlab-ci.yml` حيثُ **لا تجري** — بقاءُ تعريفٍ ليس جرَياناً. نطاقُها `M0-22`.

## 6. ملفّاتُ هذا المجلَّد

```
run.json          استجابةُ /actions/runs/33498726052 كاملةً
jobs.json         استجابةُ /jobs — فيها started_at لكلِّ وظيفة
artifacts.json    استجابةُ /artifacts
summary.json      الخلاصةُ المستخرَجةُ آليّاً من الثلاثةِ أعلاه
logs/*.log        ستُّ سجلّاتٍ كاملةٍ منزَّلةٍ من GitHub
artifacts/        الأرتفاكتاتُ الثلاثةُ منزَّلةً ومفكوكة
```
