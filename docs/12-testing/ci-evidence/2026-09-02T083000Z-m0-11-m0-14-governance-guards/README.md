# دليلٌ مُستخرَجٌ — M0-11 · M0-12 · M0-13 · M0-14 (حرّاسُ الحوكمة)

**تاريخُ الاستخراج:** 2026-09-02 · **الفرع:** `chore/m0-gate-docs-batch4` · **الأساس:** `origin/main` عندَ `44ab8bf`

**البيئةُ المحلّيّة:** Linux 6.1.155 · `bash 5.3.9` · `nproc = 2` · Node `v20.20.1` · pnpm `9.15.9` · PostgreSQL **18.6** محلّيّاً بينما CI على **`postgres:15`** — فرقُ إصدارٍ يُعلَن في كلِّ قياسٍ يمسُّ قاعدةَ البيانات (ولا يمسُّه هذا الاستخراجُ: كلُّه صَدَفةٌ لا قاعدةُ بيانات).

---

## 1. ما في هذا المجلَّد

| الملفُّ | ما يُثبتُه |
|---|---|
| `local-measurements/gov-base.raw.txt` | الحزمةُ على شجرةٍ نظيفةٍ: **153 ناجحاً · 0 فاشلاً** — أساسُ كلِّ مقارنةِ طفرةٍ |
| `local-measurements/mut-m0-11.raw.txt` | إعادةُ مصيدةِ SIGPIPE إلى `validate-launch-board.sh:79` ⇒ **152 · 1** · الساقطةُ `[د]` لا `[د2]` |
| `local-measurements/sigpipe-300-runs.txt` | قياسُ معدَّلِ التقلّبِ مباشرةً: مطفورٌ **2 من 1500** · مُصلَحٌ **0 من 600** |
| `local-measurements/probe-m0-12.raw.txt` | فرعٌ آخرُ + تحريرُ كلِّ الحجوزاتِ النشطةِ ⇒ **153 · 0** |
| `local-measurements/probe-m0-12b.raw.txt` | فرعٌ ثالثٌ + جدولُ حجوزاتٍ **فارغٌ تماماً** ⇒ **153 · 0** |
| `local-measurements/mut-m0-13.raw.txt` | مقارنةُ المالكِ حرفيّةً بلا تطبيعٍ ⇒ **149 · 4** (منها «السجلُّ الحقيقيُّ سليمٌ») |
| `local-measurements/mut-m0-13b.raw.txt` | `owner_party()` مُعطَّلةٌ في ثوبِ تطبيعٍ ⇒ سقوطُ السلبيّتَينِ — فالعلاجُ ليس تعطيلاً |
| `local-measurements/mut-m0-14a.raw.txt` | إخراجُ `docs/` من المرشِّحِ ⇒ **152 · 1** قُطريّاً |
| `local-measurements/mut-m0-14b.raw.txt` | حذفُ `is_shared "$f" && continue` ⇒ **152 · 1** قُطريّاً |
| `verdict.json` | الأحكامُ الأربعةُ مُهيكَلةً + خاناتُ CI |

---

## 2. كيف تُعادُ هذه القياساتُ حرفاً

```bash
# الأساس
bash scripts/checks/test-governance.sh

# طفرةُ M0-11 — إعادةُ الأنبوب في scripts/checks/validate-launch-board.sh السطر 79
#   grep -Fxq "$id" <<< "$board_ids"   ⟵   printf '%s\n' "$board_ids" | grep -Fxq "$id"

# قياسُ معدَّلِ التقلّب
for i in $(seq 1 300); do bash scripts/checks/validate-launch-board.sh \
  docs/16-progress/LAUNCH_EXECUTION_BOARD.md >/dev/null 2>&1 || echo fail; done | wc -l

# طفرةُ M0-13 (أ) — validate-work-claims.sh السطر 160
#   [[ "$(owner_party "$oi")" == "$(owner_party "$oj")" ]]   ⟵   [[ "$oi" == "$oj" ]]
# طفرةُ M0-13 (ب) — owner_party() تُرجِع ثابتاً واحداً للجميع

# طفرةُ M0-14 (أ) — scripts/checks/lib/meaningful-paths.sh السطر 33: حذفُ إقحامِ 'docs/|'
# طفرةُ M0-14 (ب) — validate-work-claims.sh السطر 199: حذفُ  is_shared "$f" && continue
```

**وبعدَ كلِّ طفرةٍ يُستعادُ الملفُّ من نسختِه ويُتحقَّق أنّ `git diff --stat scripts/` فارغةٌ.** ولا طفرةَ في أيِّ إيداعٍ في هذا الفرعِ.

---

## 3. دليلُ CI — مقروءٌ من الواجهةِ البرمجيّةِ

| الحقل | القيمة |
|---|---|
| رقمُ التشغيل | [`33590373349`](https://github.com/skyosv10-art/wasla/actions/runs/33590373349) |
| الرأس (`head_sha`) | `cc18e581bc130ce09c1fd64b20f9a34e3b13cb4c` |
| مجموعُ الوظائفِ | **26** |
| بدأت | **26** |
| نجحت | **26** |
| أخفقت | **0** |
| لم تبدأ | **0** |

**و«لم تبدأ» ≠ «أخفقت» ≠ «الكودُ أخفق»** — تُقرأ العدَّاداتُ من `GET /actions/runs/<id>/jobs` لا من لونِ الشارةِ.

---

## 4. ما لا يُثبتُه هذا الدليلُ

- **لا يُثبت** أنّ حارسَ الأربعينِ تشغيلاً كافٍ — بل يُثبت **العكسَ** بقياسٍ صريحٍ (قوّتُه نحوَ 5 من المئةِ عندَ المعدَّلِ المقيسِ)، وذلك مكتوبٌ في [`M0-11_GATE.md`](../../M0-11_GATE.md) §4.
- **لا يُثبت** الرقمَ التاريخيَّ «66 من 300»؛ **لم يُعَد إنتاجُه** في هذه البيئةِ ولا يُصادَق عليه بأثرٍ رجعيٍّ ولا يُنكَر.
- **لا يُثبت** فكَّ ارتباطِ `M0-12` بطفرةٍ، بل بسلوكٍ في ثلاثةِ أفرعٍ وأربعِ حالاتِ سجلٍّ.
- **لا يمسُّ** طبقةَ الإنتاجِ إطلاقاً — لا طبقةَ نشرٍ في المستودعِ.
