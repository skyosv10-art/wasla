# مخرَجُ `bash scripts/checks/validate-dependency-audit.sh` — 2026-09-02، قبلَ الرفعِ
#
# ── حدٌّ يُعلَنُ في رأسِ الملفِّ لا في حاشيتِه ──────────────────────────
# هذا نصٌّ **مُلتقَطٌ من مخرَجِ التشغيلِ الفعليِّ** ساعةَ الإخفاقِ، لا مُعادُ توليدِه:
# الشجرةُ التي أنتجته لم تعُد قائمةً بعدَ رفعِ النسختَينِ، فلا يُمكن إعادةُ إنتاجِه
# إلّا بإرجاعِ `package.json` و`pnpm-lock.yaml` إلى `main@44ab8bff` ثمّ إعادةِ التشغيلِ
# على مُسجَّلِ npm بحالتِه اليومَ. وأُلوانُ الطرفيّةِ مُزالةٌ.
#
# البيئةُ: node v20.20.1 · pnpm 9.15.9 · الفرعُ chore/m1-gate-docs-batch6 (شفرتُه شفرةُ main@44ab8bff)

✗ تدقيقُ الاعتماديات — 9 مشكلةً:
  ✗ البابُ 1: شجرةُ الإنتاجِ فيها 8 ثغرةً — ولا استثناءَ في هذا البابِ. الحزمُ: fast-uri
  ✗ البابُ 2: ثغرةٌ (high) في «fast-uri» غيرُ مُعلَنةٍ في docs/00-rules/SECURITY_RULES.md §11: GHSA-5jgf-p345-68v8 (المُصلَح: >=4.1.3).
  ✗ البابُ 2: ثغرةٌ (high) في «fast-uri» غيرُ مُعلَنةٍ في docs/00-rules/SECURITY_RULES.md §11: GHSA-5jgf-p345-68v8 (المُصلَح: >=3.1.6).
  ✗ البابُ 2: ثغرةٌ (high) في «fast-uri» غيرُ مُعلَنةٍ في docs/00-rules/SECURITY_RULES.md §11: GHSA-f65p-4m7j-42xc (المُصلَح: >=4.1.3).
  ✗ البابُ 2: ثغرةٌ (high) في «fast-uri» غيرُ مُعلَنةٍ في docs/00-rules/SECURITY_RULES.md §11: GHSA-f65p-4m7j-42xc (المُصلَح: >=3.1.6).
  ✗ البابُ 2: ثغرةٌ (high) في «fast-uri» غيرُ مُعلَنةٍ في docs/00-rules/SECURITY_RULES.md §11: GHSA-fph4-wmhf-6fwf (المُصلَح: >=4.1.3).
  ✗ البابُ 2: ثغرةٌ (high) في «fast-uri» غيرُ مُعلَنةٍ في docs/00-rules/SECURITY_RULES.md §11: GHSA-fph4-wmhf-6fwf (المُصلَح: >=3.1.6).
  ✗ البابُ 2: ثغرةٌ (high) في «fast-uri» غيرُ مُعلَنةٍ في docs/00-rules/SECURITY_RULES.md §11: GHSA-jqff-g426-hqxp (المُصلَح: >=4.1.3).
  ✗ البابُ 2: ثغرةٌ (high) في «fast-uri» غيرُ مُعلَنةٍ في docs/00-rules/SECURITY_RULES.md §11: GHSA-jqff-g426-hqxp (المُصلَح: >=3.1.6).
  المرجع: docs/00-rules/SECURITY_RULES.md §11 (M0-06)

# ── وما يُقرأُ من هذا النصِّ ولا يُستنتَجُ منه أكثرُ ────────────────────
# • «8 ثغرةً» عدُّ أزواجٍ لا استشاراتٍ: أربعُ استشاراتٍ × خطَّينِ (3.x و4.x).
# • الحزمةُ واحدةٌ (`fast-uri`) وهي **غيرُ مباشرةٍ**: لا تُذكَرُ في أيِّ package.json.
# • البابُ الأوّلُ سقطَ، وهو البابُ الذي **لا يقبلُ استثناءً بنصِّه** — فلم يكن
#   أمامَ الدفعةِ طريقٌ سوى الرفعِ: لا إعلانٌ بمهلةٍ، ولا تعطيلٌ، ولا تأجيلٌ.
