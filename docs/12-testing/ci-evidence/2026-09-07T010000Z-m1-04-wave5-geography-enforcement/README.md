# دليلٌ محلّيٌّ للموجةِ الخامسةِ من M1-04 — فرضُ حدِّ الجغرافيا (2026-09-07)

هذا مجلَّدُ قياسٍ محلّيٍّ لا دليلَ خطِّ CI: `local-measurements/verify.log` هو سجلُّ
تشغيلٍ كاملٍ لـ`scripts/verify.sh` على شجرةِ العملِ (قبلَ الإيداعِ)، وهو **مصدرُ**
كتلةِ `dynamic` في [`BASELINE.json`](../../BASELINE.json) — حتّى يُرجَعَ كلُّ عدَّادٍ
إلى ملفٍّ قائمٍ في المستودعِ لا إلى مسارٍ مؤقّتٍ (`RISK-0028`).

- `pnpm -r typecheck`: رمزُ 0 · `pnpm -r test`: **3816 ناجحاً · 0 فاشلٍ** (226 ملفَّ
  اختبارٍ مُنفَّذاً).
- بوّابةُ الحوكمةِ (12 فحصاً) + حزمةُ اختبارِ الحوكمةِ (174 حالةً · 0 فاشلةٍ) أخضرانِ.
- حزمُ التكاملِ على PostgreSQL حقيقيٍّ وبوّاباتُ الخروجِ الخمسُ التي تُشغِّلُ
  الجغرافيا خُضرٌ على حدٍّ **مفروضٍ** في كلِّ نداءٍ صادرٍ إليها.
- دليلُ خطِّ CI لرأسِ الطلبِ يُقاسُ بعدَ فتحِهِ (`ci-evidence.sh` عندَ أوّلِ
  `started_at ≠ null`) لا يُورَّثُ من هذا المحلّيِّ.
