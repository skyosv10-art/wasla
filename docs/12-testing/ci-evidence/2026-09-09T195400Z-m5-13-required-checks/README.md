# دليلُ قياسٍ — السياقاتُ المطلوبةُ بعدَ إضافةِ ساقِ التوصيلِ (M5-13 · المراجعة 3/N)

> **الحالة:** مقيسٌ · **التاريخ:** 2026-09-09T19:54:00Z · **المصدرُ:** `GET /repos/skyosv10-art/wasla/branches/main/protection`

## لماذا يوجد هذا الدليل

المراجعةُ 3/N أضافت ساقَ `delivery` إلى مصفوفةِ `db-integration` في الخطِّ
(`.github/workflows/ci.yml` · `scripts/ci/run-shared-db-integration.sh` ·
`.gitlab-ci.yml`). والفحصُ الثامنُ في بوّابةِ الحوكمةِ **يُخفِقُ على هذا التغييرِ
بالضبطِ**: وظيفةٌ في الخطِّ ليست سياقاً مطلوباً في حمايةِ الفرعِ تعني بوّابةً
تُشغَّلُ ولا تمنعُ — وهو ما تُسمّيه الوثائقُ «CI مُجمِّلٌ لا مانعٌ».

وحمايةُ الفرعِ **إعدادٌ خارجَ المستودعِ**. فأُضيفَ السياقُ **بموافقةِ المالكِ
الصريحةِ** (طلبُ تأكيدٍ في الجلسةِ · 2026-09-09) عبرَ
`PUT /repos/skyosv10-art/wasla/branches/main/protection` مع حفظِ كلِّ الأعلامِ
القائمةِ كما كانت، ثمّ **أُعيدَ قياسُ اللقطةِ من الواجهةِ الحيّةِ** — لا بتحريرِ
رقمٍ في `MERGE_BLOCKING.json` بيدٍ.

## المُقاس

- عددُ السياقاتِ المطلوبةِ: **30** (كانت 29).
- السياقُ المُضافُ:
  - `db-integration (delivery, @wasla/delivery-service, wasla_delivery_test)`
- `strict: true` · `enforce_admins: true` — بلا تغييرٍ.

## الملفات

- `protection.json` — الجوابُ الخامُّ من الواجهةِ كما وصل.

## ما لا يُدَّعى

هذه لقطةٌ مؤرَّخةٌ لا حيّةٌ: من يملكُ صلاحيةَ الإدارةِ يستطيعُ تغييرَ الحمايةِ بعدَها،
والحارسُ يكشفُ الانحرافَ عندَ إعادةِ القياسِ لا يمنعُه. ولم تُقَسْ **خضرةُ** الساقِ
في CI بعدُ — أوّلُ تشغيلٍ على الفرعِ هو أوّلُ قياسٍ لها.
