# MASTER_PROGRESS — لوحة تقدم المراحل

> **النطاق:** حالة جميع المراحل Phase 00 → Phase 24 (المراحل 71-103 + المراحل الفرعية في الدليل التنفيذي).
>
> **المرجع الأم:** أقسام 71-103 من الدليل التنفيذي + القسم 106 (Progress Ledger).
>
> **Last Updated:** 2026-08-21 (**Phase 06 · Order Engine — In Progress**: MR 4/6 مدمجة — **طبقة HTTP على المنفذ 8087 فصارت الخدمة قابلة للتشغيل لأول مرة**: سبعة مسارات + `/health` بحالتيه + مقبس معاملة `OrderRunner` يجعل كل كتابة داخل وحدة عمل واحدة + نطاق مالك يجيب **404 لا 403** ([ORDER_HTTP.md](../04-api/ORDER_HTTP.md))؛ وسابقاً MR 3/6 — استمرارية Drizzle/Postgres + **وحدة عمل تُسدّ دَين الذرّية** الذي تركته خدمة العملاء مفتوحاً ([ORDER_PERSISTENCE.md](../02-architecture/ORDER_PERSISTENCE.md) · [ADR-010 §127](../15-decisions/ADR-010-order-engine-state-machine-and-assignment-boundary.md))؛ وسابقاً MR 2/6 — طبقة المجال `@wasla/orders-service` بجدول 72 انتقالاً وحارس مطابقة مزدوج مع الوثيقة ومسح 441 زوجاً ([ORDER_CORE_DOMAIN.md](../02-architecture/ORDER_CORE_DOMAIN.md))؛ وسابقاً MR 1/6 — [ADR-010](../15-decisions/ADR-010-order-engine-state-machine-and-assignment-boundary.md) وعقود `services/orders/contracts/*` و`@wasla/contracts-order` و[ORDER_ENGINE.md](../03-domain/ORDER_ENGINE.md)؛ سابقاً Phase 04 · Customer Core **مكتملة** — MR 1/6: ADR-009 وعقود الخدمة، وMR 2/6: طبقة المجال ومنافذها ومُهيّئات الذاكرة، وMR 3/6: استمرارية Drizzle/Postgres ووظيفة `customer-db-integration`، وMR 4/6: طبقة HTTP على المنفذ 8086، وMR 5/6: ربط بوت العميل بالنواة عبر بذرة محادثة محيّدة، وMR 6/6: **بوابة خروج المرحلة E2E** اجتازت — انظر [PHASE04_EXIT_GATE_E2E.md](../12-testing/PHASE04_EXIT_GATE_E2E.md)؛ المرحلة التالية Phase 06 — Order Engine) · **Status:** Active · **Related Team:** جميع الفرق (كل فريق يحدّث حالة مرحلته)
>
> **القاعدة:** لا يسمح بعبارة «Done» بدون **Evidence** (رابط MR، نتائج اختبار، لقطة).
>
> **هذه الوثيقة = لوحة الحالة اللحظية فقط.** التسلسل والزمن ومسار التنفيذ الرسمي يعيش في [ROADMAP.md](ROADMAP.md). لا تعارض بينهما: التقدم يُسجّل هنا، والترتيب يُقرأ من هناك. أي تغيير في الترتيب يتطلب ADR (انظر قسم 7 في ROADMAP).

---

## كيفية التحديث

لكل مرحلة، يحدّث الفريق المسؤول الحقول التالية عند كل تغيير:

```text
Phase
Status
Exit Gate
Teams
Open Blockers
Last Update
Evidence
Next Step
```

الانتقال بين المراحل يتم فقط بعد اجتياز **Exit Gate** (اختبارات + وثائق + أمان + تكامل). لا يتم الانتقال لمجرد انتهاء البرمجة.

### قيم Status المعتمدة

```text
Not Started
In Progress
Blocked
Exit Gate Pending
Completed
```

---

## لوحة المراحل

| Phase | العنوان | Status | Exit Gate | Owner Teams | Open Blockers | Last Update | Evidence | Next Step |
|---|---|---|---|---|---|---|---|---|
| 00 | Repository Foundation | **Completed (W0: 2026-08-20)** — Exit Gate اجتاز | CI passes ✅ (main pipeline 2776059637)، لا أسرار، جميع الفرق clone/build/test، Docs structure، Main branch protected، MR template active | جميع الفرق (Team 10 CI، Team 11 gates، Team 12 Telegram skeleton، Team 09 DB conventions) | **لا عوائق.** shared runners مُفعّلة (تحقّق المالك من namespace). build-test CI صُلح عبر [MR !9](https://gitlab.com/uxxxu/wasla/-/merge_requests/9) (إضافة `@types/node`) ودُمج. pipeline على `main` يجتاز بالكامل (build-test + markdown-lint + repo-structure ✅). **تفاصيل التسليم: [HANDOFF_NEXT_STEPS.md](HANDOFF_NEXT_STEPS.md)** | 2026-08-20 | **[MR !1](https://gitlab.com/uxxxu/wasla/-/merge_requests/1) + [MR !3](https://gitlab.com/uxxxu/wasla/-/merge_requests/3) + [MR !4](https://gitlab.com/uxxxu/wasla/-/merge_requests/4) + [MR !9](https://gitlab.com/uxxxu/wasla/-/merge_requests/9) مدمجة** — بنية المستودع + القوانين + قالب MR + حماية main + فحص أسرار + doc-coverage + أساس بناء (pnpm+TS+Vitest) عبر [ADR-003](../15-decisions/ADR-003-monorepo-tooling.md) مع job `build-test` في CI + إصلاح `@types/node`. pipeline على main: نجاح كامل. | **بدء Phase 01**: تنفيذ خدمة Identity وفق [ADR-005](../15-decisions/ADR-005-identity-service-implementation-stack.md) عبر MRs مستقلة (إضافة اعتماديات + تنفيذ ضد العقود/الأنواع + Wasla Public ID + outbox + Contract tests) |
| 01 | Identity Foundation | **Completed (W0: 2026-08-20)** — Exit Gate اجتاز | مستخدم Telegram يُنشأ، تتغيّر Username، تبقى الهوية/Public ID/internal_uuid مستقرة، ويسجّل التاريخ/outbox | 01, 09, 10, 11, 12 | **لا عوائق.** Exit Gate مُتحقَّق عبر اختبار E2E في CI (job `db-integration` ضد postgres:15). | 2026-08-20 | عقود ([MR !2](https://gitlab.com/uxxxu/wasla/-/merge_requests/2)) + `@wasla/contracts-identity` (13) + [ADR-005](../15-decisions/ADR-005-identity-service-implementation-stack.md) + MR 1 ([!11](https://gitlab.com/uxxxu/wasla/-/merge_requests/11): نواة 15) + MR 2 ([!12](https://gitlab.com/uxxxu/wasla/-/merge_requests/12): Drizzle/Postgres + 3 تكامل) + MR 3 ([!13](https://gitlab.com/uxxxu/wasla/-/merge_requests/13): Fastify HTTP + 9) + MR 4 ([!14](https://gitlab.com/uxxxu/wasla/-/merge_requests/14): CI `db-integration` + postgres service) + **MR 5 ([!15](https://gitlab.com/uxxxu/wasla/-/merge_requests/15): Exit Gate E2E — 2 اختبار)**. 24 وحدة + 3 تكامل + 2 E2E. | بدء Phase 02 (Geography & Localization) |
| 02 | Geography & Localization Foundation | **Completed (2026-08-20)** — Exit Gate اجتاز: عقود + ADR-006 (!17) + النواة المجردة (!18) + Drizzle/Postgres + Saudi seed (!19) + طبقة HTTP (!20) + تكامل CI لقاعدة البيانات (!21) + Exit Gate E2E (!22) | المستخدم يغيّر موقعه دون حساب جديد، وكل Module يستعمل Geo IDs | 01, 02, 03, 06, 07, 08, 09 | **لا عوائق.** بوابة الخروج مُتحقَّقة باختبار E2E في CI (وظيفة `geography-db-integration` ضد postgres:15) يُشغّل الخدمتين معاً كما في الإنتاج: خدمة Identity تستمع على منفذ حقيقي وخدمة Geography تسألها عبر `HttpIdentityLookupPort` — تغيير الموقع مرتين دون إنشاء حساب ثانٍ، ثبات `wasla_public_id`/`internal_uuid`، `history` + `outbox`، وأسماء مترجمة ar/en/ur مع الرجوع إلى ar. إجمالاً 96 اختبار وحدة (41 geography) + 4 تكامل + 3 E2E. | 2026-08-20 | [ADR-006](../15-decisions/ADR-006-geography-localization-stack-and-model.md) + `@wasla/contracts-geography` (15) + [MR !17](https://gitlab.com/uxxxu/wasla/-/merge_requests/17) (عقود) + `@wasla/geography-service` pure core (25 اختباراً) + [MR !18](https://gitlab.com/uxxxu/wasla/-/merge_requests/18) (domain/ports/in-memory/use-cases/locale fallback) + [MR !19](https://gitlab.com/uxxxu/wasla/-/merge_requests/19) (Drizzle/Postgres + Saudi seed + 4 تكامل) + [MR !20](https://gitlab.com/uxxxu/wasla/-/merge_requests/20) (Fastify HTTP + تعيين الأخطاء + 16 اختبار app.inject) + [MR !21](https://gitlab.com/uxxxu/wasla/-/merge_requests/21) (وظيفة geography-db-integration في CI). + **MR 7 ([!22](https://gitlab.com/uxxxu/wasla/-/merge_requests/22): بوابة الخروج E2E — 3 اختبارات تجمع identity + geography)** | بدء Phase 03 (Telegram Channel Foundation)
| 03 | Telegram Channel Foundation | **Completed (2026-08-21)** — Exit Gate اجتاز · MR 7/7: [ADR-007](../15-decisions/ADR-007-telegram-channel-adapter-isolation-and-stack.md) + عقود القناة + `@wasla/contracts-channel` (!23) + نواة `@wasla/channel-core` المحايدة (!24) + مُهيّئ `@wasla/telegram-adapter` (!25) + طبقة تشغيل البوتات `@wasla/bot-runtime` والبوتات الثلاثة (!27) + مُهيّئات Postgres `@wasla/channel-postgres` ووظيفة `channel-db-integration` (!28) + مُهيّئ المجموعات وسجلّها منفذاً عاشراً بـ[ADR-008](../15-decisions/ADR-008-channel-groups-registry-and-reply-policy.md) ([!29](https://gitlab.com/uxxxu/wasla/-/merge_requests/29)) | كل Bot يفتح Mini App، وAdapter قابل للاستبدال بـMock | 12, 01, 02, 03, 07 | **لا عوائق.** القناة طبقة توصيل لا خدمة (`packages/channel-core` محايد + `packages/telegram-adapter` وحده يعرف Bot API + `bots/*` جذور تركيب). المنافذ **العشرة** قائمة و`MockChannelAdapter` مُنفَّذ، والمُهيّئ الحقيقي صار قابلاً للتشغيل داخل حالات استخدام النواة (8 اختبارات مطابقة منافذ في MR 3) — أي نصف بوابة الخروج (استبدال المُهيّئ) صار مُبرهناً على مستوى الحزمة. وبعد MR 4 صار **الشطر الثاني من بوابة الخروج قابلاً للتشغيل**: ثلاثة تطبيقات قابلة للنشر (8083/8084/8085) كل واحد يخدم بوته ويفتح Mini App الخاصة به ويرفض البوتين الآخرين بـ404، والسلوك كلّه في حزمة واحدة `@wasla/bot-runtime` (بوت واحد ⇄ Mini App واحدة مفروضة في مكان واحد) — يبقى E2E الجامع في MR 7. وبعد MR 5 صار **التخزين دائماً**: مُهيّئات Postgres للمنافذ الثلاثة تُركّب عند وجود `DATABASE_URL` (ومجموعة الذاكرة بغيابه للتشغيل المحلي)، فمنع التكرار وطابور إعادة المحاولة يعبران إعادة التشغيل — مُثبَت بـ**21 اختبار تكامل** على Postgres حقيقي منها 6 اختبارات مطابقة منافذ تقارن المجموعتين داخل حالات الاستخدام نفسها. وبعد MR 6 صارت **المجموعات مدعومة داخل نفس المنافذ** لا في مسار مُوازٍ: سجل مجموعات منفذاً عاشراً يقرأ الغرف من البيئة (دعم/تصعيد/مجتمع)، ولا تهيئة هوية من غرفة (المرجع مشترك)، ولا زر Mini App نحو غرفة بل رابط عميق يفتح المحادثة الخاصة، وصمت تام في غرفة غير مُعلَنة مع تسجيلها و202 — وأحداث العضوية صارت `group_event` بدل 422. و**بوابة الخروج نفسها صارت مُثبَتة في MR 7** لا موصوفة: حزمة اختبار بحتة `@wasla/channel-e2e` تبني البوتات الثلاثة في عملية واحدة أمام **خدمة هوية واحدة تستمع فعلياً على HTTP** ومخازن قناة مشتركة، فتُقاس الأخطاء التي لا يراها اختبار بوت واحد: كل بوت يُجيب `/start` بزرّ Mini App **الخاص به** (customer→customer · driver→driver · partner→partner) وبأثر `channel.mini_app.launched` منسوب إليه · شخص واحد عبر البوتات الثلاثة = **هوية واحدة** (`created: false` و`identity.created` مرّة واحدة) · التحديث المُعاد **لا يُعالَج مرّتين** (202 `duplicate`) ومنع التكرار **مقيَّد بالبوت** لا بالرقم · و`MockChannelAdapter` **تجاوزٌ لافتراض حقيقي** هو `TelegramChannelAdapter` لا مسارٌ وحيد. والبوابة غير قابلة للتخطّي: تعمل بمخازن الذاكرة في كل `pnpm -r test` وبـPostgres في وظيفة `channel-exit-gate-e2e`، وفحص طفرة أسقط 4 من 8 اختبارات عند كسر اشتقاق Mini App ([تفصيل](../12-testing/PHASE03_EXIT_GATE_E2E.md)). إجمالاً 445 اختبار وحدة + 25 تكامل + 8 بوابة خروج القناة. **يُنقَل إلى Phase 04**: مُشغّل دوري لـ`retryDueDeliveries` وناشر لصندوق الصادر (موثّقان في [CHANNEL_PERSISTENCE.md §7](../02-architecture/CHANNEL_PERSISTENCE.md)) وسياسة استبقاء لـ`channel_updates`. | 2026-08-20 | [ADR-007](../15-decisions/ADR-007-telegram-channel-adapter-isolation-and-stack.md) + `packages/channel-core/contracts/` (api.openapi.yml + events.json + schema.sql + errors.md) + `@wasla/contracts-channel` (34 اختباراً) + [MR !23](https://gitlab.com/uxxxu/wasla/-/merge_requests/23) + **MR 2 ([!24](https://gitlab.com/uxxxu/wasla/-/merge_requests/24): نموذج المجال + المنافذ التسعة + 4 حالات استخدام + مُهيّئات in-memory/Mock + 84 اختباراً منها 38 اختبار حراسة معماري — إجمالي المستودع 214 اختبار وحدة)** + [CHANNEL_LAYER_CORE.md](../02-architecture/CHANNEL_LAYER_CORE.md) + **MR 3 ([!25](https://gitlab.com/uxxxu/wasla/-/merge_requests/25): `@wasla/telegram-adapter` — TelegramUpdateParser + TelegramChannelAdapter + عميل Bot API + أزرار `web_app` + تخطيط أخطاء Bot API مع `retry_after` + ميزانية معدّل لا تنام + التحقّق من رمز الـwebhook · 86 اختباراً منها 8 مطابقة منافذ — إجمالي المستودع 300 اختبار وحدة)** + [CHANNEL_TELEGRAM_ADAPTER.md](../02-architecture/CHANNEL_TELEGRAM_ADAPTER.md) + **MR 4 ([!27](https://gitlab.com/uxxxu/wasla/-/merge_requests/27): `@wasla/bot-runtime` — سطح HTTP لعقد القناة على Fastify + تهيئة من البيئة بفشل سريع + `SingleBotRegistry` + مُهيّئ الهوية عبر HTTP + التركيب؛ و`bots/{customer,driver,partner}-bot` جذور تسمّي بوتها فقط · 76 اختباراً — إجمالي المستودع 376 اختبار وحدة)** + [CHANNEL_BOTS.md](../02-architecture/CHANNEL_BOTS.md) + **MR 5 ([!28](https://gitlab.com/uxxxu/wasla/-/merge_requests/28): `@wasla/channel-postgres` — مرآة Drizzle لـ`schema.sql` مع اختبار حراسة انحراف + منع تكرار ذرّي بـ`ON CONFLICT DO NOTHING … RETURNING` + تسليم idempotent وطابور استحقاق مُرتّب في SQL + صندوق صادر بمفتاح `event_id` + `createChannelStores` مربوطاً في `buildBotRuntime` و`onClose` · 30 اختباراً جديداً + وظيفة `channel-db-integration`)** + [CHANNEL_PERSISTENCE.md](../02-architecture/CHANNEL_PERSISTENCE.md) + **MR 6 ([!29](https://gitlab.com/uxxxu/wasla/-/merge_requests/29): `GroupRegistryPort` منفذاً عاشراً من الإعداد + `ConversationScope`/`GroupRole`/`GroupPresence` + قصر تهيئة الهوية على المحادثات الخاصة + رفض سطح التشغيل نحو غرفة من طبقتين + تحليل `my_chat_member`/`chat_member` إلى `group_event` + ثلاثة متغيّرات بيئة للغرف + ردّ مجموعة برابط عميق وصمت في الغرفة المجهولة · 45 اختباراً جديداً — إجمالي المستودع 438 اختبار وحدة)** + [ADR-008](../15-decisions/ADR-008-channel-groups-registry-and-reply-policy.md) + [CHANNEL_GROUPS.md](../02-architecture/CHANNEL_GROUPS.md)  + **MR 7 ([!30](https://gitlab.com/uxxxu/wasla/-/merge_requests/30): `@wasla/channel-e2e` — بوابة خروج المرحلة E2E بثمانية اختبارات + وظيفة `channel-exit-gate-e2e` في CI + [وثيقة البوابة](../12-testing/PHASE03_EXIT_GATE_E2E.md) · صفر تغيير في كود الإنتاج)** | بدء Phase 04 (Customer Core) |
| 04 | Customer Core | **Completed (2026-08-21)** — Exit Gate اجتاز · MR 1/6 → 6/6 مدمجة: [ADR-009](../15-decisions/ADR-009-customer-core-placement-and-order-intake-boundary.md) + عقود `services/customers/contracts/*` + `@wasla/contracts-customer` + [CUSTOMER_CORE.md](../03-domain/CUSTOMER_CORE.md) | عميل ينشئ Order صالحًا ويصل إلى Order Engine دون Matching فعلي | 02, 01, 11, 12 | **لا عوائق.** المرحلة بدأت من حدّها لا من كودها: خدمة `services/customers` مستقلّة بانحراف **معلَن** عن شجرة §68 ([CONTAINERS §4.1](../02-architecture/CONTAINERS.md))، وملف العميل **ملفُّ دور** مفتاحه `wasla_public_id` بلا FK إلى الهوية (الأدوار غير متعارضة — §7)، و**الطلب ليس مِلكها**: تُسلّم نيّة مُتحقَّقة عبر `OrderIntakePort` ولا تكتب `orders` ولا تُولّد `order_public_id` (Phase 06 تملك آلة الحالة — §15). والنقطة تُعرَّف بـ`zone_id` من هرم [ADR-006](../15-decisions/ADR-006-geography-localization-stack-and-model.md) **إلزامياً** والإحداثية اختيارية لا تُقرّر تغطية ولا مطابقة (لا Reverse Geocoding في النظام — §28)، والنقاط قائمة مرتّبة بنقطتين في هذه المرحلة فـMulti-stop بلا هجرة، والسعر وضعان صريحان بمبلغ **عدد صحيح بالوحدة الصغرى**، و`Idempotency-Key` إلزامي على الكتابات (§43)، وتعذّر التسليم **fail-closed** بصف `submission_failed` وحدث فشل و503. وكل هذه الحدود **مُختبَرة لا موصوفة**: 42 اختباراً منها حرّاس يقرأون `schema.sql` و`api.openapi.yml` و`errors.md` فيفشلون إذا ظهر جدول/مسار للطلبات أو FK عبر الخدمات أو مال عشري أو فريدة تمنع Multi-stop أو كود خطأ غير موثّق، وحارس خصوصية يُسقط أي حمولة حدث تحمل إحداثية أو نصّاً كتبه المستخدم. إجمالاً 487 اختبار وحدة على 16 مشروعاً. و**بعد MR 2/6 صار السلوك مُنفَّذاً لا موصوفاً** وبلا بنية تحتية تفرض نفسها عليه: مشروع العمل السابع عشر `services/customers` بطبقة مجال نقيّة (كيانات · تحقّق يرمي أكواداً موثّقة · ستة مصانع أحداث تحمل قاعدة الخصوصية في موضع واحد) وسبعة منافذ لكلٍّ منها مُهيّئ ذاكرة، وثمانية حالات استخدام تُثبّت ما كان نصّاً في ADR-009: المعاينة **تقرأ ولا تكتب** (مُختبَر بأثر: لا صفّ ولا حدث)، والتسليم يُحاوَل **قبل** الكتابة الواحدة فلا يوجد صفّ يعلن تسليماً «قيد التنفيذ»، وإعادة المحاولة تجري **على الصفّ نفسه** لا بصفّ ثانٍ، وتعذّر المحرّك يُنتج 503 للعميل بينما يُحفظ السبب التشغيلي (`UNAVAILABLE`/`REJECTED`/`TIMEOUT`) في الصفّ والحدث لمن يشغّل النظام، وتحديث لا يغيّر شيئاً **لا يُنتج حدثاً**، والمفتاح المفقود ≠ المعطوب، وكل قراءة وحذف مقصورة على المالك بـ404 لا 403. و48 اختباراً تحرس هذا كلّه في ~1.2 ثانية بلا قاعدة ولا شبكة، منها **اختبار خصوصية سلبي** يمرّ برحلة عميل كاملة ثم يفتّش الأحداث المُسلسَلة عن أي اسم أو تسمية أو عنوان أو ملاحظة أو إحداثية فيفشل إن ظهر أيٌّ منها. إجمالاً **535 اختبار وحدة على 17 مشروعاً**. و**بعد MR 3/6 صار التخزين دائماً بلا أن يفرض نفسه على المجال**: مرآة Drizzle لـ`schema.sql` و`PostgresCustomerRepository` و`PostgresCustomerOutbox` وراء المنافذ نفسها، و**صفر تغيير في `src/use-cases/`** — وهو المعيار المكتوب: أي اضطرار لتغيير سلوك هناك دليلٌ على أن المخطّط بدأ يقود المجال. وثلاثة قرارات في المُهيّئ لكلٍّ منها نسخة أرخص وخاطئة: العمود `NULL` يعود **حقلاً غائباً** لا `null` (وإلا صارت بصمة idempotency تعتمد على *من أين قُرئ الصف*، فتُطابق من الذاكرة وتختلف من القاعدة والنتيجة 409 على تكرار مشروع) · أخطاء التكرار **مُترجَمة** إلى رسائل مُهيّئ الذاكرة نفسها (بمشي سلسلة `cause` لأن Drizzle يلفّ خطأ المُشغّل فيسكن SQLSTATE على `error.cause`) · و`NUMERIC` يُحوَّل رقماً **مرّة واحدة** في المُهيّئ. والطلب ونقاطه في **معاملة واحدة** (مُختبَر بإفشال إدخال النقاط والتحقّق أن الطلب لم يبقَ) لأن طلباً بلا نقاطه ليس محفوظاً جزئياً بل **لا يمكن الإجابة عنه**. و**حُسم دَين `shipment_description` بالتبنّي لا بالإسقاط**: التوثيق السابق قال إن OpenAPI لا تنشره، والعقد ينشره فعلاً بحدّ 300 محرفاً واختبار العقود يُمرّره — فالمجال كان يُسقط حقلاً منشوراً بصمت؛ صار الآن مُتحقَّقاً منه وداخلاً بصمة الـidempotency ومُسلَّماً لمحرّك الطلبات، و**لا يُنشَر في أي حدث** بحارس صريح. والتحقّق ثلاث طبقات: **17** اختبار حراسة انحراف يقرأ `schema.sql` فعلياً (تعمل بلا قاعدة) · **27** اختبار مُهيّئ على Postgres حقيقي · و**16 اختبار مطابقة منافذ** تُكتَب مرّة وتُنفَّذ مرّتين (ذاكرة/Postgres) داخل حالات الاستخدام نفسها بساعة ومعرّفات ثابتة، فالنتيجة **والأحداث** يجب أن تتطابق — وهي وحدها ما يمنع أن يختلف المُهيّئان بهدوء في الترتيب أو في معنى الحقل الغائب فيظهر الخلاف في الإنتاج بعد أشهر. إجمالاً **553 اختبار وحدة** + **43 اختبار تكامل** لخدمة العملاء في وظيفة `customer-db-integration`. **دَيْن مُعلَن (مكتوب لا مكتشف):** لا ذرّية بين كتابة الصف وإلحاق الحدث (منفذان بلا حدّ معاملة — سدُّه يعني تغيير `use-cases/` وهو ما تمنعه الدفعة؛ محلّه MR 4/6) · `customer_outbox` بلا عمود `trace_id` فيفقد الحدث المُعاد بناؤه معرّف ارتباطه · ولا ناشر لصندوق الصادر بعد ([التفصيل](../02-architecture/CUSTOMER_PERSISTENCE.md)). و**بعد MR 4/6 صارت الخدمة قابلة للتشغيل لأول مرة**: `createCustomerApp` يربط المسارات العشرة بحالات الاستخدام دون بدء الاستماع (فالاختبارات عبر `app.inject` بلا منفذ)، و`src/http/server.ts` هو التركيب النهائي على **8086** والمنادي الوحيد لـ`createCustomerDb`. وثلاثة قرارات في هذه الطبقة لكلٍّ منها نسخة أرخص وخاطئة: **التحقّق شكليٌّ فقط** وكل قاعدة ذات معنى تبقى في `domain/validation.ts` لأن البوت في MR 5/6 ينادي حالات الاستخدام مباشرة، فقاعدةٌ تُكتب في HTTP تصبح قاعدة لا تراها القناة · **جسم ليس JSON يعود 400 لا 503** لأن تسميته انقطاعَ خدمة تأمر المنادي بإعادة محاولة طلبٍ لن ينجح أبداً · و**`/health` يعود `degraded` طالما لا مُهيّئ لمحرّك الطلبات** فبناءٌ لا يستطيع إتمام تسليم واحد لا يحقّ له أن يقول `ok`. و`x-request-id` مُفعَّل (معطّل افتراضياً في Fastify 5) فمعرّف ارتباط المنادي هو نفسه الذي يظهر في `trace_id` وفي مغلّفات الأحداث. و`zone_path` **أفضل-جهد**: فشل الجغرافيا يعيد `null` ولا يحجب عن العميل بياناته المحلية. **34 اختبار `app.inject`** + تشغيل حقيقي مُوثَّق الدليل — إجمالاً **587 اختبار وحدة**. **دَيْن مُعلَن بأمانة:** دَين الذرّية **لم يُسدّ في MR 4/6 خلافاً لما وعدت به الدفعة السابقة**، وأُعيد إسناده إلى **Phase 09** لأن منفذ وحدة-عمل يمسّ أربع حالات استخدام ومُهيّئين وطاقم المطابقة فهو دفعة كاملة، ولأن أول من يتضرّر من حدثٍ ناقص هو الناشر وهو غير موجود بعد · ولا مصادقة على المسارات (قرار البوابة — Phase 06) · واستدعاء جغرافيا لكل منطقة مميّزة بلا دالة دفعة ([التفصيل](../04-api/CUSTOMER_HTTP.md)). و**بعد MR 5/6 صار للنواة مستهلك حقيقي بلا أن تتعلّم طبقة القناة وجود مجال**: بدلاً من فرع `if (bot === "customer")` داخل مسار الويب هوك — وهو النسخة الأرخص التي تهدم ADR-007 rule 2 وتُدخل فروع مجالات في المسار الأمني الوحيد غير المُصادَق في وصلة — أُضيفت **بذرة محادثة محيّدة** في `@wasla/bot-runtime`: الجذر يُسلّم دالّة واحدة تأخذ حدثاً محايداً (مرجع محادثة، معرّف تحديث، أمر، نطاق، اسم عرض ولغة) وتُعيد نصاً أو **صمتاً**. ولا يعبر الحدّ حمولةٌ خام ولا معرّف قناة أصلي — وهذا **مُختبَر بمفاتيح الحدث مُثبَّتة بالاسم** لا موصوفاً، وملف اختبار البذرة لا يذكر عميلاً ولا طلباً ولا منطقة. وربط بوت العميل ثلاثة تدفقات فقط: `/start` يضمن الملف **صامتاً** (رسالة الترحيب هي الردّ، فلا رسالتان) · `/places` قائمة الأماكن المحفوظة · `/orders` أحدث خمسة طلبات وحالتها. و**إنشاء الطلب لم يُنفَّذ بقصد، خلافاً لما وعدت به دفعة MR 4/6 نصّاً**: الطلب الصالح يحتاج محطّتين بمنطقة ومصدر لكل واحدة وصنف مركبة ونمط سعر (ADR-009 §4)، وجمع ذلك عبر رسائل يعني اختراع تدفّق منتج داخل دفعة هندسية، بينما [USER_FLOWS §1 و§6](../01-product/USER_FLOWS.md) يضع «الأعمال الثقيلة» في التطبيق المصغّر — فأُسنِد إلى **Phase 11** صراحةً. وثلاثة قرارات أخرى لكلٍّ منها نسخة أرخص وخاطئة: **`ensureProfile` يقرأ ثمّ يُنشئ عند الغياب وحده** لأن `upsert` أعمى كان سيستبدل الاسم الذي ضبطه المستخدم في التطبيق عند كل `/start` · **الهوية تُحلَّل متأخّرةً ومرّة واحدة** (`resolveIdentity()` مُخزَّن) فتدفّقٌ لا يسأل لا يدفع رحلة شبكة، وصفر رحلات إضافية عند `/start` · و**فشل غير مجاليّ يعني صمتاً و202** لا 5xx، لأن رفض تحديثٍ سُجّل كمُعالَج يجعل تيليجرام يُعيده فيُرفَض كتكرار: تُفقَد الرسالة **و**يُستهلك بدل المحاولات. والاعتماد على النواة يعيش في **ملف واحد** (`customer-core.ts`) وراء `CustomerFlowsPort`، فالانتقال إلى نداء خارج العملية استبدالٌ لا إعادة كتابة. وبلا `CUSTOMER_DATABASE_URL` **لا تُركَّب التدفقات ولا يُسجَّل الأمران** (422 صريح) ولا بديل في الذاكرة، لأن قاعدة عملاء فارغة في نشرٍ حقيقي تجيب «لا توجد أماكن» لعميل لديه عشرون مكاناً — كذبةٌ مُقنِعة أسوأ من رفض ظاهر. **+29 اختباراً** (13 للبذرة · 14 للتدفقات · 2 لإتاحة الفاعل) — إجمالاً **616 اختبار وحدة** على 17 مشروعاً، بلا منفذ ولا قاعدة ولا رمز تيليجرام. **دَيْن مُعلَن:** `/health` للبوت لا يُعلن غياب النواة · مسار المنطقة البشري لا يُحلّ في قوائم الأماكن (نداء جغرافيا لكل مكان) · ودَين الذرّية باقٍ في Phase 09 ([التفصيل](../02-architecture/CUSTOMER_BOT_FLOWS.md)). **المتبقّي:** MR 6/6 (بوابة الخروج E2E مع محرّك طلبات بديل ووظيفة CI وإغلاق المرحلة) — [HANDOFF §9](HANDOFF_NEXT_STEPS.md). | 2026-08-21 | [ADR-009](../15-decisions/ADR-009-customer-core-placement-and-order-intake-boundary.md) + `services/customers/contracts/` (api.openapi.yml بسبعة مسارات + events.json بستة أحداث + schema.sql بخمسة جداول + errors.md بـ18 كوداً) + `@wasla/contracts-customer` (42 اختباراً) + [CUSTOMER_CORE.md](../03-domain/CUSTOMER_CORE.md) + [MR !31](https://gitlab.com/uxxxu/wasla/-/merge_requests/31) + **MR 2/6 ([!32](https://gitlab.com/uxxxu/wasla/-/merge_requests/32): `@wasla/customers-service` — `src/domain/{model,errors,validation,events}` + سبعة منافذ في `src/ports.ts` + ثماني حالات استخدام + `mappers.ts` (المجال camelCase ⇄ العقد snake_case في موضع واحد) + `src/infrastructure/in-memory.ts` بمستودع يُطبّق قيود `schema.sql` بنفسه و`UnavailableOrderIntake` افتراضاً fail-closed · 48 اختباراً — إجمالي المستودع 535 اختبار وحدة)** + [CUSTOMER_CORE_DOMAIN.md](../02-architecture/CUSTOMER_CORE_DOMAIN.md)  + **MR 3/6 ([!34](https://gitlab.com/uxxxu/wasla/-/merge_requests/34): `infrastructure/drizzle/{schema,db,repository}.ts` — مرآة Drizzle للجداول الخمسة + `PostgresCustomerRepository` و`PostgresCustomerOutbox` + تبنّي `shipment_description` + وظيفة `customer-db-integration` · 17 حراسة انحراف + 1 حارس خصوصية + 43 اختبار تكامل)** + [CUSTOMER_PERSISTENCE.md](../02-architecture/CUSTOMER_PERSISTENCE.md) + **MR 4/6 ([!35](https://gitlab.com/uxxxu/wasla/-/merge_requests/35): `src/http/{requests,errors,app,server}.ts` — عشرة مسارات + `/health` بحالتيه + تخطيط الأكواد الثمانية عشر بلا كود جديد + `infrastructure/{http-identity-lookup,http-geography}.ts` + `x-request-id` → `trace_id` → مغلّف الحدث · 34 اختبار `app.inject` — إجمالي المستودع 587 اختبار وحدة)** + [CUSTOMER_HTTP.md](../04-api/CUSTOMER_HTTP.md) + **MR 5/6 ([!36](https://gitlab.com/uxxxu/wasla/-/merge_requests/36): `packages/bot-runtime/src/conversation.ts` — بذرة محادثة محيّدة + خيار `onConversation` · `ReceiveUpdateResult.actor` · `bots/customer-bot/src/{flows.ts,customer-core.ts}` — ثلاثة تدفقات وراء `CustomerFlowsPort` واعتمادٌ على النواة في ملف واحد · 29 اختباراً — إجمالي المستودع 616 اختبار وحدة)** + [CUSTOMER_BOT_FLOWS.md](../02-architecture/CUSTOMER_BOT_FLOWS.md) + **MR 6/6 (بوابة الخروج · [!37](https://gitlab.com/uxxxu/wasla/-/merge_requests/37)): حزمة اختبار بحتة `@wasla/customer-e2e` تبني بوت العميل ونواة العميل في عملية واحدة أمام **خدمتَي هوية وجغرافيا تستمعان فعلاً على HTTP** و**محرّك طلبات بديل يقرأ عقد `OrderIntakeRequest` ويرفض ما لا يطابقه**، فتُقاس الأخطاء التي لا يراها اختبار وحدة: الحمولة تُسَلسَل بالحقول المنشورة (snake_case · معرّفات مناطق نشيطة حقيقية · سعر عدد صحيح بوحدات صغرى · `idempotency_key`) · الصفّ يحمل `order_public_id` **الذي منحه المحرّك** · إعادة نفس المفتاح ⇒ 200 ونداء واحد للمحرّك · مفتاح بجسم مختلف ⇒ 409 · الرفض والتعطّل وانعدام الإجابة كلّها **fail-closed** (صفّ `submission_failed` + حدث بسببه الدقيق + 503، والبوت يعرض «لم يصل للمحرّك») · منطقة مجهولة ⇒ 404 وصفر نداء · ما يكتبه HTTP يقرأه البوت · ولا حدث يحمل إحداثية أو نصّاً كتبه المستخدم · و`/health` = `ok` فقط لتركيب يستطيع إتمام تسليم. البوابة **غير قابلة للتخطّي**: تعمل بمخازن الذاكرة في كل `pnpm -r test` وعلى Postgres في وظيفة `customer-exit-gate-e2e` — **11/11 في الوضعين** · إجمالي المستودع 627 اختباراً على 18 مشروعاً · صفر تغيير في منطق الإنتاج (تصدير نصّين للعرض فقط) — [وثيقة البوابة](../12-testing/PHASE04_EXIT_GATE_E2E.md)** | بدء Phase 06 (Order Engine) — المحرّك البديل في `packages/customer-e2e/src/stub-order-engine.ts` هو مواصفته التنفيذية · و**المُنقَل إلى Phase 09**: ذرّية الكتابة والنشر + ناشر `customer_outbox` + مُشغّل دوري لـ`retryDueDeliveries` · و**إلى Phase 11**: إنشاء الطلب وحفظ مكان من الواجهة |
| 05 | Driver Core | Not Started | Driver profile مكتمل قابل للإدخال في Candidate pool | 03, 01, 11, 12, 09 | — | — | — | Registration + Documents + Trial subscription + States |
| 06 | Order Engine | **In Progress (2026-08-21)** — MR 4/6 من ستّ مدمجة: **طبقة HTTP على 8087** ([ORDER_HTTP.md](../04-api/ORDER_HTTP.md))؛ وMR 3/6: استمرارية Drizzle/Postgres + **وحدة عمل تُسدّ دَين الذرّية** ([ORDER_PERSISTENCE.md](../02-architecture/ORDER_PERSISTENCE.md))؛ وMR 2/6: [ADR-010](../15-decisions/ADR-010-order-engine-state-machine-and-assignment-boundary.md) وعقود `services/orders/contracts/*` و`@wasla/contracts-order` و[ORDER_ENGINE.md](../03-domain/ORDER_ENGINE.md) وطبقة المجال `@wasla/orders-service` | إنشاء Order وتغييره عبر الحالة دون حالات مستحيلة | 02, 03, 04, 05, 09, 11 | **لا عوائق — والمرحلة لا تنتظر Phase 05.** بدأت المرحلة من حدّها لا من كودها: [ADR-010](../15-decisions/ADR-010-order-engine-state-machine-and-assignment-boundary.md) يُثبّت خمسة قرارات كلٌّ منها له نسخة أرخص وخاطئة. **(1) لا حالة `draft`**: الطلب يبدأ `published` لأن نيّة غير مُتحقَّقة لا تستحقّ معرّفاً عاماً ولا صفّاً في `orders` — وPhase 04 تُسلّم نيّة **مُتحقَّقة بالفعل** عبر `OrderIntakePort`، فحالة مسوّدة كانت ستكرّر تحقّقاً موجوداً وتُنتج طلبات معلّقة بلا مالك. **(2) جدول انتقالات صريح لا مُشتَقّ**: 21 حالة تعني 441 زوجاً، والمسموح **72** فقط — والاثنان والسبعون مكتوبة صفّاً صفّاً في [ORDER_ENGINE.md §4](../03-domain/ORDER_ENGINE.md) لأن قاعدة عامة («لا رجوع للخلف») تفشل بصمت في الحالات الشاذّة، والحالة المستحيلة التي تمنعها بوابة الخروج هي بالضبط ما تُنتجه قاعدةٌ عامة تنسى استثناءً. **(3) `driver_public_id` مرجع opaque بـCHECK بلا FK** — ولذلك **تسبق Phase 06 مرحلةَ Driver Core (05) بلا انحراف عن الترتيب**: المحرّك يخزّن مرجعاً ولا يحكم على أهلية سائق، فلا حاجة لجدول سائقين موجود، والحدّ **مُختبَر لا موصوف** (حارس يقرأ `schema.sql` ويفشل إن ظهر FK عبر الخدمات). **(4) `ORD-` + عشرة أرقام من متتالية في القاعدة** لا UUID ظاهر ولا عدّاد تطبيقي: العميل يقرأ الرقم في محادثة دعم، والعدّاد التطبيقي يتصادم عند أول نسختين. **(5) خصوصية الأحداث بالمنطقة لا بالإحداثية**: لا حمولة حدث تحمل نقطة ولا نصّاً كتبه المستخدم — حارس صريح. و**المحرّك لا يُقرّر الإسناد**: لا مرشّحين ولا أمواج ولا مهل (Phase 07 §16)، يُسجّل فقط. و**بعد MR 1/6 صار كل هذا عقداً قابلاً للتحقّق لا نصّاً**: `schema.sql` بخمسة جداول (`orders` · `order_stops` · `order_status_history` · `order_assignments` · `order_outbox`) بقيود مُسمّاة تمنع بالبناء ما كان سيُترك للمراجعة البشرية (إسناد نشيط في حالة بحث · حالة مُسنَدة بلا إسناد · حالة نهائية بلا سبب · عرض عميل بلا مبلغ · حقول شحنة على رحلة · تسلسل تدقيق يتراجع) — و**تحقّقت الـDDL على Postgres 18.4 فعلياً**: الجداول الخمسة أُنشئت، والقيود رفضت كل حالة من الستّ، وأول إدخال صالح أنتج `ORD-0000000001`. و`api.openapi.yml` على المنفذ **8087** بسبعة مسارات، و`events.json` بأربعة أحداث في مغلّف واحد، و`errors.md` بـ**18 كود خطأ و24 سبباً** في كتالوج مُغلَق. و`@wasla/contracts-order` يُصدّر الأنواع المُكتبة و**108 اختباراً** تحرس الحدّ: منها **21 اختباراً تقرأ جدول الانتقالات من الوثيقة نفسها** وتفشل إن اختلف العدد المعلن عن الصفوف، أو ظهر انتقال ذاتي، أو خرج سهم من حالة نهائية، أو صارت حالة **غير قابلة للوصول** — والحالة غير القابلة للوصول *هي* حالة مستحيلة، فبوابة خروج المرحلة صارت مقيسة على العقد قبل أن يُكتب سطر منطق واحد. و**بعد MR 2/6 صار الجدول كوداً يُنفَّذ لا نصّاً يُقرأ، وصارت الوثيقة مُختبَرة من الطرفين**: مشروع العمل العشرون `@wasla/orders-service` بطبقة مجال كاملة **بلا بنية تحتية** — `domain/state-machine.ts` يحمل الاثنين والسبعين صفّاً صريحاً، كل صفّ يُسمّي المصدر والهدف والفاعل المتوقَّع والسبب النمطي، و**الاشتقاقات تُحسَب من الجدول لا تُعلَن**: الحالات النهائية مجموعةٌ مخارجها فارغة (وتُختبَر مطابقتها لـ`ORDER_TERMINAL_STATUSES` في العقد، فسهمٌ يُضاف خارج `expired` غداً يُسقط الاختبار بدل أن تبقى ثابتةٌ تكذب على الجدول)، و`is_terminal` في الحدث يُحسَب وقت النشر فلا يُكتَب يدوياً، و`assignmentRequirement` بثلاث درجات `{6 required · 4 forbidden · 11 optional}` مرآةً لقيد `ck_orders_assignment_matches_status` وكل حالة مُصنَّفة **مرّة واحدة** (مُختبَر). و**حارس المطابقة المزدوج يعمل فعلاً**: 28 اختباراً تقرأ [ORDER_ENGINE §4](../03-domain/ORDER_ENGINE.md) وتُقارنها بالكود **في الاتجاهين** صفّاً بصفّ وتتحقّق أن العدد المعلن في كل عنوان حالة يطابق صفوفه — فصفٌّ في الكود غير موثّق يُسقط المجموعة، وصفٌّ موثّق غير مُنفَّذ كذلك، ولا يمكن أن تفترق الوثيقة عن السلوك بمرور الوقت. و**المسح الحقيقي أُنجز الآن لا في MR 6/6**: `transition-order.test.ts` تُنشئ طلباً وتسوقه إلى كل حالة من الـ21 بأقصر مسار (BFS) ثمّ تُجرّب الانتقال إلى كل حالة من الـ21 — **441 حالة اختبار فعلية**، المسموح ينجح والباقي `ORDER_ILLEGAL_TRANSITION`. وخمسة قرارات في هذه الطبقة لكلٍّ منها نسخة أرخص وخاطئة: **ترتيب الفحص عقدٌ مُختبَر** (404 → 409 للجدول → 422 للسبب → 422 لشكل الفاعل → 422 لحرّاس الإسناد) لأن فحص السبب لو سبق الجدول لخرج للمنادي «سبب مفقود» على انتقالٍ ما كان ليُقبَل بأي سبب · **الفاعل: الشكل مُلزِم والهوية لا** — وهذا **مكتوب صراحةً في الكود** كي لا يُقرَأ عمود «الفاعل المتوقَّع» كضمانٍ غير موجود قبل وجود المصادقة · **القبول لا يُحرّك الحالة** لأن تحريكها في `resolveAssignment` كان سيُنتج تغييرَ حالةٍ واحداً لم يمرّ بالجدول ولم يُنتج صفّ تدقيق بترتيبه — أي ثغرة في الضمان كلّه · **لا فرع «تحرير» في حلّ العرض**: العرض يُحلّ مرّة واحدة، فسائقٌ يتراجع بعد القبول هو انتقالٌ إلى `driver_cancelled` لا صفٌّ يُعاد كتابته، ولفكّ الإسناد **مالك واحد** هو `transitionOrder` · و**البصمة تتجاهل `traceId` والمفتاح** فإعادة المحاولة بتتبّع جديد تبقى إعادة محاولة لا طلباً ثانياً. والاستلام ثلاث نتائج + رابعة (`ORDER_REQUEST_ALREADY_INGESTED`) لأن المفتاح يحمي من تكرار **النداء** أما `order_request_id` فيحمي من تسليم **الطلب** مرّتين عبر نداءين مختلفين. و**التدقيق والنشر متلازمان بالعدد لا بالنيّة**: صفوف التاريخ = الانتقالات + 1، وأحداث `status_changed` = صفوف التاريخ، والصفوف متلاصقة (`from_status` = `to_status` لما قبله)، و`from_status = null` مرّة واحدة في عمر الطلب. إجمالاً **558 اختباراً** لمحرّك الطلبات في ~1.4 ثانية بلا قاعدة ولا شبكة — و**خصائص [ORDER_ENGINE §5](../03-domain/ORDER_ENGINE.md) 1–11 كلّها مُثبَتة**، والخاصية 12 (تطابق المخزنين) محلّها MR 3/6 لأنها تحتاج مخزناً ثانياً. إجمالي المستودع **1293 اختباراً على 19 مشروع اختبار (20 مشروع عمل)**. **ما لم يُنجَز بقصد:** لا قاعدة ولا HTTP بعد فالخدمة **غير قابلة للتشغيل** · و**لا ذرّية بين المستودع والصادر** (منفذان بلا حدّ معاملة) — الكتابة الثلاثية في معاملة واحدة هي عمل MR 3/6 و**يجب أن تُنفَّذ بلا تغيير في `use-cases/`**، فأي اضطرار لذلك دليلٌ على أن المخطّط بدأ يقود المجال. | 2026-08-21 | [ADR-010](../15-decisions/ADR-010-order-engine-state-machine-and-assignment-boundary.md) + `services/orders/contracts/` (schema.sql بخمسة جداول ومتتالية ودالّة trigger + api.openapi.yml بسبعة مسارات على 8087 + events.json بأربعة أحداث + errors.md بـ18 كوداً و24 سبباً + README) + `@wasla/contracts-order` (**108 اختباراً**: 38 عقود · 31 حدود · 21 جدول انتقالات · 18 أحداث) + [ORDER_ENGINE.md](../03-domain/ORDER_ENGINE.md) (جدول الانتقالات الكامل: 72 زوجاً مسموحاً من 441) + [CONTAINERS §4.2](../02-architecture/CONTAINERS.md) + **MR 2/6 ([!39](https://gitlab.com/uxxxu/wasla/-/merge_requests/39): `@wasla/orders-service` — `src/domain/{state-machine,model,errors,validation,events}.ts` بجدول 72 انتقالاً صريحاً واشتقاقاته + `src/ports.ts` (خمسة منافذ في `OrderDependencies`) + أربع حالات استخدام (استلام · انتقال · إسناد · قراءة) + `mappers.ts` (المجال camelCase ⇄ العقد snake_case في موضع واحد) + `src/infrastructure/in-memory.ts` بمستودع يُطبّق قيود `schema.sql` بأسمائها · **558 اختباراً**: 441 مسح أزواج + 28 حارس مطابقة مزدوج + 27 استلام + 19 إسناد + 14 تخطيط + 11 قراءة وصادر — إجمالي المستودع 1293 اختباراً)** + [ORDER_CORE_DOMAIN.md](../02-architecture/ORDER_CORE_DOMAIN.md) + إجمالي المستودع **1293 اختباراً** على 19 مشروع اختبار (20 مشروع عمل) + **MR 3/6 ([!40](https://gitlab.com/uxxxu/wasla/-/merge_requests/40)): `infrastructure/drizzle/{schema,db,repository,transaction}.ts` — مرآة Drizzle للجداول الخمسة + `PostgresOrderRepository` و`PostgresOrderOutbox` و`PostgresOrderPublicIdGenerator` يقبلون `DbOrTx` + **`PostgresOrderUnitOfWork`** يُسدّ دَين الذرّية ([ADR-010 §127](../15-decisions/ADR-010-order-engine-state-machine-and-assignment-boundary.md)) فيفتح معاملة واحدة ويسلّم نفس المقبض للمستودع والصادر · 17 حراسة انحراف + **30 اختبار تكامل** (19 مستودع + 4 ذرّية + 7 مطابقة منافذ بصادر حقيقي) · إجمالي المستودع 1323 اختباراً · [ORDER_PERSISTENCE.md](../02-architecture/ORDER_PERSISTENCE.md)  + **MR 4/6 ([!41](https://gitlab.com/uxxxu/wasla/-/merge_requests/41)): `src/http/{requests,errors,app,server}.ts` + `src/runner.ts` + `infrastructure/drizzle/runner.ts` — المسارات السبعة على **8087** بلا كود خطأ جديد (الثمانية عشر كما هي) + **`OrderRunner` مقبس المعاملة**: المصنع يستقبل مُشغّلاً لا تبعيات، فكل كتابة داخل وحدة عمل واحدة ولا يستطيع معالج مسار أن ينسى المعاملة + **404 لا 403** لقراءة طلب عميل آخر لأن `order_public_id` تسلسلي فـ403 عرّافُ وجود + الاستلام **201** جديد و**200** إعادة تشغيل مفتاح + ترويسة مكرّرة تُرفض بدل تخمين القيمة + `x-request-id` → `trace_id` → صفّ التدقيق + كتالوجات تعدادات وقت التشغيل في `@wasla/contracts-order` (عضو مجهول يُرفَض 400 على الحدّ لا يُحمَل إلى الداخل) + **`assertNotes`** يُصلح تباعداً حقيقياً بين المحوّلين (قيد `notes` في القاعدة كان بلا مقابل في المجال فكان 503 حيث يجب 400) · **46 اختبار `app.inject`** — `@wasla/orders-service` **621 اختباراً** و`@wasla/contracts-order` **119** · تشغيل فعلي مُوثَّق الدليل ([ORDER_HTTP.md](../04-api/ORDER_HTTP.md))** | MR 5/6 — `HttpOrderIntakePort` في `services/customers` بدل `UnavailableOrderIntake` |
| 07 | Dispatch & Matching MVP | Not Started | Request كامل من Customer إلى Driver assignment في بيئة اختبار | 04, 05, 03, 02, 09, 11 | — | — | — | Candidate filtering + Ranking + Dispatch waves + Community fallback |
| 08 | Negotiation & Chat | Not Started | عميل وسائق يمكنهما التفاوض والتوافق على السعر وتسجيله في Order | 02, 03, 12, 09, 11 | — | — | — | Conversation + Countdown + Translation |
| 09 | Reputation + Fraud Foundation | Not Started | كل Completed Order ينتج Reputation events، والدعم يراجع التغير | 01, 04, 05, 08, 09, 11 | — | — | — | Ratings + Score + Fraud signals + Moderation |
| 10 | Driver Subscription & Referral | Not Started | Trial → Active → Expired → Community، والإحالات لا تكافئ النشاط الوهمي | 03, 01, 09, 12, 08, 11 | — | — | — | 250/250/400 plans + Tap + Referral attribution |
| 11 | Marketplace Foundation | Not Started | مستخدم قائم ينشئ Store من هويته الحالية، ويضيف منتج، ويطلب المراجعة | 06, 01, 07, 02, 03, 09, 11 | — | — | — | Store + Catalog + Inventory + Moderation |
| 12 | Marketplace Search | Not Started | منتج منشور يُعثر عليه بالعربي والإنجليزي + جاهز لإضافة اللغات | 06, 09, 12, 11 | — | — | — | Multilingual + Geo search + Ranking + Index rebuild |
| 13 | Store Orders + Delivery | Not Started | شراء → تجهيز → إسناد سائق → Pickup → Delivery → Completion | 06, 05, 04, 03, 02, 07 | — | — | — | Store order + Inventory reserve + Payment evidence |
| 14 | Partner / Enterprise | Not Started | Partner ينشئ طلبًا عبر Portal أو API وتتبع حالته | 07, 06, 05, 04, 12, 11 | — | — | — | Partner onboarding + Webhooks + Fleet + SLA + Multi-stop |
| 15 | Admin Operations | Not Started | الإدارة تشغّل الحالات اليومية دون تعديل DB يدويًا | 08, 01, 03, 04, 05, 06, 07, 11 | — | — | — | Moderation + Manual dispatch + Broadcast + Config UI |
| 16 | Support & Escalation | Not Started | نزاع كامل من العميل/السائق → Support → Escalation → Resolution → Reputation | 08, 02, 03, 11, 12 | — | — | — | Ticketing + Support/Escalation groups + Evidence |
| 17 | Billing & Store Platform Fees | Not Started | Billing كامل قابل للتدقيق ولا يختلط بأموال Trip Settlement | 09, 12, 07, 06, 08, 11 | — | — | — | Invoice + Subscription billing + Tap + Store fees |
| 18 | Observability & Resilience | Not Started | تعطيل خدمة ثانوية دون إسقاط Core، واستعادة من Backup وفق RTO/RPO | 10, 11, 09, 12 | — | — | — | OpenTelemetry + Circuit breakers + Dead-letter + Restore drill |
| 19 | Security Hardening | Not Started | لا ثغرات حرجة، الأسرار ليست في Git، Production access مضبوط | 11, 10, 01, 12 | — | — | — | Threat model + Pen test + Secret rotation + Audit integrity |
| 20 | Saudi Launch Readiness | Not Started | E2E pass + Load baseline + DR proof + Runbooks + Docs complete | جميع الفرق حسب المجال | — | — | — | Launch checklist + Rollback runbook + Incident runbook |
| 21 | Gulf / Egypt / Jordan Expansion | Not Started | Configuration لكل دولة دون تعديل Core Domain | جميع الفرق حسب المجال | — | — | — | Country/Currency/Language/Compliance/Pricing Config |
| 22 | Global Expansion | Not Started | Country Packs + adapters محلية | جميع الفرق حسب المجال | — | — | — | Local payment/KYC/Maps adapters + Timezone/Tax |
| 23 | Channel Independence | Not Started | Core يعمل عبر Telegram/Web/Mobile/Future | 12 + جميع الفرق | — | — | — | Web Adapter + Mobile Adapter دون إعادة كتابة Core |
| 24 | Service Extraction | Not Started | فصل Microservices عند سبب واضح + ADR | حسب الخدمة | — | — | — | Bounded Context + Contracts + Data Ownership قبل الاستخراج |

---

## Exit Gates التفصيلية للمراحل الحرجة

### Phase 00 — Repository Foundation (Exit Gate)

```text
CI passes
No secrets in repo
All teams can clone/build/test
Docs structure exists
Main branch protected
MR template active
```

**المخرجات:** Monorepo created، Docs skeleton created، CI pipeline skeleton، Protected branches، CODEOWNERS، Issue templates، MR template، Architecture ADR template، Migration convention، Testing convention.

### Phase 01 — Identity Foundation (Exit Gate)

إنشاء مستخدم من Telegram وبقاء هويته مستقرة عبر تغيير Username.

### Phase 03 — Telegram Channel Foundation (Exit Gate)

كل Bot يفتح Mini App المناسبة ويمكن استبدال Telegram adapter في الاختبارات بـMock Adapter.

كيف يُثبت آلياً (وفق [ADR-007](../15-decisions/ADR-007-telegram-channel-adapter-isolation-and-stack.md)): اختبار E2E (MR 7 من خطة المرحلة) يبني كل بوت بـ`MockChannelAdapter` بدلاً من `TelegramChannelAdapter` دون تعديل الـCore، ويؤكد أن كل بوت يُنتج زر Mini App الخاص به (`customer→customer` · `driver→driver` · `partner→partner`) وأن التحديث المكرر لا يُعالَج مرتين.

### Phase 06 — Order Engine (Exit Gate)

يمكن إنشاء Order وتغييره عبر الحالة دون حالات مستحيلة.

كيف يُثبت آلياً (وفق [ADR-010](../15-decisions/ADR-010-order-engine-state-machine-and-assignment-boundary.md)): «حالة مستحيلة» عبارة غامضة، فعُرِّفت **قابلة للقياس** في أربع صور تفشل إن ظهرت أيٌّ منها — انتقال خارج الجدول المنشور · سهم يخرج من حالة نهائية · حالة **لا يمكن الوصول إليها** من `published` · وصفٌّ في القاعدة يخالف قيداً مُسمّى. الصور الأربع مُختبَرة على ثلاث طبقات: **العقد** (21 اختباراً تقرأ الجدول من [ORDER_ENGINE.md §4](../03-domain/ORDER_ENGINE.md) — منجَز في MR 1/6؛ و28 اختباراً أخرى في طبقة المجال تقرأ الوثيقة نفسها من الطرفين) · **المجال** (**منجَز في MR 2/6**: `state-machine.ts` يُنتج الأزواج الاثنين والسبعين نفسها بحارس مزدوج، و**مسح الـ441 زوجاً صار اختبار وحدة** — 441 حالة اختبار فعلية في `transition-order.test.ts`) · و**القاعدة** (MR 3/6: القيود تُختبَر على Postgres حقيقي). وبوابة الخروج نفسها (MR 6/6) تمرّ برحلة طلب كاملة ثمّ **تحاول كل انتقال من الـ441 وتؤكّد أن غير المسموح يُرفَض بـ`ORDER_ILLEGAL_TRANSITION`** — لا عيّنة منها.

### Phase 07 — Dispatch & Matching MVP (Exit Gate)

Request كامل من Customer إلى Driver assignment في بيئة اختبار حقيقية.

### Phase 20 — Saudi Launch Readiness (Exit Gate)

```text
Identity, Customer, Driver, Order, Matching, Dispatch, Community fallback,
Reputation, Support, Admin, Marketplace basic, Subscriptions, Billing,
Monitoring, Backup, Security
```

لا يدخل النظام الإنتاج إلا بعد: E2E pass، Load baseline، Disaster recovery proof، Support runbook، Rollback runbook، Incident runbook، Documentation complete.

---

## روابط ذات صلة

- [README.md](../../README.md) — نظرة عامة ومراحل
- [/CONTRIBUTING.md](../../CONTRIBUTING.md) — سير العمل وExit Gates
- [/docs/00-rules/DEFINITION_OF_DONE.md](../00-rules/DEFINITION_OF_DONE.md) — DoR/DoD
- [/docs/15-decisions/](../15-decisions/) — سجل القرارات (ADR)
