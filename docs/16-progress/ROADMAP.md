# ROADMAP — خارطة طريق التنفيذ الزمنية (ملزمة)

> **النوع:** مصدر الحقيقة للتسلسل الزمني للتنفيذ (Binding Execution Roadmap).
>
> **القاعدة الملزمة:** هذا هو المسار الرسمي. أي شخص يعمل في المشروع — مطور أو وكيل آلي — يسير على هذه الوثيقة بترتيبها. لا تبدأ مرحلة قبل اجتياز **Exit Gate** للمرحلة السابقة.
>
> **العلاقة مع [MASTER_PROGRESS.md](MASTER_PROGRESS.md):** هذه الوثيقة = التسلسل والزمن. تلك = لوحة الحالة اللحظية فقط. لا تعارض بينهما؛ التقدم الفعلي يُسجّل هناك، والترتيب يُقرأ من هنا.
>
> **المرجع الأم:** أقسام 71–103 و162 من الدليل التنفيذي (مؤرشف في `docs/99-reference/`).
>
> **الحالة:** Baseline v1.0 · **آخر تحديث:** 2026-08-24 (**Phase 10 (Driver Subscription & Referral) = قيد التنفيذ 🔄 — MR 5/6 من ستّ مدفوعة**: **الخدمةُ صارت تتكلّم وتسمع** — ستّةُ أنواعِ أحداثٍ بمُغلَّفٍ واحدٍ تُكتب **صفّاً في صندوقِ الصادرِ في نفسِ معاملةِ التغيير** لا تُنشَر داخلَها، والضمانُ **مُعلَنٌ: مرّةً على الأقلّ** ومنعُ التكرارِ مسؤوليّةُ المستهلكِ بالعقد؛ ومخزنُ منعِ التكرارِ **بلا `UPDATE` أبداً** يُعيد **نفسَ البايتات**؛ و`ReputationFactConsumer` يُحوّل واقعةَ سمعةٍ إلى **أيّامٍ في الدفترِ لا رصيدٍ**، تبدأ من **نهايةِ تغطيةِ المُحيل** لا من الآن. **وعيبُ إنتاجٍ حقيقيٌّ كشفه اختبارُ التكاملِ وأُصلح: الصفُّ البائتُ يمنع حقّاً** — مَن دفع في يومِ تجربتِه الأوّلِ يبقى صفُّه `trial` وهو سارٍ بالاشتقاق، فكان تأهّلُ إحالتِه يُرفَض بـ`referrer_not_active` رفضاً كاذباً حتّى تُغلَق النافذة؛ والحلُّ اشتقاقُ الحالةِ **الآن** من الدفترِ في نفسِ المعاملة. و**الذرّيّةُ مُثبَتةٌ بمِسْبارٍ** لا موصوفةً · **205 سريعاً · 54 تكاملاً** محلّيّاً · وثيقةٌ جديدة [SUBSCRIPTION_EVENTS](../02-architecture/SUBSCRIPTION_EVENTS.md) · **المتبقّي 6/6 بوابةُ الخروج وإعادةُ تشغيلِ مسارِ HTTP المؤجَّلةُ إليها بقصد**) · سابقاً 2026-08-24 (Phase 10 · MR 4/6 — طبقةُ التطبيقِ والمعاملةُ وHTTP على 8093) · سابقاً 2026-08-23 (**Phase 09 (Reputation & Fraud) = Completed ✅ — ستّ مراجعات من ستّ · بوابة الخروج اجتازت**: **MR 6/6 — بوابة الخروج مُثبَتة لا موصوفة**: حزمة `packages/reputation-e2e` ترفع **محرّكَ الطلبِ الحقيقيَّ وخدمةَ السمعة** كمُستمعَينِ حقيقيَّين بساعتَينِ مكتوبتَين، فتسوق طلباً عبر HTTP إلى `completed` ثمّ تقرأ **صفَّ صادرِ المحرّك** فتأخذ الحمولةَ **التي أصدرها هو** وتُمرِّرها كما هي إلى `consumeSourceEvent`، ثمّ تقرأ النتيجةَ من `GET /reputation/scores/...` على المِقبض، ثمّ تُصرِّف صفَّ صادرِ السمعة إلى مصرفٍ مُسجِّل — **ولا حمولةَ حدثٍ واحدةً مكتوبةً بيد الاختبار**، وهو الفرقُ الذي لا تُثبته الـ282 اختباراً داخل الخدمة (تلك تُثبت أنّ الخدمةَ تفهم العقدَ كما فهمناه، لا أنّ العقدَ الذي فهمناه هو الذي يُصدره المحرّك). **اثنا عشر اختباراً**: حمولةُ المحرّكِ تحمل كلَّ حقلٍ يقرؤه المُستهلك (حارسُ انحرافٍ يُقاس على **الطرفين معاً**) · الإكمالُ واقعتان للعميلِ وللسائقِ بمُعرّفَيهما الفعليَّين · النتيجةُ المقروءةُ من المسارِ المُعلَن **نفسُ** ما قاله المُستهلك (`score_points` · `tier` · `fact_count` · `ruleset_version`) · **العميلُ 63 والسائقُ 64** بوزنَي `saudi-launch-v1` · `source_event_id` **يساوي `event_id` الذي أصدره المحرّك** (الشرطُ الملزمُ في HANDOFF §16 حرفيّاً) · **لا حدثٌ واحدٌ من دورةِ حياةٍ كاملةٍ يُرفَض** وما لا يعنينا يخرج `unsupported` بنوعِه واثنان فقط مقبولان (`order.created` · `order.assignment_offered`) فنوعٌ ثالثٌ يُسقط البوابةَ قصداً · `expired` تُتجاهَل بـ`status_owned_by_no_party` · إلغاءُ العميلِ **54** · قبولُ الإسناد **61** · و**الرفضُ حقٌّ لا مخالفة: صفرٌ مُعلَن والنتيجةُ تبقى 60** — «الخدمةُ لا تعاقب أحداً» مقيسةً برقم · وصفُّ الصادرِ يُسلَّم مرّةً واحدة (`published == claimed == delivered` و`failed == []`، وتصريفٌ ثانٍ يُطالِب بصفر) · وإعادةُ نفسِ الحدثِ **تكرارٌ مُسمّىً بلا صفِّ صادرٍ ثانٍ** — أثرُ `ux_reputation_facts_source` مقيساً عند المستهلك. و**البوابةُ لم تُظهر عطباً في الإنتاج**: 11 من 12 نجحت في أوّل تشغيل ولا حقلَ واحداً عُدِّل في `parseSourceEvent` ولا في `translate.ts` — نتيجةٌ موجبةٌ تُسجَّل لحرّاسِ الانحرافِ الأحدَ عشرَ في MR 5/6 لأنّهم قرؤوا العقدَ من القرص. والتوكيدُ الوحيدُ الذي سقط أوّلاً كان عدداً كلّيّاً لصفوفِ الصادر، **والإصلاحُ في التوكيدِ لا في الكود** (`tier_changed` تُصدَر حين تتغيّر الفئةُ فقط). **ولا سطرَ إنتاجٍ واحدٍ تغيّر** في `services/reputation` ولا في `services/orders`؛ والتغييرُ الوحيدُ خارج الحزمة مَخرجان فرعيّان في `package.json` (`./http` · `./runner`) لأنّ حرسَ النقاءِ يمنع كلمةَ `runner` في `index.ts`، **فبقي الحدُّ كما هو ولم يُثقَب استثناؤه**. المقيس محلّيّاً: **12/12** للحزمة، والمستودعُ كاملاً **2856 ناجحاً · 0 فاشل** و`pnpm -r typecheck` نظيفٌ على **35 مشروعاً** وحرّاسُ الخدمة الأربعة **76/76** — والتفصيل في [PHASE09_EXIT_GATE_E2E.md](../12-testing/PHASE09_EXIT_GATE_E2E.md). **الدَّينُ المُعلَنُ المنقولُ من المرحلة:** لا ناقلَ حقيقيّاً بين الخدمتين (المُشترِكُ والمُجدوِل، وقرارُ الناقلِ من Phase 11) · و`PostgresOutboxDrainStore`/`PostgresOutboxDrainRunner` **مكتوبان ومُنمَّطان وغيرُ مقيسَين على قاعدةٍ حقيقيّة** (ومحلُّ قياسِهما حزمةُ تكاملِ الخدمة بمتغيّر `DATABASE_URL`) · و`FOR UPDATE SKIP LOCKED` لا يُقاس بعاملَين متزامنَين على الذاكرة · ولا وظيفةَ CI جديدة لأنّ البوابةَ بلا وضعِ Postgres. **والتالي: Phase 10 — Driver Subscription & Referral**، وشروطُها في [HANDOFF §17](HANDOFF_NEXT_STEPS.md). وقبلها **MR 5/6 — مستهلكُ حدثَي محرّك الطلب وناشرُ صندوق الصادر** ([!68](https://gitlab.com/uxxxu/wasla/-/merge_requests/68)): `parseSourceEvent` يقرأ نوعَ الحدثِ **أوّلاً** قبل أيِّ حقلٍ آخر فحدثٌ لا يعنينا يخرج `unsupported` بلا أن نتحقّق من حمولةٍ لا نملك عقدَها (ولو انعكس الترتيبُ لصار كلُّ حدثٍ في المنصّة يُرفَض من السمعة بـ400)، و`source_event_id` **يُحرَس كـUUID على الحدّ** لا في القاعدة؛ و`translateSourceEvent` **دالّةٌ نقيّة** تُنتج مسوّداتٍ وأسباباً مُسمّاةً للتجاهل (`status_not_reputable` · `status_owned_by_no_party` · `assignment_cancelled_by_system` · `driver_absent_in_payload`) — والإكمالُ **واقعتان لا واحدة** لأنّ العميلَ أكمل والسائقَ أكمل، وإلغاءُ سائقٍ بلا مُعرّفِ سائقٍ **لا يُخمَّن ولا يُسجَّل على العميل** (واقعةٌ تُنسَب إلى غير فاعلها أسوأُ من واقعةٍ لم تُسجَّل)، و`assignment_rejected` **بوزنٍ صفرٍ مُعلَن**؛ و`consumeSourceEvent` عقدُه سطرٌ واحد: **رجوعٌ عاديٌّ يعني «استُهلك» ورفعُ خطأٍ يعني «أعِد التسليم»** — فلا دالّةَ `requiresRedelivery` تُسأل بعد الرجوع، والرفضُ المُسمّى (400/409/422) يُجمَع في `outcomes` ولا يُعاد تسليمُه (إعادةُ واقعةٍ متأخّرةٍ ستتأخّر دائماً)، وما ليس مُسمّىً **يُرفَع كما هو**؛ و`EventSinkPort` **منفذٌ** لا `fetch` (فحرسُ النقاءِ لم يحتج تعديلاً ولا استثناءً)، و`drainOutbox` يُطالِب ثمّ يُسلّم ثمّ يُعلّم بحدٍّ إلزاميّ، والفشلُ **لا يُعلّم الصفَّ** فيُعاد في التصريفِ التالي بلا نشرتَين لصفٍّ واحد. و**57 اختباراً جديداً** (11 حارس انحرافٍ للحمولة + 22 للترجمة + 10 للمُستهلك + 14 للتصريف) ⇒ **الخدمة 282 اختباراً في 15 ملفّاً**. وقبلها **MR 4/6 — طبقة HTTP على المنفذ 8092**: `createReputationApp` يخدم **تسعة مسارات (إحدى عشرة عمليّة)** بينها `/health` بحالتيه (`postgres` ⇒ `ok` · `memory` ⇒ `degraded`) و`last_tick_at`، فوق **`ReputationRunner` وحده** فلا معالجُ مسارٍ يرى `deps` ولا يستطيع فتحَ معاملة، و**لا `try/catch` في أيّ معالج** بل مُترجِمُ خطأٍ واحد؛ و**الدَّين المُعلَن (د) مسدود**: `response_status`/`response_body` صارا **الجوابَ المحفوظَ فعلاً** فإعادةُ المفتاح تُعيد **نفس البايتات** بـ`200` و`duplicate: true` — لا `409` ولا جسماً مُعاد بناؤه من حالةٍ تحرّكت بعده؛ و`onlyKeys()` على كلّ حمولة و**كتالوجاتُ تعدادات وقتِ تشغيل** بلا `as` للتحقّق؛ و**لا `502` في أيّ فرع**، و`500` **انحرافٌ مُعلَن** لقيدِ تماسكٍ ملفوفٍ في `cause` برمزٍ `REPUTATION_INTERNAL_ERROR` **غائبٍ عن الكتالوج قصداً ومحروسٍ بغيابه** (الفرقُ المحميّ: `503` «لم نعرف شيئاً فأعد» مقابل `500` «كُسر شيءٌ عندنا فلا تُعد»)؛ و`http/server.ts` **الموضعُ الوحيد** الذي يقرأ `process.env` و`infrastructure/runtime.ts` **الموضعُ الوحيد** الذي يقرأ ساعةَ النظام، ولكلٍّ منهما **اختبارٌ موجَبٌ يُحسب من المصدر** فلا تتّسع قائمةُ الاستثناء بالإهمال (توسيعُ نطاقٍ لا تضييقُ حدّ). و**57 اختباراً جديداً** (44 على `app.inject` + **13 حارس انحراف** يقرأ العقدَ من القرص ويقابله بما سجّله Fastify **في الاتجاهين** وبقوائم مفاتيح الأجسام والمُرشِّحات وبالحالات المنشورة) ⇒ **الخدمة 225 اختباراً في 11 ملفّاً**، والمستودع كاملاً **2787 ناجحاً + 1 متروكاً بقصد · 0 فاشل** و`pnpm -r typecheck` نظيف على **34 مشروعاً** — والتفصيل في [REPUTATION_HTTP.md](../04-api/REPUTATION_HTTP.md). و`services/reputation/contracts/*` **لم تُلمَس**، ولا حالةَ استخدامٍ عُدِّلت. وقبلها: العقود الكنسيّة وحزمة الأنواع وADR-014 في 1/6، ثمّ **طبقة المجال النقيّة** `@wasla/reputation-service` في 2/6 (بلا قاعدةٍ ولا HTTP ولا ساعةِ نظام)، ثمّ **الاستمراريّة على Postgres ووحدةُ العمل ومطابقةُ المُهيئَين** في 3/6 (**52 اختبار تكامل على PostgreSQL 18.4 حقيقيّ · 0 فاشل**، و**ثلاثةُ فروقٍ حقيقيّةٍ بين مُهيئَي الذاكرة وPostgres كُشفت وأُصلحت** — أظهرُها `traceId` غائبٌ من صفّ التقييم في الذاكرة و`null` منه في Postgres · و**حدُّ المعاملة في ملفٍّ واحد** فلا شيءَ غيره يفتحها · ونسخةُ القواعد مبذورةٌ **من الترحيل** لا من الكود · وترحيلٌ عكوس) — و**لا HTTP بعد: تلك 4/6** ([REPUTATION_PERSISTENCE.md](../02-architecture/REPUTATION_PERSISTENCE.md)). **القاعدة الحاكمة:** السمعة **نتيجةٌ مُشتقّة** من دفتر وقائع append-only، والاحتيال **إشاراتٌ مُسمّاة لا حُكم**، و**الخدمة لا تعاقب أحداً** — تُعلن حقائق ويملك أثرَها غيرُها. مصدرُ الحقيقة **حدثان منشوران** من محرّك الطلب (`order.status_changed` · `order.assignment_resolved`) لا استعلامٌ دوريّ ولا قراءةُ قاعدة، والتسليم at-least-once فالتفرّد **قيدٌ في القاعدة** (`ux_reputation_facts_source`) لا فحصٌ في الكود، و**حذفُ جدول النتائج بالكامل عملٌ بلا خسارة** لأنّ `recompute` يُعيده من الدفتر. **انحرافٌ موثّق في العدد لا في الموضع:** خدمةٌ واحدة `services/reputation` تحمل السمعة والاحتيال معاً، و`services/fraud/` تبقى **فارغة بقرار** ([ADR-014](../15-decisions/ADR-014-reputation-derived-scores-and-fact-sourced-fraud-signals.md) القرار 1) — لأنّ قواعد الاحتيال تقرأ **نفس دفتر الوقائع**، وخدمةٌ ثانية تعني نسخةً ثانية من الوقائع تتباعد بصمت. المُسلَّم: [ADR-014](../15-decisions/ADR-014-reputation-derived-scores-and-fact-sourced-fraud-signals.md) بتسعة قرارات · عقود `services/reputation/contracts/*` (**تسعة جداول** بقيودٍ مُسمّاة · **تسعة مسارات** على المنفذ **8092** · **خمسة أحداث** · **17 كود خطأ**) · حزمة `@wasla/contracts-reputation` بـ**81 حارس انحراف** · [نموذج المجال](../03-domain/REPUTATION_FRAUD.md) · [CONTAINERS §4.6](../02-architecture/CONTAINERS.md). وسبعة حدود يفرضها **حارس اختبار لا مراجعة بشرية**: الواقعة تُخزَّن والنقطة تُشتقّ (`setScore` · `adjustScore` · `setTier` ممنوعة) · الأرقام **بياناتٌ لا كود** في نسخةٍ مجمّدة مُرقّمة (`saudi-launch-v1`) وكل نتيجةٍ تحمل `ruleset_version` ولا `NUMERIC` ولا عائم · لا عمود `is_suspended` ولا `is_fraudster` ولا حدث `reputation.subject_suspended` (الإيقاف ملكُ `services/drivers` والقرار الإداريّ ملكُ Phase 15) · التقييم **درجةٌ ورمزُ سبب** بلا عمود نصٍّ حرّ أصلاً (سابقة `shipment_description` في ADR-009 §7) ولا ذاتيّاً ولا من نفس الجانب · الإشارة بلا `state` وبلا `probability`/`confidence`/`model_version`، وواحدةٌ لكل (قاعدة × شخص × نافذة) · والزمن **نبضة لا مؤقّت** (`POST /reputation/tick` وحده) والاستحقاق مُخزَّن ومفهرس. المقيس محلّيّاً: **81 اختباراً في أربعة ملفات · 0 فاشل** لحزمة `@wasla/contracts-reputation` (مخطّط 24 · أحداث 20 · حدود 19 · عقود 18) تقرأ العقود **من القرص** لا نسخةً منها في الكود، و`pnpm -r typecheck` نظيف على 32 مشروعاً. **وقد نُفِّذت ثمّ اجتازت البوابةُ بعدها — وكانت MR 5/6: مستهلكُ حدثَي محرّك الطلب (`order.status_changed` · `order.assignment_resolved`) وناشرُ `reputation_outbox`** — و`source_event_id` **يجب أن يكون UUID الحدث** لا مُعرّفاً مُركّباً، والتفصيل الملزم في [HANDOFF §16](HANDOFF_NEXT_STEPS.md). وسابقاً في اليوم نفسه **Phase 08 (Negotiation & Chat) = Completed ✅ — MR 6/6 من ستّ مدفوعة** · **بوابة الخروج اجتازت**: حزمة `packages/negotiation-e2e` تُشغّل **سبع خدمات مُنصتة** بساعة واحدة محقونة (هوية · جغرافيا · عملاء · طلبات · مطابقة · توزيع · تفاوض)، ومنفذا التفاوض الخارجان يُبنيان بـ`configuredDispatchOffers` و`configuredAgreedPrice` — الدالّتان نفسهما التي يناديهما `src/http/server.ts` — فما يُثبَت هو التركيب المنشور لا تركيبٌ كُتب للاختبار. **ثمانية اختبارات، والأربعة الملزمة فيها**: مسار سعيد (نيّة عميل ⇒ طلب `negotiable` بلا سعر ⇒ موجة توزيع حقيقية ⇒ خيط على عرضها ⇒ جولة ⇒ قبول ⇒ `GET /orders/lookup` يحمل المبلغ و`agreed_negotiation_id`، **وحالة الطلب لم تتغيّر** لأنّ تسجيل السعر ليس انتقالاً)، وإعادةٌ بالمفتاح نفسه (201 ثمّ 200، جسمٌ متطابق حرفياً ومحاولة تسليم **واحدة**، ونبضةٌ بعدها لا تُعيد التسليم)، ورفضٌ نهائي من المحرّك (عرضان حيّان على طلب واحد ⇒ اتفاقان ⇒ الأوّل `handed_off` والثاني `rejected` بـ`ORDER_AGREED_PRICE_ALREADY_SET` **مع بقاء الاتفاق صحيحاً والقبول 201 ولا `502` في أي موضع** — ADR-013 القرار 2)، ومحرّكٌ غير قابل للوصول (يُغلق مُستمعه فعلاً ⇒ القبول 201 و`pending` و`next_handoff_at` = الآن + **30s** بالثانية، ونبضةٌ قبل اللحظة لا تفعل شيئاً، ثمّ يرجع المحرّك على **المنفذ نفسه** وتتقدّم الساعة فنبضةٌ واحدة تُسلّم السعر). **ولا قراءة من مخزن ولا صفٌّ مبنيٌّ بيد الاختبار ولا `sleep`.** والبوابة **أظهرت** أنّ الحالة الملزمة الثالثة كما وُصفت في HANDOFF غير قابلة للوصول عبر HTTP العام (`openThread` يرفض أوّلاً بـ422 على طلبٍ غير قابل للتفاوض)، فأُعيد تصميمها إلى الطريق الذي يقع فعلاً، وصار الوصف الأصلي حرزاً خامساً يمنع نسخ `negotiable` بدل قراءته من `price_mode` عند المحرّك. المقيس محلياً: **8/8 على الذاكرة و8/8 على Postgres 17 حقيقي**، ووظيفة `negotiations-exit-gate-e2e` بمتغيّر `NEGOTIATION_DATABASE_URL` وقاعدة `wasla_negotiation_e2e`، وإجمالي المستودع **2481 اختباراً + 1 متروك بقصد في 150 ملفاً · 0 فاشل** و`pnpm -r typecheck` نظيف على 32 مشروعاً — والتفصيل في [PHASE08_EXIT_GATE_E2E.md](../12-testing/PHASE08_EXIT_GATE_E2E.md). **والتالي: Phase 09 — Reputation + Fraud.** سابقاً في المرحلة نفسها **MR 5/6**: **المنافذ الصادرة الحقيقية موصولة، والدَّين المُعلَن على محرّك الطلب مسدود** — `orders` كسب أعمدة الاتفاق الأربعة (`agreed_amount_minor`, `agreed_currency`, `agreed_at`, `agreed_negotiation_id`) بثلاثة قيود DDL تجعل الحقيقة غير قابلة للتشويه (الرباعيّة كاملةٌ أو غائبة · لا سعر متَّفق عليه إلّا على `price_mode='negotiable'` · وخيطُ تفاوضٍ واحد لا يُسجّل سعرَين)، و**`POST /orders/agreed-prices`** مُفتاحُه إلزاميّ ويقرأ الطلب بمعرّفه العام لا بـUUID (لأنّ التفاوض لا يملك UUID الطلب أصلاً) و**لا يُغيّر حالة الطلب ولا يُصدر حدثاً جديداً**، و**`GET /orders/lookup`** يُفشي وضعَ السعر وحده لا بيانات العميل؛ و**`GET /dispatch/offers/{offer_id}`** يُضيف `standing` **قراراً في مجال التوزيع لا مقارنةً بساعة الحائط**؛ وخدمة التفاوض تستبدل `Unconfigured*` بمحوّلَين حقيقيَّين **بلا لمس `src/use-cases/` ولا `src/domain/` ولا `src/ports.ts`**، يحرسان الفرق الذي هو كلّ شيء: **`null` ⇒ 422 (حقيقةٌ عرفناها) مقابل رمي ⇒ 503 (لم نعرف شيئاً)**، و**مفتاح تفرّدٍ ثابت `negotiation-{threadId}` لا يدخله رقم المحاولة** (وإلّا سُجّل سعرٌ مرّتين عند أوّل مهلةٍ كاذبة)، و**404 على التسليم تُرمى ولا تُقرأ رفضاً**، و**لا رمز منشور لفشل التسليم** فيبقى 201 ومعه اتفاق (ADR-013 · القرار 2)؛ وبوتا العميل والسائق كسبا `/negotiations` و`/accept` و`/reject` **بلا عرضٍ مضاد من المحادثة** (حدث المحادثة لا يحمل نصّاً حرّاً، والعمل الثقيل في Mini App) وبقاعدة لبسٍ صريحة: لا فعل إلّا إذا كان الخيط واحداً بدورٍ معلّق من الطرف الآخر؛ و**69 اختباراً جديداً** (21 للمنافذ الصادرة + 12 للعميل + 12 للسائق + 24 لمحرّك الطلب والتوزيع) ⇒ المستودع **2473 اختباراً + 1 متروك بقصد في 149 ملفاً** و`pnpm -r typecheck` نظيف على **31 مشروعاً**، و**اختبارات التكامل شُغّلت على Postgres حقيقي محلّيّاً قبل الدمج** (تفاوض 62 · طلبات 32 · توزيع 48) بوصفةٍ بلا root في [LOCAL_POSTGRES_FOR_TESTS.md](../14-runbooks/LOCAL_POSTGRES_FOR_TESTS.md) — والتفصيل في [NEGOTIATION_HTTP.md §12](../04-api/NEGOTIATION_HTTP.md). وقبلها **MR 4/6**: **طبقة HTTP لخدمة التفاوض على المنفذ 8091** — `createNegotiationApp` يخدم **عشرة مسارات (ثلاث عشرة عمليّة) و`/health`** بمقبس معاملة واحد (`NegotiationRunner {write, read}`) فلا معالج مسار يستطيع فتح معاملة، و**لا `try/catch` في أيّ معالج** بل معالج خطأ واحد؛ والإعادة تُحسم في المجال بـ`guardIdempotency` وHTTP يترجم `replay` إلى **200 بدل 201** فقط؛ والقبول يعيد **الاتفاق** والرفض يعيد **الخيط**؛ و**فشل تسليم السعر يبقى 201 ومعه اتفاق كامل** بحالة `pending` وموعد إعادة — لا 502 ولا صنف `bad_gateway` في هذه الخدمة أصلاً (ADR-013 · القرار 2)؛ و**`NEGOTIATION_INTERNAL_ERROR` و`500` غائبان عن الكتالوج قصداً ومحروسان** (قيدُ تماسكٍ ملفوفٌ في `cause` يُشخَّص بـ`details.constraint` ولا يُنشَر كحالةٍ يتعاقد عليها مستهلك)؛ و**50 اختباراً جديداً** (36 على `app.inject` + **14 حارس انحراف** يقرأ العقد من القرص ويقابله بما سجّله Fastify فعلاً في الاتجاهين، وبقوائم مفاتيح الطلب، وبقائمة الحالات المنشورة) ⇒ **208 اختباراً للخدمة في 12 ملفاً**، وإجمالي المستودع **2405 اختباراً + 1 متروك بقصد في 145 ملفاً** و`pnpm -r typecheck` نظيف على 31 مشروعاً — والتفصيل في [NEGOTIATION_HTTP.md](../04-api/NEGOTIATION_HTTP.md). وقبلها **MR 3/6**: استمرارية Drizzle/Postgres لخدمة التفاوض — مرآة مخطّط لجداول العقد الثمانية، وثمانية مستودعات، و`PostgresNegotiationUnitOfWork` بمعاملة واحدة لكل عمليّة تطبيقيّة **بلا تعديل سطرٍ واحد في `src/use-cases/` ولا `src/domain/` ولا `src/ports.ts`**؛ و**62 اختبار تكامل على Postgres حقيقي** (41 مستودعاً وقيوداً + 10 مطابقة منافذ ذاكرة↔Postgres + 11 ذرّية) و**27 حارس انحراف مرآة↔DDL**، ووظيفة CI `negotiations-db-integration` — والتفصيل في [NEGOTIATION_PERSISTENCE.md](../02-architecture/NEGOTIATION_PERSISTENCE.md). وقبلها **MR 2/6**: طبقة المجال النقيّة `@wasla/negotiations-service` — آلتا حالة الخيط والدور، والسياسة المُقفلة `saudi-launch-v1`، وثمانية أفعال (فتح · اقتراح · قبول · رفض · رسالة · إلغاء · قراءة · نبضة)، ومُهيّئ ذاكرة يُطبّق **24 قاعدة مُسمّاة بأسمائها الحرفيّة**؛ و**131 اختباراً بلا قاعدة ولا شبكة ولا `sleep`** كشفت ثلاثة عيوب حقيقيّة فأُصلحت في نفس المراجعة (حرس التكرار كان يعمل بعد تحقّق الحالة فيُجاب على إعادة محاولةٍ ناجحة بـ409 · الإلغاء كان يترك دوراً معلّقاً على خيطٍ منتهٍ · قيدان يُرفعان باسمٍ لا يميّزهما) — التفصيل في [NEGOTIATION_CORE_DOMAIN.md](../03-domain/NEGOTIATION_CORE_DOMAIN.md). إجمالي المستودع **2328 اختباراً + 1 متروك بقصد في 142 ملفاً**. وقبلها **MR 1/6 ([!57](https://gitlab.com/uxxxu/wasla/-/merge_requests/57))**: [ADR-013](../15-decisions/ADR-013-negotiation-chat-agreement-boundary-and-tick-driven-expiry.md) والعقود الكنسية لخدمة `services/negotiations` على **8091** (ثمانية جداول بـ22 قيداً مُسمّى · عشرة مسارات · تسعة أحداث · 29 كود خطأ) و`@wasla/contracts-negotiation` بـ**70 حارس انحراف** تقرأ العقد من القرص، و[نموذج المجال](../03-domain/NEGOTIATION_CHAT.md) و[CONTAINERS §4.5](../02-architecture/CONTAINERS.md). القاعدة الحاكمة: **يملك التفاوض «بكم اتّفقنا» ولا يكتب السعر في `orders`** — يُسلَّم عبر `AgreedPricePort` وحده و**فشل التسليم لا يُبطل الاتفاق**، والاتفاق **قبولٌ صريح لدورٍ برقمه** بقيدٍ في القاعدة يمنع من اقترح أن يقبل، والزمن **نبضة لا مؤقّت** والانتهاء يُقاس **أيضاً عند كل فعل**، والأحداث لا تحمل نصّ المحادثة. إجمالي المستودع **2197 اختباراً + 1 متروك بقصد في 133 ملفاً**. **الدَّين المُعلَن:** أعمدة الاتفاق في `orders` مالكها محرّك الطلب في MR 5/6. والخطّة الكاملة لما تمّ وما بقي في [HANDOFF §14](HANDOFF_NEXT_STEPS.md)) · سابقاً 2026-08-22 (**Phase 05 = Completed** — بوابة الخروج اجتازت في MR 6/6: سبع خدمات مُنصتة بساعة واحدة، وأهليّة `driver_core` محسوبة تُخرج السائق **بنبضة واحدة**؛ والتالي **Phase 08**. سابقاً: المنافذ الصادرة الحقيقية إلى المطابقة 8088 والجغرافيا 8081، و**تقاعُد 502**، وواجهة بوت السائق · وفيها إصلاح عيبٍ كان يُسقط كتابةً محليّة ناجحة عند عطب المطابقة، وعيبِ مُشغِّلٍ كان يُلغي تدفقات بوت العميل المنشور) · سابقاً 2026-08-22 (Phase 05 · MR 4/6 — طبقة HTTP لخدمة السائقين على 8090 بثلاثة عشر مساراً و64 اختبار HTTP · سابقاً MR 3/6 — استمرارية السائق على Drizzle/Postgres بوحدة عمل واحدة، **بلا تغيير في `src/use-cases/`**) · سابقاً 2026-08-22 (Phase 05 · MR 2/6 — طبقة مجال السائق النقيّة وحاسب الأهليّة، بقاعدة **«كلّ الأسباب لا أوّلها»**) · سابقاً 2026-08-22 (Phase 05 · MR 1/6 — العقود وADR-012) · سابقاً 2026-08-21 (Phase 06 · MR 6/6 — **المرحلة مكتملة** · البوابة أسقطت عيب ربط الإسناد فصُحّح) · سابقاً 2026-08-21 (Phase 06 · MR 5/6 ثم MR 4/6 ثم MR 3/6) · سابقاً 2026-08-21 (Phase 06 · MR 2/6) · سابقاً 2026-08-20 (بعد دمج [MR !1](https://gitlab.com/uxxxu/wasla/-/merge_requests/1) و[MR !2](https://gitlab.com/uxxxu/wasla/-/merge_requests/2))
>
> **ملاحظة الحالة (2026-08-23، محدّث):** **Phase 09 (Reputation & Fraud) = Completed ✅ — ستّ مراجعات من ستّ · بوابة الخروج اجتازت**: **MR 6/6 — بوابة الخروج مُثبَتة لا موصوفة**: حزمة `packages/reputation-e2e` ترفع **محرّكَ الطلبِ الحقيقيَّ وخدمةَ السمعة** كمُستمعَينِ حقيقيَّين بساعتَينِ مكتوبتَين، فتسوق طلباً عبر HTTP إلى `completed` ثمّ تقرأ **صفَّ صادرِ المحرّك** فتأخذ الحمولةَ **التي أصدرها هو** وتُمرِّرها كما هي إلى `consumeSourceEvent`، ثمّ تقرأ النتيجةَ من `GET /reputation/scores/...` على المِقبض، ثمّ تُصرِّف صفَّ صادرِ السمعة إلى مصرفٍ مُسجِّل — **ولا حمولةَ حدثٍ واحدةً مكتوبةً بيد الاختبار**، وهو الفرقُ الذي لا تُثبته الـ282 اختباراً داخل الخدمة (تلك تُثبت أنّ الخدمةَ تفهم العقدَ كما فهمناه، لا أنّ العقدَ الذي فهمناه هو الذي يُصدره المحرّك). **اثنا عشر اختباراً**: حمولةُ المحرّكِ تحمل كلَّ حقلٍ يقرؤه المُستهلك (حارسُ انحرافٍ يُقاس على **الطرفين معاً**) · الإكمالُ واقعتان للعميلِ وللسائقِ بمُعرّفَيهما الفعليَّين · النتيجةُ المقروءةُ من المسارِ المُعلَن **نفسُ** ما قاله المُستهلك (`score_points` · `tier` · `fact_count` · `ruleset_version`) · **العميلُ 63 والسائقُ 64** بوزنَي `saudi-launch-v1` · `source_event_id` **يساوي `event_id` الذي أصدره المحرّك** (الشرطُ الملزمُ في HANDOFF §16 حرفيّاً) · **لا حدثٌ واحدٌ من دورةِ حياةٍ كاملةٍ يُرفَض** وما لا يعنينا يخرج `unsupported` بنوعِه واثنان فقط مقبولان (`order.created` · `order.assignment_offered`) فنوعٌ ثالثٌ يُسقط البوابةَ قصداً · `expired` تُتجاهَل بـ`status_owned_by_no_party` · إلغاءُ العميلِ **54** · قبولُ الإسناد **61** · و**الرفضُ حقٌّ لا مخالفة: صفرٌ مُعلَن والنتيجةُ تبقى 60** — «الخدمةُ لا تعاقب أحداً» مقيسةً برقم · وصفُّ الصادرِ يُسلَّم مرّةً واحدة (`published == claimed == delivered` و`failed == []`، وتصريفٌ ثانٍ يُطالِب بصفر) · وإعادةُ نفسِ الحدثِ **تكرارٌ مُسمّىً بلا صفِّ صادرٍ ثانٍ** — أثرُ `ux_reputation_facts_source` مقيساً عند المستهلك. و**البوابةُ لم تُظهر عطباً في الإنتاج**: 11 من 12 نجحت في أوّل تشغيل ولا حقلَ واحداً عُدِّل في `parseSourceEvent` ولا في `translate.ts` — نتيجةٌ موجبةٌ تُسجَّل لحرّاسِ الانحرافِ الأحدَ عشرَ في MR 5/6 لأنّهم قرؤوا العقدَ من القرص. والتوكيدُ الوحيدُ الذي سقط أوّلاً كان عدداً كلّيّاً لصفوفِ الصادر، **والإصلاحُ في التوكيدِ لا في الكود** (`tier_changed` تُصدَر حين تتغيّر الفئةُ فقط). **ولا سطرَ إنتاجٍ واحدٍ تغيّر** في `services/reputation` ولا في `services/orders`؛ والتغييرُ الوحيدُ خارج الحزمة مَخرجان فرعيّان في `package.json` (`./http` · `./runner`) لأنّ حرسَ النقاءِ يمنع كلمةَ `runner` في `index.ts`، **فبقي الحدُّ كما هو ولم يُثقَب استثناؤه**. المقيس محلّيّاً: **12/12** للحزمة، والمستودعُ كاملاً **2856 ناجحاً · 0 فاشل** و`pnpm -r typecheck` نظيفٌ على **35 مشروعاً** وحرّاسُ الخدمة الأربعة **76/76** — والتفصيل في [PHASE09_EXIT_GATE_E2E.md](../12-testing/PHASE09_EXIT_GATE_E2E.md). **الدَّينُ المُعلَنُ المنقولُ من المرحلة:** لا ناقلَ حقيقيّاً بين الخدمتين (المُشترِكُ والمُجدوِل، وقرارُ الناقلِ من Phase 11) · و`PostgresOutboxDrainStore`/`PostgresOutboxDrainRunner` **مكتوبان ومُنمَّطان وغيرُ مقيسَين على قاعدةٍ حقيقيّة** (ومحلُّ قياسِهما حزمةُ تكاملِ الخدمة بمتغيّر `DATABASE_URL`) · و`FOR UPDATE SKIP LOCKED` لا يُقاس بعاملَين متزامنَين على الذاكرة · ولا وظيفةَ CI جديدة لأنّ البوابةَ بلا وضعِ Postgres. **والتالي: Phase 10 — Driver Subscription & Referral**، وشروطُها في [HANDOFF §17](HANDOFF_NEXT_STEPS.md). وقبلها **MR 5/6 — مستهلكُ حدثَي محرّك الطلب وناشرُ صندوق الصادر** ([!68](https://gitlab.com/uxxxu/wasla/-/merge_requests/68)): `parseSourceEvent` يقرأ نوعَ الحدثِ **أوّلاً** قبل أيِّ حقلٍ آخر فحدثٌ لا يعنينا يخرج `unsupported` بلا أن نتحقّق من حمولةٍ لا نملك عقدَها (ولو انعكس الترتيبُ لصار كلُّ حدثٍ في المنصّة يُرفَض من السمعة بـ400)، و`source_event_id` **يُحرَس كـUUID على الحدّ** لا في القاعدة؛ و`translateSourceEvent` **دالّةٌ نقيّة** تُنتج مسوّداتٍ وأسباباً مُسمّاةً للتجاهل (`status_not_reputable` · `status_owned_by_no_party` · `assignment_cancelled_by_system` · `driver_absent_in_payload`) — والإكمالُ **واقعتان لا واحدة** لأنّ العميلَ أكمل والسائقَ أكمل، وإلغاءُ سائقٍ بلا مُعرّفِ سائقٍ **لا يُخمَّن ولا يُسجَّل على العميل** (واقعةٌ تُنسَب إلى غير فاعلها أسوأُ من واقعةٍ لم تُسجَّل)، و`assignment_rejected` **بوزنٍ صفرٍ مُعلَن**؛ و`consumeSourceEvent` عقدُه سطرٌ واحد: **رجوعٌ عاديٌّ يعني «استُهلك» ورفعُ خطأٍ يعني «أعِد التسليم»** — فلا دالّةَ `requiresRedelivery` تُسأل بعد الرجوع، والرفضُ المُسمّى (400/409/422) يُجمَع في `outcomes` ولا يُعاد تسليمُه (إعادةُ واقعةٍ متأخّرةٍ ستتأخّر دائماً)، وما ليس مُسمّىً **يُرفَع كما هو**؛ و`EventSinkPort` **منفذٌ** لا `fetch` (فحرسُ النقاءِ لم يحتج تعديلاً ولا استثناءً)، و`drainOutbox` يُطالِب ثمّ يُسلّم ثمّ يُعلّم بحدٍّ إلزاميّ، والفشلُ **لا يُعلّم الصفَّ** فيُعاد في التصريفِ التالي بلا نشرتَين لصفٍّ واحد. و**57 اختباراً جديداً** (11 حارس انحرافٍ للحمولة + 22 للترجمة + 10 للمُستهلك + 14 للتصريف) ⇒ **الخدمة 282 اختباراً في 15 ملفّاً**. وقبلها **MR 4/6 — طبقة HTTP على المنفذ 8092**: `createReputationApp` يخدم **تسعة مسارات (إحدى عشرة عمليّة)** بينها `/health` بحالتيه (`postgres` ⇒ `ok` · `memory` ⇒ `degraded`) و`last_tick_at`، فوق **`ReputationRunner` وحده** فلا معالجُ مسارٍ يرى `deps` ولا يستطيع فتحَ معاملة، و**لا `try/catch` في أيّ معالج** بل مُترجِمُ خطأٍ واحد؛ و**الدَّين المُعلَن (د) مسدود**: `response_status`/`response_body` صارا **الجوابَ المحفوظَ فعلاً** فإعادةُ المفتاح تُعيد **نفس البايتات** بـ`200` و`duplicate: true` — لا `409` ولا جسماً مُعاد بناؤه من حالةٍ تحرّكت بعده؛ و`onlyKeys()` على كلّ حمولة و**كتالوجاتُ تعدادات وقتِ تشغيل** بلا `as` للتحقّق؛ و**لا `502` في أيّ فرع**، و`500` **انحرافٌ مُعلَن** لقيدِ تماسكٍ ملفوفٍ في `cause` برمزٍ `REPUTATION_INTERNAL_ERROR` **غائبٍ عن الكتالوج قصداً ومحروسٍ بغيابه** (الفرقُ المحميّ: `503` «لم نعرف شيئاً فأعد» مقابل `500` «كُسر شيءٌ عندنا فلا تُعد»)؛ و`http/server.ts` **الموضعُ الوحيد** الذي يقرأ `process.env` و`infrastructure/runtime.ts` **الموضعُ الوحيد** الذي يقرأ ساعةَ النظام، ولكلٍّ منهما **اختبارٌ موجَبٌ يُحسب من المصدر** فلا تتّسع قائمةُ الاستثناء بالإهمال (توسيعُ نطاقٍ لا تضييقُ حدّ). و**57 اختباراً جديداً** (44 على `app.inject` + **13 حارس انحراف** يقرأ العقدَ من القرص ويقابله بما سجّله Fastify **في الاتجاهين** وبقوائم مفاتيح الأجسام والمُرشِّحات وبالحالات المنشورة) ⇒ **الخدمة 225 اختباراً في 11 ملفّاً**، والمستودع كاملاً **2787 ناجحاً + 1 متروكاً بقصد · 0 فاشل** و`pnpm -r typecheck` نظيف على **34 مشروعاً** — والتفصيل في [REPUTATION_HTTP.md](../04-api/REPUTATION_HTTP.md). و`services/reputation/contracts/*` **لم تُلمَس**، ولا حالةَ استخدامٍ عُدِّلت. وقبلها **MR 3/6: العقود الكنسيّة وحزمة الأنواع وADR-014 (1/6)، ثمّ طبقة المجال النقيّة `@wasla/reputation-service` (2/6)، ثمّ الاستمراريّة على Postgres ووحدةُ العمل ومطابقةُ المُهيئَين (3/6) — بلا HTTP بعد.** **القاعدة الحاكمة:** السمعة **نتيجةٌ مُشتقّة** من دفتر وقائع append-only، والاحتيال **إشاراتٌ مُسمّاة لا حُكم**، و**الخدمة لا تعاقب أحداً** — تُعلن حقائق ويملك أثرَها غيرُها. مصدرُ الحقيقة **حدثان منشوران** من محرّك الطلب (`order.status_changed` · `order.assignment_resolved`) لا استعلامٌ دوريّ ولا قراءةُ قاعدة، والتسليم at-least-once فالتفرّد **قيدٌ في القاعدة** (`ux_reputation_facts_source`) لا فحصٌ في الكود، و**حذفُ جدول النتائج بالكامل عملٌ بلا خسارة** لأنّ `recompute` يُعيده من الدفتر. المُسلَّم: [ADR-014](../15-decisions/ADR-014-reputation-derived-scores-and-fact-sourced-fraud-signals.md) بتسعة قرارات · عقود `services/reputation/contracts/*` (**تسعة جداول** بقيودٍ مُسمّاة · **تسعة مسارات** على المنفذ **8092** · **خمسة أحداث** · **17 كود خطأ**) · حزمة `@wasla/contracts-reputation` بـ**81 حارس انحراف** · [نموذج المجال](../03-domain/REPUTATION_FRAUD.md) · [CONTAINERS §4.6](../02-architecture/CONTAINERS.md). **انحرافٌ موثّق في العدد لا في الموضع:** خدمةٌ واحدة `services/reputation` تحمل السمعة والاحتيال معاً، و`services/fraud/` تبقى **فارغة بقرار** ([ADR-014](../15-decisions/ADR-014-reputation-derived-scores-and-fact-sourced-fraud-signals.md) القرار 1) — لأنّ قواعد الاحتيال تقرأ **نفس دفتر الوقائع**، وخدمةٌ ثانية تعني نسخةً ثانية من الوقائع تتباعد بصمت. **وما لا يوجد في الخدمة بقرار** (ADR-014 القرار 7 · 9): لا إيقاف ولا حجب ولا عقوبة · ولا اشتراكات ولا مكافآت · ولا تسعيرٌ يعتمد النقاط · ولا تعليقٌ نصّي ولا بلاغٌ ولا تنقيح · ولا بيانات شخصية ولا مُعرّف قناة — لا عموداً ولا مساراً ولو معطّلاً؛ مالكوها Phase 15 · 10 · 16 · 19. المقيس محلّيّاً: **81 اختباراً في أربعة ملفات · 0 فاشل** لحزمة `@wasla/contracts-reputation` (مخطّط 24 · أحداث 20 · حدود 19 · عقود 18) تقرأ العقود **من القرص** لا نسخةً منها في الكود، و`pnpm -r typecheck` نظيف على 32 مشروعاً. **الدَّين المُعلَن:** ناشرُ `reputation_outbox` (نفس دَين الأطوار 06 · 07 · 08) ومستهلكُ أحداث الطلب في MR 5/6. **وقد نُفِّذت MR 5/6 — مستهلكُ حدثَي محرّك الطلب وناشرُ صندوق الصادر — ثمّ MR 6/6 بوابةُ الخروج** عبر نفس حالات الاستخدام بلا تعديلٍ فيها، وشرطها الملزم في [HANDOFF §16](HANDOFF_NEXT_STEPS.md). **وسابقاً في اليوم نفسه:** **Phase 08 (Negotiation & Chat) = Completed ✅ — ستّ مراجعات من ستّ مدمجة في `main`**، و**بوابة الخروج اجتازت**: حزمة `packages/negotiation-e2e` تُشغّل **سبع خدمات مُنصتة** بساعة واحدة محقونة (هوية · جغرافيا · عملاء · طلبات · مطابقة · توزيع · تفاوض)، ومنفذا التفاوض الخارجان يُبنيان بـ`configuredDispatchOffers` و`configuredAgreedPrice` — الدالّتان نفسهما التي يناديهما `src/http/server.ts` — فما يُثبَت هو التركيب المنشور لا تركيبٌ كُتب للاختبار. **ثمانية اختبارات، والأربعة الملزمة فيها**: مسار سعيد (نيّة عميل ⇒ طلب `negotiable` بلا سعر ⇒ موجة توزيع حقيقية ⇒ خيط على عرضها ⇒ جولة ⇒ قبول ⇒ `GET /orders/lookup` يحمل المبلغ و`agreed_negotiation_id`، **وحالة الطلب لم تتغيّر** لأنّ تسجيل السعر ليس انتقالاً)، وإعادةٌ بالمفتاح نفسه (201 ثمّ 200، جسمٌ متطابق حرفياً ومحاولة تسليم **واحدة**، ونبضةٌ بعدها لا تُعيد التسليم)، ورفضٌ نهائي من المحرّك (عرضان حيّان على طلب واحد ⇒ اتفاقان ⇒ الأوّل `handed_off` والثاني `rejected` بـ`ORDER_AGREED_PRICE_ALREADY_SET` **مع بقاء الاتفاق صحيحاً والقبول 201 ولا `502` في أي موضع** — ADR-013 القرار 2)، ومحرّكٌ غير قابل للوصول (يُغلق مُستمعه فعلاً ⇒ القبول 201 و`pending` و`next_handoff_at` = الآن + **30s** بالثانية، ونبضةٌ قبل اللحظة لا تفعل شيئاً، ثمّ يرجع المحرّك على **المنفذ نفسه** وتتقدّم الساعة فنبضةٌ واحدة تُسلّم السعر). **ولا قراءة من مخزن ولا صفٌّ مبنيٌّ بيد الاختبار ولا `sleep`.** والبوابة **أظهرت** أنّ الحالة الملزمة الثالثة كما وُصفت في HANDOFF غير قابلة للوصول عبر HTTP العام (`openThread` يرفض أوّلاً بـ422 على طلبٍ غير قابل للتفاوض)، فأُعيد تصميمها إلى الطريق الذي يقع فعلاً، وصار الوصف الأصلي حرزاً خامساً يمنع نسخ `negotiable` بدل قراءته من `price_mode` عند المحرّك. المقيس محلياً: **8/8 على الذاكرة و8/8 على Postgres 17 حقيقي**، ووظيفة `negotiations-exit-gate-e2e` بمتغيّر `NEGOTIATION_DATABASE_URL` وقاعدة `wasla_negotiation_e2e`، وإجمالي المستودع **2481 اختباراً + 1 متروك بقصد في 150 ملفاً · 0 فاشل** و`pnpm -r typecheck` نظيف على 32 مشروعاً — والتفصيل في [PHASE08_EXIT_GATE_E2E.md](../12-testing/PHASE08_EXIT_GATE_E2E.md). **المرحلة التالية: Phase 09 — Reputation + Fraud** (كل طلبٍ مكتمل يُنتج أحداث سمعة)، وشروط بدئها في [HANDOFF §15](HANDOFF_NEXT_STEPS.md). **وقبل البوابة في المرحلة نفسها:** العقود الكنسية وحزمة الأنواع وADR-013 (MR 1/6 · [!57](https://gitlab.com/uxxxu/wasla/-/merge_requests/57))، ثمّ **طبقة المجال النقيّة** بـ131 اختباراً ([NEGOTIATION_CORE_DOMAIN.md](../03-domain/NEGOTIATION_CORE_DOMAIN.md))، ثمّ **استمرارية Drizzle/Postgres** بوحدة عمل واحدة **بلا تغيير في `src/use-cases/`** بـ**62 اختبار تكامل على محرّك حقيقي** و**27 حارس انحراف** ووظيفة `negotiations-db-integration` ([NEGOTIATION_PERSISTENCE.md](../02-architecture/NEGOTIATION_PERSISTENCE.md))، ثمّ **طبقة HTTP على 8091** بعشرة مسارات وثلاث عشرة عمليّة و50 اختباراً جديداً منها 14 حارس انحراف ([NEGOTIATION_HTTP.md](../04-api/NEGOTIATION_HTTP.md))، ثمّ **المنافذ الصادرة الحقيقية** (`AgreedPricePort` → الطلبات 8087 · `DispatchOfferPort` → التوزيع 8089) مع **أعمدة الاتفاق وقيودها في `orders`** و**أوامر التفاوض في بوتَي العميل والسائق** ([NEGOTIATION_HTTP.md §12](../04-api/NEGOTIATION_HTTP.md))؛ **والمتبقّي في المرحلة مراجعةٌ واحدة**: بوابة الخروج E2E في `packages/negotiation-e2e` — تفاوضٌ كامل عبر HTTP العام وحده بساعةٍ واحدة محقونة، والتفصيل الملزم في [HANDOFF §14](HANDOFF_NEXT_STEPS.md). **وسابقاً (2026-08-22):** **W0 = 2026-08-20** (اجتازت Phase 00 Exit Gate — CI أخضر على main). **Phase 00 = Completed**، **Phase 01 = Completed** (خدمة Identity: Fastify + Drizzle/Postgres + Exit Gate E2E في CI)، و**Phase 02 = Completed** (خدمة Geography: عقود + ADR-006 + نواة مجردة + Drizzle/Postgres + Saudi seed + Fastify HTTP + وظيفة `geography-db-integration` + [بوابة خروج E2E](../12-testing/PHASE02_EXIT_GATE_E2E.md) تُشغّل identity و geography معاً في CI). **Phase 03 = Completed (2026-08-21)** — سبع مراجعات مدمجة على أساس [ADR-007](../15-decisions/ADR-007-telegram-channel-adapter-isolation-and-stack.md) (القناة طبقة توصيل لا خدمة: `packages/channel-core` محايد + `packages/telegram-adapter` وحده يعرف Bot API + `bots/*` جذور تركيب) و[ADR-008](../15-decisions/ADR-008-channel-groups-registry-and-reply-policy.md) (سجل المجموعات منفذاً عاشراً)، و**بوابة الخروج مُثبَتة لا موصوفة**: [PHASE03_EXIT_GATE_E2E.md](../12-testing/PHASE03_EXIT_GATE_E2E.md) — البوتات الثلاثة في عملية واحدة أمام خدمة هوية واحدة على HTTP، كل بوت يفتح Mini App الخاصة به، وشخص واحد = هوية واحدة، والمُهيّئ قابل للاستبدال بـ`MockChannelAdapter`. **Phase 04 (Customer Core) = Completed (2026-08-21)** — ستّ مراجعات مدمجة على أساس [ADR-009](../15-decisions/ADR-009-customer-core-placement-and-order-intake-boundary.md) (خدمة `services/customers` مستقلّة بانحراف معلَن · ملف العميل **ملفُّ دور** مفتاحه `wasla_public_id` بلا FK · **حدّ تسليم الطلب** عبر `OrderIntakePort` وحده: الخدمة لا تكتب `orders` ولا تُولّد `order_public_id`): MR 1/6 العقود ونموذج المجال · 2/6 طبقة المجال النقيّة (سبعة منافذ + ثماني حالات استخدام) · 3/6 استمرارية Drizzle/Postgres ووظيفة `customer-db-integration` · 4/6 طبقة HTTP على المنفذ 8086 ([CUSTOMER_HTTP.md](../04-api/CUSTOMER_HTTP.md)) · 5/6 ربط بوت العميل بالنواة عبر بذرة محادثة محيّدة ([CUSTOMER_BOT_FLOWS.md](../02-architecture/CUSTOMER_BOT_FLOWS.md)) · و**6/6 بوابة الخروج مُثبَتة لا موصوفة**: [PHASE04_EXIT_GATE_E2E.md](../12-testing/PHASE04_EXIT_GATE_E2E.md) — بوت العميل والنواة في عملية واحدة أمام هوية وجغرافيا على HTTP حقيقي ومحرّك طلبات بديل يرفض أي جسم لا يطابق `OrderIntakeRequest`، فيُثبَت أنّ **طلباً صالحاً يصل إلى المحرّك بحمولته المنشورة**، وأنّ كل مسارات الفشل fail-closed (11/11 بمخازن الذاكرة وعلى Postgres، ووظيفة `customer-exit-gate-e2e` في CI). **Phase 06 (Order Engine) = Completed (2026-08-21)** — ستّ مراجعات مدمجة، والمحرّك البديل في `packages/customer-e2e/src/stub-order-engine.ts` كان مواصفتها التنفيذية فصار المحرّك الحقيقي يجيب مكانه. **MR 1/6 و2/6 و3/6 منجَزة** على أساس [ADR-010](../15-decisions/ADR-010-order-engine-state-machine-and-assignment-boundary.md): عقود `services/orders/contracts/*` و`@wasla/contracts-order` و[جدول الانتقالات الكامل](../03-domain/ORDER_ENGINE.md) (72 زوجاً مسموحاً من 441) و[CONTAINERS §4.2](../02-architecture/CONTAINERS.md)؛ ثمّ **طبقة المجال `@wasla/orders-service`** — الاثنان والسبعون صفّاً صريحاً في `domain/state-machine.ts` بحارس مطابقة مزدوج مع الوثيقة في الاتجاهين، و**مسح الـ441 زوجاً منجَز فعلاً في اختبار وحدة** لا مؤجَّلاً إلى بوابة الخروج، و558 اختباراً بلا قاعدة ولا HTTP ([ORDER_CORE_DOMAIN.md](../02-architecture/ORDER_CORE_DOMAIN.md))؛ ثمّ **الاستمرارية Drizzle/Postgres** — مرآة `schema.sql` + `PostgresOrderRepository` و`PostgresOrderOutbox` و`PostgresOrderPublicIdGenerator` + **`PostgresOrderUnitOfWork` يُسدّ دَين الذرّية** ([ADR-010 §127](../15-decisions/ADR-010-order-engine-state-machine-and-assignment-boundary.md)): معاملة واحدة وسلّم نفس المقبض للمستودع والصادر، فالكتابة الثلاثية (حالة · تدقيق · صادر) ذرّية **بلا تغيير في `src/use-cases/`** · 17 حراسة انحراف + 30 اختبار تكامل (19 مستودع + 4 ذرّية + 7 مطابقة منافذ بصادر حقيقي) · إجمالي المستودع 1323 اختباراً ([ORDER_PERSISTENCE.md](../02-architecture/ORDER_PERSISTENCE.md)). **MR 4/6 منجَزة:** طبقة HTTP على المنفذ **8087** — سبعة مسارات + `/health` بحالتيه، و**`OrderRunner` مقبس معاملة** يجعل كل كتابة داخل وحدة عمل واحدة بلا أن يتذكّرها أي معالج مسار، ونطاق مالك يجيب **404 لا 403** لأن المُعرّف العام تسلسلي، والاستلام **201** جديد و**200** إعادة تشغيل مفتاح، وكتالوجات تعدادات وقت التشغيل ترفض العضو المجهول بـ400 على الحدّ، و`assertNotes` يُصلح تباعداً بين المحوّلين كان يُخرج 503 حيث يجب 400 · 46 اختبار `app.inject` · 621 اختباراً للخدمة ([ORDER_HTTP.md](../04-api/ORDER_HTTP.md)). **MR 5/6 منجَزة:** `HttpOrderIntakePort` الإنتاجي في خدمة العميل يستبدل `UnavailableOrderIntake` وينادي `POST /orders/intake`؛ خريطة الحالات مُصرَّحة (200 نجاح · 409/422 رفض نهائي · 400 خطؤنا لا رفض تجاري · 5xx قابل لإعادة المحاولة بنفس المفتاح · مهلة = `TIMEOUT` مُسجَّل)، ومفتاح التكرار و`x-request-id` يمرّان كما هما، و`/health` عند خدمة العميل صار **`ok` لأول مرة** · 17 اختباراً على مُنصت حقيقي + تسليم فعلي بين خدمتين تعملان ([ORDER_INTAKE_HANDOVER.md](../04-api/ORDER_INTAKE_HANDOVER.md)). **MR 6/6 منجَزة — بوابة الخروج مُثبَتة لا موصوفة**: [PHASE06_EXIT_GATE_E2E.md](../12-testing/PHASE06_EXIT_GATE_E2E.md) ([!43](https://gitlab.com/uxxxu/wasla/-/merge_requests/43)) — حزمة `@wasla/order-e2e` تُشغّل هوية وجغرافيا ونواة عميل ومحرّك طلبات كأربعة مُنصتات حقيقية، والتسليم بينها **بالمحوّل الإنتاجي نفسه** لا بنسخة منه، فيُقاد الطلب عبر HTTP من `published` إلى `completed` بسجلّ تدقيق كامل مرتّب، ثمّ **يُمسح فضاء الانتقالات كاملاً فوق HTTP: 441 زوجاً — 72 تنجح و369 تُرفض بـ`ORDER_ILLEGAL_TRANSITION` والحالة لا تتغيّر**، ويُحرَس انزلاق الحدّ بقراءة قائمة `required` من ملف العقد وقت التشغيل — 16/16 بمخزن ذاكرة وعلى Postgres (وظيفة `order-exit-gate-e2e`)، وإجمالي المستودع **1402 اختباراً**. **Phase 07 (Dispatch & Matching MVP) = Completed (2026-08-22)** — ستّ مراجعات مدمجة: MR 1/6 و2/6 و3/6 و4/6 و5a/6 (آخرها **استمرارية التوزيع على Postgres** بوحدة عمل واحدة تجعل النبضة كلّها ترتكز أو تتراجع، و47 اختبار تكامل في وظيفة `dispatch-db-integration`، ومطابقة منافذ تُقارن مُهيّئي الذاكرة وPostgres أحدهما بالآخر — [DISPATCH_PERSISTENCE.md](../02-architecture/DISPATCH_PERSISTENCE.md) · [!48](https://gitlab.com/uxxxu/wasla/-/merge_requests/48)؛ و**MR 5/6 قُسِمت إلى 5a/6 و5b/6 ودُمجتا** فصارت الخدمتان قابلتين للإقلاع والنداء على 8088 و8089 بثلاثة محوّلات HTTP إنتاجية، ثمّ **MR 6/6 منجَزة — بوابة الخروج مُثبَتة لا موصوفة**: [PHASE07_EXIT_GATE_E2E.md](../12-testing/PHASE07_EXIT_GATE_E2E.md) — حزمة `@wasla/dispatch-e2e` تُشغّل **ستّ خدمات** كمُنصتات حقيقية (هوية · جغرافيا · محرّك طلبات · نواة عميل · مطابقة · توزيع) بـ**ساعة واحدة مُحقونة** في المطابقة والتوزيع معاً، والتوزيع يسأل المطابقة ويخاطب المحرّك **بالمحوّلين الإنتاجيين نفسيهما**؛ فيُثبَت أنّ طلباً حقيقياً يجد سائقاً حقيقياً عبر موجات (رفض ← موجة تستثني الرافض ← قبول، فيصبح الطلب `accepted` مربوطاً بالسائق من سجلّ الإسنادات وإسقاط الجهوزية `busy`)، وأنّ الصمت يُنهي العرض عند الموعد بالضبط بلا `sleep`، وأنّ لا-مرشّحين تعني تصعيداً إلى المجتمع والطلب لا يزال `searching` ثمّ `no_driver_found` بعد نافذة التصعيد، وأنّ خاسر السباق `superseded` **لا** `rejected` لأنّه لم يقل «لا». **والبوابة كشفت عيباً يُسقط كل قبول سائق في الإنتاج**: `accept-offer.ts` كان ينادي `transitionOrder` قبل `resolveAssignment`، والمحرّك يربط السائق داخل نفس `UPDATE` التي تُحرّك الحالة (قيد `ck_orders_assignment_matches_status` · [ADR-010 §4/§7](../15-decisions/ADR-010-order-engine-state-machine-and-assignment-boundary.md))، فكان كل قبول يُرفض 422؛ أُصلح الترتيب و**عُلِّم البديل القاعدة نفسها** في نفس المراجعة لئلّا يعود الاختفاء — 5/5 بمخازن الذاكرة وعلى Postgres (وظيفة `dispatch-exit-gate-e2e` على `wasla_dispatch_e2e`)، وإجمالي المستودع **1838 اختباراً + 1 متجاوَز** في 110 ملفات)؛ وMR 1/6 مدمجة ([!44](https://gitlab.com/uxxxu/wasla/-/merge_requests/44)): [ADR-011](../15-decisions/ADR-011-matching-dispatch-separation-candidate-source-and-tick-driven-time.md) يحسم أربعة أسئلة كانت مفتوحة — **خدمتان لا واحدة** (`services/matching` :8088 دالّة «من يصلح؟» · `services/dispatch` :8089 مهمّة «من يأخذه الآن؟»، والمحرّك لا يعرف أنّ التوزيع موجود) · **مصدر المرشّحين** إسقاط `driver_candidacy` مفتاحه `driver_public_id` بلا FK والأهليّة فيه **مُدّعاة ومصدر الادّعاء مخزّن**، والمجهول ليس مرشّحاً (fail-closed) فلا تنتظر Phase 05 · **الزمن نبضة لا مؤقّت**: كل استحقاق في القاعدة و`POST /dispatch/tick` هو الموضع المُعلَن الوحيد لتقديمه، فإعادة التشغيل لا تُفقد مهلة والاختبار يُقدّم الساعة بلا انتظار · **الأوزان بيانات بنسخة مُقفَلة** مجموعها 100 بقيد في القاعدة، وصفر ETA والمسافة والتقييم **صفرٌ مُعلَن** لا إغفال، وحسم التعادل مُصرَّح. وسُلّمت العقود الكنسية للخدمتين و`@wasla/contracts-matching` و`@wasla/contracts-dispatch` بـ46 حارس انحراف و[نموذج المجال](../03-domain/MATCHING_DISPATCH.md) و[CONTAINERS §4.3](../02-architecture/CONTAINERS.md) — إجمالي المستودع 1448 اختباراً** — الإسنادات في بوابة المرحلة 06 مُسجَّلة بالشكل نفسه الذي يجب أن تُنتجه المطابقة، فالمحرّك **يسجّل الإسناد ولا يقرّره** ([ADR-010](../15-decisions/ADR-010-order-engine-state-machine-and-assignment-boundary.md)): المطابقة تنادي `POST /orders/{id}/assignments` ثمّ `PATCH …/{assignmentId}` ولا تكتب حالة الطلب بنفسها. **وترتيب المسار لم يُخترق: Phase 06 تسبق Phase 05 عن قصد وبقرار موثّق** — `driver_public_id` مرجع opaque بلا FK، فالمحرّك لا يحتاج جدول سائقين ليعمل، وPhase 05 (Driver Core) تبقى خارج المسار الحرج `00 → 01 → 02 → 04 → 06 → 07 → 09 → 20` كما هي في §3 أدناه. لا تعديل على الترتيب ⇒ لا ADR ترتيب مطلوب (§7). **Phase 05 (Driver Core) = Completed (2026-08-22)** — ستّ مراجعات مدمجة على أساس [ADR-012](../15-decisions/ADR-012-driver-core-eligibility-derivation-and-candidacy-publication.md) (**الأهليّة دالّة مُشتقّة لا عمود** بحارس يفشل على عودة `eligibility_state` بالاسم · **لا كتابة في قاعدة المطابقة** بل نشرٌ عبر `PUT /candidacy/{id}` بـ`eligibility_source = driver_core` الذي انتظره عقد المطابقة منذ الطور 07 · «الالتزام الجاري يعلو على الإعلان» فـ`busy` يملكها التوزيع · **الزمن نبضة لا مؤقّت** فلا حالة `expired` على وثيقة · الوثائق مراجع لا محتوى · وحارس خصوصيّة ينفي من كل حمولة حدث الاسم والهاتف والهوية واللوحة): MR 1/6 العقود (8 جداول · 13 مساراً · 11 حدثاً · 21 كود خطأ · 59 حارس انحراف) · 2/6 طبقة المجال النقيّة وحاسب الأهليّة بقاعدة **«كلّ الأسباب لا أوّلها»** (69 اختباراً) · 3/6 استمرارية Drizzle/Postgres بوحدة عمل واحدة **بلا تغيير في `src/use-cases/`** ووظيفة `drivers-db-integration` (28 حارس انحراف + 79 اختبار تكامل) · 4/6 طبقة HTTP على **8090** بمقبس معاملة فلا معالج مسار يستطيع فتح معاملة (64 اختباراً) · 5/6 المنافذ الصادرة الحقيقية إلى المطابقة 8088 والجغرافيا 8081 وبوت السائق، وفيها **تقاعُد 502** · و**6/6 بوابة الخروج مُثبَتة لا موصوفة**: [PHASE05_EXIT_GATE_E2E.md](../12-testing/PHASE05_EXIT_GATE_E2E.md) — **سبع خدمات مُنصتة بساعة واحدة مُحقونة** تُثبت أنّ سائقاً يُسجَّل ويُراجَع فيصله عرضٌ حقيقي من التوزيع **بأهليّة `driver_core` محسوبة** ثمّ **بنبضة واحدة** يخرج من التجمّع (14/14) — وقد أسقطت **عيباً حقيقياً**: تصادُم مفتاح منع التكرار تحت نبضة ساعة واحدة كان يجعل نشراً «ناجحاً» لا يُحرّك صفّ المطابقة، فأُصلح بعدّاد محاولات وحُرِس بحارس بساعة مجمَّدة، والفائدة قِيست بفحص طفرة (2 من 14 تسقط بإعادة العيب). **المرحلة التالية: Phase 08 (Negotiation & Chat)** — اعتمادها الوحيد 07 وقد أُغلقت، وهي على المسار الحرج؛ التفصيل في [HANDOFF_NEXT_STEPS.md §12](HANDOFF_NEXT_STEPS.md). **المُنقَل إلى Phase 09:** ذرّية كتابة الصفّ ونشر الحدث + ناشر `customer_outbox` + مُشغّل دوري لـ`retryDueDeliveries` (دَين المرحلة 03). **المُنقَل إلى Phase 11:** إنشاء الطلب وحفظ مكان من التطبيق المصغّر — [HANDOFF §9](HANDOFF_NEXT_STEPS.md).** Timeline أدناه نسبية إلى W0 (المُعتمد).

---

## 1. مبدأ الزمن النسبي (يمنع تقادم الوثيقة)

```text
W0 = اليوم الذي تجتاز فيه Phase 00 Exit Gate — وليس يوم إنشاء المستودع.
```

كل المدد أدناه **نسبية إلى W0**. لا تواريخ ثابتة مقفلة. الأسبوع الزمني تقديري لتخطيط الفريق، لكن:

> **الانتقال الفعلي بين المراحل لا يتم بالوقت، بل باجتياز Exit Gate.**
> إذا لم يُجتز الـ Exit Gate، يبقى النظام في المرحلة رغم انتهاء الأسبوع المقدر.

---

## 2. الخط الزمني الكلي (Baseline v1.0)

```text
┌──────────── الجذر الأساسي ────────────┐
│ W0  – W1   : Phase 00 — Repository Foundation
├────────── الهوية والأساس ────────────┤
│ W1  – W6   : Phases 01–03 — Identity, Geography, Telegram Foundation
├────────── المحرك التشغيلي ──────────┤
│ W6  – W15  : Phases 04–10 — Customer, Driver, Order, Dispatch, Matching, Reputation, Subscription
├────────── النظام التجاري ───────────┤
│ W16 – W26  : Phases 11–17 — Marketplace, Search, Store Orders, Partner, Admin, Support, Billing
├────────── الجاهزية للإطلاق ──────────┤
│ W26 – W30  : Phases 18–20 — Observability, Security, Saudi Launch Readiness
│            ★ MILESTONE: SAUDI LAUNCH (نهاية ~W30)
├────────── ما بعد الإطلاق ───────────┤
│ W30+       : Phases 21–24 — Regional, Global, Channel Independence, Service Extraction
└──────────────────────────────────────┘
```

---

## 3. جدول المراحل بالتفصيل

| Phase | العنوان | Start (نسبية) | المدة المقدرة | يعتمد على | Owner Teams | Exit Gate (ملخص) |
|---|---|---|---|---|---|---|
| 00 | Repository Foundation | W0 | ~1 أسبوع | — | جميع الفرق | CI passes، لا أسرار، Docs structure، main محمي، MR template |
| 01 | Identity Foundation | W1 | ~2 أسبوعين | 00 Exit Gate | 01,09,10,11,12 | هوية مستقرة عبر تغيير Username |
| 02 | Geography & Localization | W3 | ~2 أسبوعين | 00 + (01 جزئياً) | 01,02,03,06,07,08,09 | تغيير الموقع دون حساب جديد + i18n (AR/EN/UR) |
| 03 | Telegram Channel Foundation | W4 | ~2 أسبوعين | 00 + 01 | 12,01,02,03,07 | كل Bot يفتح Mini App + Adapter قابل للاستبدال بـMock |
| 04 | Customer Core | W6 | ~2 أسبوعين | 01,02,03 | 02,01,11,12 | عميل ينشئ Order صالحاً يصل لـ Order Engine |
| 05 | Driver Core | W6 | ~2 أسبوعين (توازٍ مع 04) | 01,02,03 | 03,01,11,12,09 | Driver profile قابل للإدخال في Candidate pool — ✅ **Completed 2026-08-22 (MR 1/6 → 6/6 · بوابة الخروج اجتازت)**: العقود + ADR-012 · طبقة المجال النقيّة وحاسب الأهليّة بقاعدة «كلّ الأسباب لا أوّلها» · استمرارية Drizzle/Postgres بوحدة عمل واحدة تجعل العمليّة كلّها ترتكز أو تتراجع **بلا تغيير في `src/use-cases/`** · طبقة HTTP على 8090 بمقبس معاملة · المنافذ الصادرة الحقيقية إلى المطابقة 8088 والجغرافيا 8081 وبوت السائق · و**بوابة الخروج**: سبع خدمات مُنصتة بساعة واحدة تُثبت أهليّة `driver_core` محسوبة وخروجاً **بنبضة واحدة** ([PHASE05_EXIT_GATE_E2E.md](../12-testing/PHASE05_EXIT_GATE_E2E.md) · وظيفة `driver-exit-gate-e2e`) |
| 06 | Order Engine | W8 | ~3 أسابيع | 04,05 | 02,03,04,05,09,11 | Order + State machine بلا حالات مستحيلة + Outbox + Audit |
| 07 | Dispatch & Matching MVP | W11 | ~3 أسابيع | 06 | 04,05,03,02,09,11 | Customer → Driver assignment في بيئة اختبار — **مُنجَزة 2026-08-22 (MR 1/6 و2/6 و3/6 و4/6 و5a/6 مدمجة: العقود ثمّ مجال المطابقة النقي ثمّ استمرارية المطابقة على Postgres بوحدة عمل ووظيفة CI `matching-db-integration` ثمّ **مجال التوزيع النقي** بـ142 اختباراً — النبضة المُقدِّم الوحيد للزمن ومواعيد مخزَّنة وسباق قبول محسوم بترتيب مُعلَن ([DISPATCH_CORE_DOMAIN.md](../02-architecture/DISPATCH_CORE_DOMAIN.md)) — ثمّ **استمرارية التوزيع** بوحدة عمل تجعل النبضة كلّها ترتكز أو تتراجع، بـ47 اختبار تكامل في وظيفة `dispatch-db-integration` ومطابقة منافذ تُقارن المُهيّئين ببعضهما ([DISPATCH_PERSISTENCE.md](../02-architecture/DISPATCH_PERSISTENCE.md)) · **MR 5/6 قُسِمت إلى 5a/6 و5b/6 ودُمجتا** · ثمّ **6/6 بوابة الخروج مُثبَتة لا موصوفة** ([PHASE07_EXIT_GATE_E2E.md](../12-testing/PHASE07_EXIT_GATE_E2E.md)): حزمة `@wasla/dispatch-e2e` تُشغّل ستّ خدمات كمُنصتات حقيقية بساعة واحدة مُحقونة، فيُقاد طلب حقيقي إلى سائق حقيقي عبر موجات — رفض ثمّ موجة تستثني الرافض ثمّ قبول — ويُثبَت انقضاء المهلة والتصعيد والنفاد والسبق (`superseded` لا `rejected`)؛ **وكشفت عيباً يُسقط كل قبول سائق في الإنتاج** (ترتيب النداءين في `accept-offer.ts`) — 5/5 بمخازن الذاكرة وعلى Postgres (وظيفة `dispatch-exit-gate-e2e`) وإجمالي المستودع **1838 اختباراً**. **البوابة هي الدليل، والمرحلة مُغلقة.**)** |
| 08 | Negotiation & Chat | W13 | ~2 أسبوعين | 07 | 02,03,12,09,11 | تفاوض + توافق على السعر + تسجيله في Order — ✅ **Completed 2026-08-23 (ستّ مراجعات من ستّ مدمجة في `main`)**: العقود وADR-013 ([!57](https://gitlab.com/uxxxu/wasla/-/merge_requests/57)) · طبقة المجال النقيّة بـ24 قاعدة مُسمّاة و131 اختباراً ([!58](https://gitlab.com/uxxxu/wasla/-/merge_requests/58)) · استمرارية Drizzle/Postgres بوحدة عمل واحدة و62 اختبار تكامل ([!59](https://gitlab.com/uxxxu/wasla/-/merge_requests/59)) · **طبقة HTTP على 8091** بعشرة مسارات و50 اختباراً منها 14 حارس انحراف ([!60](https://gitlab.com/uxxxu/wasla/-/merge_requests/60) · [NEGOTIATION_HTTP.md](../04-api/NEGOTIATION_HTTP.md)) · **المنافذ الصادرة الحقيقية + أعمدة الاتفاق في `orders` + `POST /orders/agreed-prices` + `GET /orders/lookup` + `GET /dispatch/offers/{offer_id}` + أوامر التفاوض في البوتين، بـ69 اختباراً جديداً** ([!62](https://gitlab.com/uxxxu/wasla/-/merge_requests/62) · [NEGOTIATION_HTTP.md §12](../04-api/NEGOTIATION_HTTP.md)) · **بوابة الخروج E2E اجتازت: سبع خدمات مُنصتة بساعة واحدة، والأربعة الملزمة (مسار سعيد · إعادة بالمفتاح نفسه ⇒ اتفاق وسعر ومحاولة واحدة · رفض نهائي مع بقاء الاتفاق ولا 502 · محرّك مُغلَق ⇒ pending ثمّ تسليم بنبضة بعد 30s)، 8/8 على الذاكرة و8/8 على Postgres 17** ([!63](https://gitlab.com/uxxxu/wasla/-/merge_requests/63) · [PHASE08_EXIT_GATE_E2E.md](../12-testing/PHASE08_EXIT_GATE_E2E.md)) |
| 09 | Reputation + Fraud | W14 | ~2 أسبوعين | 07,08 | 01,04,05,08,09,11 | كل Completed Order ينتج Reputation events — ✅ **Completed 2026-08-23 · MR 1/6 → 6/6 · بوابة الخروج اجتازت** (الدليل: `packages/reputation-e2e` **12/12** — طلبٌ حقيقيٌّ عبر HTTP إلى `completed` ⇒ واقعتان ⇒ **العميل 63 والسائق 64** ⇒ صفُّ صادرٍ يُسلَّم مرّةً واحدة، والمستودع **2856 ناجحاً · 0 فاشل** · [PHASE09_EXIT_GATE_E2E.md](../12-testing/PHASE09_EXIT_GATE_E2E.md)) — وطبقاتُها بالترتيب: [ADR-014](../15-decisions/ADR-014-reputation-derived-scores-and-fact-sourced-fraud-signals.md) بتسعة قرارات · عقود `services/reputation/contracts/*` (**تسعة جداول** بقيودٍ مُسمّاة · **تسعة مسارات** على المنفذ **8092** · **خمسة أحداث** · **17 كود خطأ**) · حزمة `@wasla/contracts-reputation` بـ**81 حارس انحراف** · [نموذج المجال](../03-domain/REPUTATION_FRAUD.md) · [CONTAINERS §4.6](../02-architecture/CONTAINERS.md). **القاعدة الحاكمة:** السمعة **نتيجةٌ مُشتقّة** من دفتر وقائع append-only، والاحتيال **إشاراتٌ مُسمّاة لا حُكم**، و**الخدمة لا تعاقب أحداً** — تُعلن حقائق ويملك أثرَها غيرُها. مصدرُ الحقيقة **حدثان منشوران** من محرّك الطلب (`order.status_changed` · `order.assignment_resolved`) لا استعلامٌ دوريّ ولا قراءةُ قاعدة، والتسليم at-least-once فالتفرّد **قيدٌ في القاعدة** (`ux_reputation_facts_source`) لا فحصٌ في الكود، و**حذفُ جدول النتائج بالكامل عملٌ بلا خسارة** لأنّ `recompute` يُعيده من الدفتر. **انحرافٌ موثّق في العدد لا في الموضع:** خدمةٌ واحدة `services/reputation` تحمل السمعة والاحتيال معاً، و`services/fraud/` تبقى **فارغة بقرار** ([ADR-014](../15-decisions/ADR-014-reputation-derived-scores-and-fact-sourced-fraud-signals.md) القرار 1) — لأنّ قواعد الاحتيال تقرأ **نفس دفتر الوقائع**، وخدمةٌ ثانية تعني نسخةً ثانية من الوقائع تتباعد بصمت. المقيس محلّيّاً: **81 اختباراً في أربعة ملفات · 0 فاشل** لحزمة `@wasla/contracts-reputation` (مخطّط 24 · أحداث 20 · حدود 19 · عقود 18) تقرأ العقود **من القرص** لا نسخةً منها في الكود، و`pnpm -r typecheck` نظيف على 32 مشروعاً. **وMR 2/6 مدفوعة ([!65](https://gitlab.com/uxxxu/wasla/-/merge_requests/65)) — طبقة المجال النقيّة** `@wasla/reputation-service`: 19 ملف مصدر بلا قاعدةٍ ولا HTTP ولا ساعةِ نظام؛ حاسبُ النقاط **دالّةٌ نقيّة** بتلاشٍ صريحٍ على `occurredAt` بنصف عمر 180 يوماً، وخمسُ قواعد احتيالٍ مُسمّاة تُعيد إشارةً أو لا شيء، وحدُّ النافذة **يُحسب لا يُخمَّن**، ومُهيّئ ذاكرةٍ يفرض **القيود الخمسة عشر بأسمائها الحرفيّة**؛ المقيس محلّيّاً **133 اختباراً في 8 ملفات · 0 فاشل** والمستودع **2695 + 1 متخطّى في 162 ملفاً** و`typecheck` نظيف على **34 مشروعاً** ([REPUTATION_CORE_DOMAIN.md](../02-architecture/REPUTATION_CORE_DOMAIN.md)). **والمتبقّي في المرحلة أربع مراجعات**: 3/6 استمرارية Drizzle/Postgres بوحدة عمل واحدة · 4/6 طبقة HTTP على **8092** · 5/6 المنافذ الصادرة الحقيقية (استهلاك حدثَي الطلب) · 6/6 **بوابة الخروج E2E** في `packages/reputation-e2e` تُثبت أنّ طلباً مكتملاً عبر HTTP العام يُنتج **حدث سمعةٍ واحداً لا صفراً ولا اثنين** وأنّ إعادة الحدث نفسه **لا تُضاعف النقاط** — والتفصيل في [HANDOFF §16](HANDOFF_NEXT_STEPS.md) |
| 10 | Driver Subscription & Referral | W15 | ~2 أسبوعين | 05,09 | 03,01,09,12,08,11 | Trial→Active→Expired→Community + إحالات بلا نشاط وهمي — 🔄 **قيد التنفيذ: MR 3/6 (استمراريّةُ Postgres · مُهاجرةٌ تُطبّق `contracts/schema.sql` على محرّكٍ حقيقيّ · بذرةُ الخطّةِ **من `plans.ts` نفسِه** حقلاً حقلاً · دفترُ إضافةٍ فقط بلا `UPDATE` واحدٍ · مطابقةُ اشتقاقٍ: قاعدة = ذاكرة · 21 اختبارَ تكاملٍ + حارسُ انحرافٍ بلا قاعدة · `purity.test.ts` **مُحدَّثٌ بالأسماء لا مُبطَل**) مدفوعة؛ MR 2/6 `56d1a6c7` · merge `0f0d0ccc`؛ **وMR 4/6 مدفوعة** (طبقةُ التطبيقِ ومعاملةٌ واحدةٌ وHTTP على **8093** · المؤشِّرُ **مُشتقٌّ** لا مصدرَ حقيقةٍ ثانياً · عيبُ الوثبةِ الوسطى مُصلَحٌ بـ`transitionPath`) — [!75](https://gitlab.com/uxxxu/wasla/-/merge_requests/75) merge `2708223c`؛ **وMR 5/6 مدفوعة** (الأحداثُ وصندوقُ الصادرِ ومنعُ التكرارِ ومكافأةُ الإحالة: صفٌّ في **نفسِ معاملةِ التغيير** لا نشرٌ داخلَها · تسليمٌ **مرّةً على الأقلّ** مُعلَناً · مخزنٌ **بلا `UPDATE`** يُعيد نفسَ البايتات · مكافأةٌ **أيّامٌ في الدفترِ** تبدأ من نهايةِ تغطيةِ المُحيل · **عيبُ الصفِّ البائتِ مُصلَحٌ** فلا يضيع حقُّ سائقٍ بتأخّرِ نبضةٍ · أجسامُ الكتابةِ مُطابقةٌ للعقدِ بحارسٍ يقرأ `required:` من القرص · الذرّيّةُ **مُثبَتةٌ بمِسْبارٍ** · 205 سريعاً + 54 تكاملاً)؛ التالي **MR 6/6** (بوابةُ الخروج e2e مع خدمةِ السمعةِ الحقيقيّةِ + إعادةُ تشغيلِ مسارِ HTTP) وفق [HANDOFF §18.10](HANDOFF_NEXT_STEPS.md)** |
| 11 | Marketplace Foundation | W16 | ~3 أسابيع | 01,07 | 06,01,07,02,03,09,11 | مستخدم قائم ينشئ Store + منتج + طلب مراجعة |
| 12 | Marketplace Search | W18 | ~2 أسبوعين | 11 | 06,09,12,11 | منتج يُعثر عليه بالعربي والإنجليزي + جاهز للغات |
| 13 | Store Orders + Delivery | W19 | ~3 أسابيع | 11,12,07 | 06,05,04,03,02,07 | شراء→تجهيز→إسناد→Pickup→Delivery→Completion |
| 14 | Partner / Enterprise | W21 | ~3 أسابيع | 07,13 | 07,06,05,04,12,11 | Partner ينشئ طلباً عبر Portal/API وتتبع حالته |
| 15 | Admin Operations | W23 | ~2 أسبوعين | 14,11 | 08,01,03,04,05,06,07,11 | الإدارة تشغّل اليوميات دون تعديل DB يدوي |
| 16 | Support & Escalation | W24 | ~2 أسبوعين | 15 | 08,02,03,11,12 | نزاع كامل → Support → Escalation → Resolution → Reputation |
| 17 | Billing & Store Fees | W25 | ~2 أسبوعين | 16,10 | 09,12,07,06,08,11 | Billing قابل للتدقيق بلا خلط مع Trip Settlement |
| 18 | Observability & Resilience | W26 | ~2 أسبوعين | 17 | 10,11,09,12 | تعطيل خدمة ثانوية بلا إسقاط Core + استعادة وفق RTO/RPO |
| 19 | Security Hardening | W27 | ~2 أسبوعين | 18 | 11,10,01,12 | لا ثغرات حرجة + الأسرار خارج Git + Production access مضبوط |
| 20 | **Saudi Launch Readiness** | W28 | ~2 أسبوعين | 19 | جميع الفرق | E2E + Load + DR + Runbooks + Docs complete |
|  | ★ **MILESTONE: SAUDI LAUNCH** | ~W30 | — | 20 Exit Gate | — | دخول الإنتاج |
| 21 | Gulf/Egypt/Jordan Expansion | W30+ | ~4 أسابيع | 20 | جميع الفرق حسب المجال | Configuration لكل دولة بلا تعديل Core |
| 22 | Global Expansion | W33+ | ~4 أسابيع | 21 | جميع الفرق | Country Packs + adapters محلية |
| 23 | Channel Independence | W36+ | ~3 أسابيع | 22 | 12 + جميع الفرق | Core يعمل عبر Telegram/Web/Mobile |
| 24 | Service Extraction | W38+ | مستمر | 23 | حسب الخدمة | فصل Microservices عند سبب واضح + ADR |

---

## 4. المسار الحرج (Critical Path)

المسار الذي لا يمكن تأخيره دون تأخير الإطلاق:

```text
Phase 00 → 01 → 02 → 04 → 06 → 07 → 09 → 20 (Launch)
```

المراحل **خارج** المسار الحرج يمكن تأخيرها أو إعادة ترتيبها دون تهديد موعد الإطلاق، شأن يعامل عبر ADR.

---

## 5. فرص التوازي (Parallelization)

| المجموعة | المراحل | ملاحظة |
|---|---|---|
| الأساس | 01 + 02 | 02 يبدأ بعد الجزء الأول من 01 (Identity link جاهز). |
| الكور | 04 + 05 | Driver و Customer يعملان بالتوازي بعد 01/02/03. |
| التجاري | 11 + 12 | Search يبدأ بعد أن يكون Catalog جاهزاً من 11. |
| الجاهزية | 18 + 19 | Observability و Security يعملان بالتوازي بعد 17. |

التوازي مُجدول ضمن المدة المقدرة للمجموعة، لا يضيف أسابيع منفصلة.

---

## 6. المعالم (Milestones)

| المعلم | عند | المعنى |
|---|---|---|
| M0 — Foundation Ready | نهاية W1 | المستودع جاهز، CI يعمل، الفرق تستطيع البناء. |
| M1 — Identity Stable | نهاية W3 | المستخدم يُنشأ من Telegram ويبقى مستقراً. |
| M2 — Order Lifecycle | نهاية W10 | Order يتحرك عبر الحالات بلا حالات مستحيلة. |
| M3 — End-to-End Dispatch | نهاية W13 | طلب كامل من عميل إلى إسناد سائق. |
| **M4 — SAUDI LAUNCH** | ~W30 | دخول الإنتاج السعودي. |
| M5 — Regional Expansion | W30+ | الخليج + مصر + الأردن. |
| M6 — Channel Independence | W36+ | Core يعمل عبر قنوات متعددة. |

---

## 7. قاعدة تعديل الخارطة (Roadmap Change Log)

الخارطة ليست «وثيقة تجميد». عند تغيير الترتيب أو المدد أو إدخال مرحلة جديدة:

```text
القرار القديم
    ↓
ADR (docs/15-decisions/)
    ↓
تحديث ROADMAP.md + تسجيل التغيير في سجل التعديلات أدناه
    ↓
تحليل الأثر (Impact Analysis)
    ↓
خطة الانتقال (Migration Plan)
```

**لا تُغيَّر المدد أو الترتيب بلا ADR.** التعديلات المباشرة للجدول دون ADR تعتبر مخالفة.

---

## 8. سجل تعديل الخارطة

| الإصدار | التاريخ | التغيير | المرجع |
|---|---|---|---|
| v1.0 | 2026-08-19 | إنشاء خارطة الطريق الأساسية (Baseline) من أقسام 71–103 | [TASK_LOG](TASK_LOG.md) |
| — | 2026-08-24 | **تحديثُ حالةٍ لا تعديلَ ترتيبٍ**: الطور 10 صار **MR 5/6 مدفوعة** (الأحداثُ وصندوقُ الصادرِ في نفسِ المعاملة · منعُ تكرارٍ يُعيد نفسَ البايتات · مكافأةُ إحالةٍ **مُدّةٌ في الدفتر** · عيبُ الصفِّ البائتِ في التأهّلِ مُصلَحٌ · انحرافُ أجسامِ الكتابةِ مُصلَحٌ · الذرّيّةُ مُثبَتةٌ بمِسْبار). لا مرحلةَ نُقلت ولا تبعيّةَ تغيّرت ولا موعدَ عُدِّل؛ ووثيقةٌ معماريّةٌ جديدة [SUBSCRIPTION_EVENTS](../02-architecture/SUBSCRIPTION_EVENTS.md). **المتبقّي: 6/6 بوابةُ الخروج وإعادةُ تشغيلِ مسارِ HTTP.** |
| — | 2026-08-24 | **تحديثُ حالةٍ لا تعديلَ ترتيبٍ**: الطور 10 صار **MR 4/6 مدفوعة** (طبقةُ التطبيقِ ومعاملةٌ واحدةٌ وHTTP على 8093 · المؤشِّرُ مُشتقٌّ · عيبُ الوثبةِ الوسطى مُصلَحٌ · انحرافُ مفاتيحِ `details` مُصلَحٌ) — [!75](https://gitlab.com/uxxxu/wasla/-/merge_requests/75) · commit `4ab26b62`. لا مرحلةَ نُقلت ولا مدّةَ تغيّرت، فلا يحتاج ADR. | [TASK_LOG](TASK_LOG.md) · [SUBSCRIPTION_HTTP](../04-api/SUBSCRIPTION_HTTP.md) |
| — | 2026-08-24 | **تحديثُ حالةٍ لا تعديلَ ترتيبٍ**: الطور 10 صار **MR 3/6 مدفوعة** (استمراريّةُ Postgres: مُهاجرةٌ تُطبّق العقد · بذرةٌ من الكتالوج · دفترُ إضافةٍ فقط). لا مرحلةَ نُقلت ولا مدّةَ تغيّرت، فلا يحتاج ADR. | [TASK_LOG](TASK_LOG.md) · [SUBSCRIPTION_PERSISTENCE](../02-architecture/SUBSCRIPTION_PERSISTENCE.md) |

---

## 9. ملخص

> **الزمن تقديري للتخطيط. الانتقال الفعلي يتم باجتياز Exit Gate لا بالوقت.** أي تعديل في الترتيب أو المدد يجب أن يمرّ عبر ADR. وهذا التعديل نفسه — كونه مسّ مسار `docs/` و `scripts/` — رافقه إدخال في [TASK_LOG.md](TASK_LOG.md) والتزاماً بقاعدة [PUSH_DOCUMENTATION_RULE](../00-rules/PUSH_DOCUMENTATION_RULE.md).
