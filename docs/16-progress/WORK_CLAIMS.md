# سجل حجز العمل (Work Claims) — إلزامي

**الحالة:** إلزامي · مفروض آليًا · **آخر تحديث:** `2026-08-29`
**المرجع الحاكم:** [`LAUNCH_TO_100_ROADMAP.md`](LAUNCH_TO_100_ROADMAP.md) §0.11 · [`docs/00-rules/WORK_CLAIM_RULE.md`](../00-rules/WORK_CLAIM_RULE.md)

> **الغرض:** أن يكون لكل منطقة عمل مالك واحد معلوم في كل لحظة، فلا تبني جهتان نفس الشيء ولا تتصادم جهتان على نفس الملفات.
>
> **القاعدة:** لا `commit` على أي فرع قبل وجود سطر حجز نشط بالفرع نفسه في جدول «الحجوزات النشطة» أدناه. الدفع الذي يمس ملفًا خارج نطاق حجز فرعه **يُرفض آليًا**.

---

## 1. كيف تحجز

```bash
# 1) ابحث أولًا — إلزامي
bash scripts/checks/find-existing-work.sh "marketplace"

# 2) أضف سطرًا في «الحجوزات النشطة» بنفس ترتيب الأعمدة تمامًا
# 3) تحقق قبل الدفع
bash scripts/checks/verify-governance.sh
```

### قواعد تعبئة الأعمدة

| العمود | القاعدة |
|---|---|
| `Claim ID` | `CLM-NNNN` تسلسلي، لا يُعاد استخدامه أبدًا |
| `Work Item` | معرّف من [`LAUNCH_EXECUTION_BOARD.md`](LAUNCH_EXECUTION_BOARD.md) حصرًا بصيغة `Mx-yy` — لا عمل بلا عنصر |
| `Owner` | `@handle` مسؤول بشري واحد. الوكلاء الآليون: `@handle (agent:<name>)` — المسؤولية تبقى بشرية |
| `Branch` | اسم الفرع الفعلي الكامل. يجب أن يطابق الفرع المدفوع |
| `Scope Paths` | بادئات مسارات مفصولة بفواصل بلا مسافات. مسار المجلد ينتهي بـ `/`. **لا كلام وصفي** |
| `Started` | `YYYY-MM-DD` |
| `Expires` | `YYYY-MM-DD` — الحد الأقصى **14 يومًا** من `Started`. التجاوز يُبطل الحجز |
| `Status` | `Active` أو `Paused` فقط في هذا الجدول |

### مسارات مستثناة من فحص التقاطع

هذه سجلات مشتركة تُلزم القاعدة الجميع بتحديثها، فلا تُحتسب تقاطعًا:

```text
docs/16-progress/TASK_LOG.md
docs/16-progress/LAUNCH_EXECUTION_BOARD.md
docs/16-progress/WORK_CLAIMS.md
docs/16-progress/WORK_INDEX.md
docs/16-progress/MASTER_PROGRESS.md
```

> **⚠ تعارضُ مُعرِّفاتٍ سبَّبتُه أنا، وعلاجُه (2026-08-26):** `CLM-0005` و`CLM-0006` كانا **مأخوذَينِ** على فرعَي `M0-09` و`M0-10` منذ 2026-08-25 بمالكٍ `@uxxxu`. وأنا خصّصتُهما لـ`M5-11` و`M0-12` لأنّي قرأتُ سجلَّ `main` وحدَه ولم أنظر في الفروعِ الأخرى — **وهو نفسُ خطأِ «قراءةِ مصدرٍ واحدٍ والحكمِ بأنّه كلُّ الحقيقة»** الذي كنتُ أُعالجه في `M0-12`. أُعيد ترقيمُ حجزَي الفرعَينِ إلى `CLM-0009` و`CLM-0010` عندَ إنزالِ عملِهما، ولم تُمسَّ `CLM-0005`/`CLM-0006` على `main`. **والحدُّ البنيويُّ باقٍ:** السجلُّ نفسُه ملفٌّ في المستودعِ، ولكلِّ فرعٍ نسختُه — فلا شيءَ يمنع فرعَينِ من تخصيصِ المُعرِّفِ الواحدِ لعنصرَينِ مختلفَين. لا يكشفه فحصٌ يقرأ فرعاً واحداً.
>
> **⚠ تصحيحٌ (`M0-13` — إغلاق):** لا تُسمَّ هذه الخمسةُ في عمودِ النطاقِ إطلاقاً. `is_shared()` يتخطّاها في **فحصِ التقاطعِ وفحصِ الاحتواءِ كليهما**، و`MEANINGFUL` لا يشمل `docs/` أصلاً فلا يبلغُها الفحصُ. تسميتُها في `CLM-0005`/`CLM-0006`/`CLM-0007` كانت **زائدةً لا مُلزَمةً** — وهو وصفٌ خاطئٌ كتبتُه في سجلِّ `M0-12` وصحّحتُه هنا.
>
> **القائمة الحاكمة واحدة في أربعةِ مواضع** (رُصد الموضعُ الرابعُ في `M0-13`: [`WORK_CLAIM_RULE.md`](../00-rules/WORK_CLAIM_RULE.md) §3 نفسُها كانت تُعلن أربعةً بلا `MASTER_PROGRESS.md`). هذه الخمسة **بالحرف** هي `SHARED_LEDGERS` في [`validate-work-claims.sh`](../../scripts/checks/validate-work-claims.sh) و`LEDGER_ONLY_PATHS` في [`require-doc-update.sh`](../../scripts/checks/require-doc-update.sh). أيُّ إضافةٍ هنا تُضاف في الموضعين معاً، وإلّا رُفض دفعُ مَن اتَّبع الوثيقة. (كانت الوثيقةُ تُعلن `MASTER_PROGRESS.md` ولا تُعلن `WORK_INDEX.md`، و`validate-work-claims.sh` يفعل العكس، و`require-doc-update.sh` يُعلن الخمسةَ — ثلاثةُ مواضعَ بثلاثِ قوائم. وُحِّدت في `M0-12` بقرارِ مالكِ البرنامج.)

أي مسار مشترك آخر يحتاجه حجزان في وقت واحد يُحل بتقسيم العنصر أو تسلسله بقرار Program Owner مسجَّل في اللوحة — **لا بتعطيل الفحص**.

---

## 2. الحجوزات النشطة

| Claim ID | Work Item | Owner | Branch | Scope Paths | Started | Expires | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| CLM-0004 | M0-02 | @uxxxu | fix/m0-02-drivers-conformance-zone-seed | services/drivers/,docs/16-progress/ | 2026-08-25 | 2026-09-08 | Active |
| CLM-0029 | M1-03 | @uxxxu (agent:computer) | feat/m1-03-gap-closure | packages/service-auth/,packages/dispatch-e2e/,packages/driver-e2e/,packages/negotiation-e2e/,services/matching/,services/dispatch/,services/drivers/,scripts/,docs/15-decisions/,docs/07-security/,docs/00-rules/,docs/14-runbooks/,pnpm-lock.yaml | 2026-08-31 | 2026-09-14 | Active |
| CLM-0030 | M0-08 | @uxxxu (agent:computer) | feat/m1-03-gap-closure | docs/12-testing/ | 2026-08-31 | 2026-09-14 | Active |
| CLM-0035 | M0-22B | @uxxxu (agent:computer) | chore/m0-22b-gate-closeout | docs/12-testing/,docs/07-security/ | 2026-09-02 | 2026-09-16 | Active |
| CLM-0049 | M0-15 | @uxxxu (agent:computer) | chore/m0-gate-docs-batch5 | docs/12-testing/,docs/07-security/ | 2026-09-02 | 2026-09-16 | Active |
| CLM-0050 | M0-16 | @uxxxu (agent:computer) | chore/m0-gate-docs-batch5 | docs/12-testing/ | 2026-09-02 | 2026-09-16 | Active |
| CLM-0051 | M0-17 | @uxxxu (agent:computer) | chore/m0-gate-docs-batch5 | docs/12-testing/ | 2026-09-02 | 2026-09-16 | Active |
| CLM-0052 | M0-19 | @uxxxu (agent:computer) | chore/m0-gate-docs-batch5 | docs/12-testing/ | 2026-09-02 | 2026-09-16 | Active |

> **`CLM-0049`–`CLM-0052` — دفعةُ وثائقِ البوّاباتِ الخامسةُ، ولمَ تبدأُ الأرقامُ من 49 لا من 36 (2026-09-02):** المعرّفاتُ `CLM-0036`–`CLM-0048` **مستعملةٌ فعلاً** على أربعةِ فروعٍ قائمةٍ لمّا تُدمَج بعدُ (`chore/m0-gate-docs-batch1`…`batch4` · طلباتُ الدمجِ #8 · #9 · #10 · #11)، وقاعدةُ تفرُّدِ المعرّفِ **على مستوى الملفِّ بعدَ الدمجِ** لا على مستوى الفرعِ وحده. فلو بُدِءَ من 36 لوقع **تعارضُ معرّفاتٍ يومَ دمجِ الدفعاتِ السابقةِ** — والفراغُ مُعلَنٌ مقصودٌ لا قفزةٌ ساهيةٌ. **ولا تقاطعَ يُرفَضُ مع `CLM-0035` على `docs/12-testing/` و`docs/07-security/`** لأنّ `owner_party()` تقرأُ الطرفَينِ `@uxxxu`، ويُكتبُ ذلك صراحةً لأنّ سكوتَ الفحصِ ليس إذناً. **و`docs/07-security/` في نطاقِ `CLM-0049` وحدها** لأنّ الثغرةَ المقيسةَ في `M0-15` أوجبت خطراً مُسجَّلاً (`RISK-0016`)، والدفعةُ واحدةٌ لا دفعتانِ (قاعدةُ التوثيقِ معَ الدفعِ). **ولا يُدَّعى إغلاقٌ:** الأربعةُ تبقى `Ready for Gate`، والنقلُ إلى `Completed` سلطةُ مالكِ البرنامجِ وحدَه.

> **`CLM-0033` — حجزُ `M0-22A`، ونطاقُه يُفسَّرُ لا يُسرَد (2026-09-01):** `docs/00-rules/` لأنّ قاعدةَ «`Completed` لا تُمنَحُ إلّا باجتماعِ Implementation + Verification + Gate» قرارُ حوكمةٍ ثبَّتَه المالكُ ويجبُ أن يسكنَ القاعدةَ لا سجلَّ المهامِّ وحدَه · و`docs/07-security/` لإغلاقِ `RISK-0001` وإعادةِ صياغةِ `RISK-0002` على ما يُغلقُه فعلاً · و`docs/15-decisions/` لقرارِ إجبارِ الفحوصِ · و`docs/12-testing/ci-evidence/` لدليلِ الرفضِ المتعمَّدِ. **والعنصرُ `M0-22A` مُسجَّلٌ في اللوحةِ قبلَ الحجزِ لا بعدَه**، لأنّ مدقِّقَ الحجوزاتِ يرفضُ حجزاً لعنصرٍ لا وجودَ له.

> **`CLM-0035` — حجزُ إقفالِ بوّابةِ `M0-22B`، ولمَ لا يُرفَضُ بتقاطعٍ (2026-09-02):** `CLM-0030` نشطةٌ على `docs/12-testing/` عينِها لعنصرٍ آخرَ (`M0-08`) وعلى فرعٍ آخرَ. **ولا تقاطعَ يُرفَضُ لأنّ الجهةَ المسؤولةَ واحدةٌ:** `owner_party()` في [`validate-work-claims.sh`](../../scripts/checks/validate-work-claims.sh) تقصُّ ` (agent:…)` فتقرأ الطرفَينِ `@uxxxu`، و«نفس الجهة: مسؤوليتها». **ويُكتَبُ هذا صراحةً لا يُترَكُ للفحصِ وحدَه**، لأنّ سكوتَ الفحصِ ليس إذناً: التداخلُ حقيقيٌّ في المسارِ، والذي يُبيحُه أنّ المالكَ واحدٌ ويعرفُ ما على الفرعَينِ. **ولا يُحجَزُ `M0-22B` مرّتَينِ:** `CLM-0034` تُحرَّرُ في §3 في هذه الدفعةِ نفسِها قبلَ إدراجِ هذا السطرِ. و`docs/07-security/` في النطاقِ **لتضييقِ `RISK-0007` بالقياسِ** (البندُ 11 من البوّابةِ) لا لتوسّعٍ مفتوح.

> **⚠️ انحرافٌ عن [`WORK_CLAIM_RULE.md` §6](../00-rules/WORK_CLAIM_RULE.md) يُعلَنُ ولا يُطوى — توسيعُ نطاقِ `CLM-0033` بعدَ بدءِ الكتابة (2026-09-01):** وُسِّع النطاقُ من `docs/12-testing/ci-evidence/` إلى `docs/12-testing/` كاملاً. **والسببُ سلسلةٌ ماديّةٌ لا رغبةٌ في توسّع:** إغلاقُ `RISK-0001` أنقصَ العدّادَ `static.risks_not_closed` من 15 إلى 14، فأخفقَ **الفحصُ 11** (الأساسُ الآليُّ · `M0-08`) لأنّ الحالةَ الحيّةَ خالفت `docs/12-testing/BASELINE.json` المُلتزَم. فالملفّانِ مقترنانِ: **لا يمكنُ إغلاقُ الخطرِ دونَ تحريكِ الأساس**، وأيُّ فرعٍ يفعلُ الأوّلَ يجبُ أن يفعلَ الثاني وإلّا رُفضت دفعتُه.
>
> **والطرقُ الثلاثُ في §6 مسدودةٌ كلُّها**، والسدُّ مقيسٌ لا مفترَض: التوسيعُ ممنوعٌ بعدَ بدءِ الكتابةِ · وحجزٌ ثانٍ لـ`M0-22A` يرفضه فحصُ التكرار · وحجزٌ ثانٍ لـ`M0-08` يرفضه كذلك لأنّ `CLM-0030` **ما زال نشطاً على فرعٍ حيٍّ** — سُئل المستودعُ عن `feat/m1-03-gap-closure` فأجابَ `200` لا `404`، فلا يُحرَّرُ حجزُه بحجّةِ الدمج. **وهذا هو عينُ عيبِ الحارسِ المُسجَّلِ عنصرَ عملٍ `M0-20`**: فحصُ التكرارِ يقرأُ عنصرَ العملِ ولا يقرأُ عمودَ المالكِ، فيُعامِلُ جهةً واحدةً على فرعَينِ معاملةَ جهتَين.
>
> **وما لم يُفعَل:** لم يُعطَّل الفحصُ 11، ولم يُستثنَ `BASELINE.json` من الحارسِ، ولم يُحرَّر رقمٌ بيدٍ لإسكاتِ الفرقِ. أُعيد توليدُ الأساسِ بـ`scripts/baseline.sh` من **سجلِّ تحقُّقٍ كاملٍ أخضرَ** (بصمةُ `sha256:ae902babc8bb9632ea65e171711889cda4539fc0d36a428df10ba56faf48f149` — وقد جاءت مطابقةً للبصمةِ المحسوبةِ يدويّاً بـ`baseline_canon.fingerprint`، وهو تدقيقٌ متقاطعٌ لا مصادفة). **والانحرافُ يُرفَعُ إلى مالكِ البرنامجِ للعلمِ لا للاستئذانِ بأثرٍ رجعيٍّ** — والتقاطعُ لا يضرُّ اليومَ لأنّ المالكَ واحدٌ، وكان سيُوجِبُ تقسيمَ العنصرِ لو دخلَ مالكٌ ثانٍ.

> **`CLM-0032` (مُحرَّرٌ الآن) — ولمَ حجزٌ ثانٍ لا تمديدُ `CLM-0031` (2026-09-01):** بعدَ دمجِ [PR #2](https://github.com/skyosv10-art/wasla/pull/2) لزِمَ فرعٌ ثانٍ لحفظِ دليلِ التشغيلِ، **فرفضَته البوّابةُ بحقٍّ**: دُفعَ الفرعُ `feat/m0-21-ci-evidence` بلا حجزٍ فأجابَ التشغيلُ [`33499994547`](https://github.com/skyosv10-art/wasla/actions/runs/33499994547) بـ`✗ رفض حجز العمل: لا حجز نشط للفرع «feat/m0-21-ci-evidence»`. **وهذا الرفضُ يُسجَّلُ لا يُطوى:** هو أوّلُ دليلٍ على أنّ الحارسَ **يمنعُ فعلاً** في الخطِّ الجديدِ لا في المحلّيِّ وحدَه — وهو بالضبطِ ما لم يكن يُثبَتُ حينَ لم تكن الوظائفُ تبدأ. والحلُّ كان تسجيلَ الحجزِ لا تعطيلَ الفحص. ولم يُمدَّد `CLM-0031` لأنّ الحجزَ يرتبطُ بفرعٍ واحدٍ بنصِّ [`WORK_CLAIM_RULE.md`](../00-rules/WORK_CLAIM_RULE.md)، ولا يوجدُ حجزانِ نشطانِ للعنصرِ نفسِه — فحُرِّر الأوّلُ إلى [§3](#3-الحجوزات-المحرَّرة-released) بدليلِ دمجِه ثمّ فُتح الثاني.

> **`CLM-0031` (مُحرَّرٌ الآن) — تقاطعُ نطاقٍ مقصودٌ مع `CLM-0029`، ولمَ لم يرفضه الفحصُ (2026-09-01):** يشملُ نطاقُ هذا الحجزِ `docs/15-decisions/` و`docs/07-security/` وهما داخلُ نطاقِ `CLM-0029` النشطِ. والفحصُ الرابعُ في [`WORK_CLAIM_RULE.md` §3](../00-rules/WORK_CLAIM_RULE.md) يرفضُ التقاطعَ **«لمالكٍ مختلف»** — والمالكُ هنا واحدٌ (`@uxxxu (agent:computer)`)، فلا رفض. **ويُكتَبُ هذا صراحةً لا صمتاً:** التقاطعُ قائمٌ فعلاً، وسلامتُه من وحدةِ المالكِ لا من غيابِ التقاطع. ولو دخلَ مالكٌ ثانٍ على `docs/15-decisions/` غداً لَوجبَ تقسيمُ العنصرِ أو تسلسلُه بقرارِ مالكِ البرنامج. **و`.github/` ليست في `MEANINGFUL`** ([`lib/meaningful-paths.sh`](../../scripts/checks/lib/meaningful-paths.sh))، فلا يُوجِبُ تعديلُها حجزاً ولا يفحصُ المدقِّقُ احتواءَها — **وقد سُمِّيت في النطاقِ رغمَ ذلك** إعلاناً للنيّةِ لا استيفاءً لشرطٍ. وهذا حدٌّ حقيقيٌّ مُسجَّلٌ في [ADR-023](../15-decisions/ADR-023-ci-migration-to-github-actions.md) §5 ونطاقُ علاجِه `M0-22`: ملفُّ خطِّ CI الجديدُ اليومَ **غيرُ محروسٍ** لا بقاعدةِ الحجزِ ولا بأبوابِ `validate-ci-mandatory.sh`.

> **لمَ حجزانِ جديدانِ لا إحياءُ `CLM-0027`/`CLM-0028` (2026-08-31):** الحجزانِ السابقانِ كانا على فرعِ `feat/m1-03-service-identity`، **وقد دُمج وحُذف** — سُئل المستودعُ عنه فأجاب `404 Branch Not Found`، فبقاؤهما `Active` يُصيِّر الفحصَ الرابعَ (بياتُ الحجوزاتِ) أحمرَ ويُقفِل مساراتٍ على لا أحدٍ. فنُقلا إلى [§3](#3-الحجوزات-المحرَّرة-released) بدليلِهما، وأُنشئ حجزانِ جديدانِ على فرعِ الدفعةِ الجديدِ. **والنطاقُ أوسعُ بصدقٍ لا بصمتٍ:** إنفاذُ الهُويّةِ على حدٍّ حقيقيٍّ يمسّ `services/matching/` (المتحقِّقُ) و`services/dispatch/` و`services/drivers/` (الموقِّعانِ) وثلاثَ حزمِ إثباتٍ من طرفٍ إلى طرفٍ، وحارسُ التغطيةِ يمسّ `scripts/`، وقرارا التدويرِ والإعادةِ يمسّانِ `docs/15-decisions/` و`docs/14-runbooks/` و`docs/00-rules/`، و`RISK-0015` يمسّ `docs/07-security/`. وكُتب هذا **قبلَ** الالتزامِ لا بعدَه.

> **حاجزٌ أُعلِن ثمّ انحلَّ بالدمجِ، ويُكتب بشوطَيه لا بنتيجتِه وحدَها (2026-08-30):** أوجبت قاعدةُ `M0-08` إعادةَ توليدِ `docs/12-testing/BASELINE.json` معَ دفعةِ `M1-02` (تحرَّك عدّادانِ: ملفّاتُ الاختبارِ المتتبَّعةُ 252→255 · وبصمةُ `pnpm-lock.yaml`). وحينَ كُتبت الدفعةُ كان فرعُها **مُكدَّساً** فوقَ فرعِ `M1-01` غيرِ المدموجِ، و`CLM-0022` لـ`M0-08` نشطةً على الفرعِ الأبِ — **فسُدَّت الطرقُ الثلاثُ في [§6](../00-rules/WORK_CLAIM_RULE.md) كلُّها**: التوسيعُ ممنوعٌ بعدَ بدءِ الكتابةِ · وحجزٌ ثانٍ لـ`M1-02` رفضَته البوابةُ («محجوز مرتين في وقت واحد») · وحجزٌ ثانٍ لـ`M0-08` رفضَته كذلك لأنّ فحصَ التكرارِ يقرأ عنصرَ العملِ **ولا يقرأ عمودَ المالكِ** فيُعامِل جهةً واحدةً على فرعَينِ مُكدَّسَينِ معاملةَ جهتَين. فأُخرِج الأساسُ من الدفعةِ **وأُعلِن الإخفاقُ** ولم يُعطَّل فحصٌ ولم يُحرَّر رقمٌ بيدٍ. ثمّ **دُمج [MR !114](https://gitlab.com/uxxxu/wasla/-/merge_requests/114) بقرارِ مالكِ البرنامجِ** فحُذف الفرعُ الأبُ وحُرِّرت `CLM-0022`، فصار `M0-08` حرّاً وأُعيد الحجزُ بـ`CLM-0026` والتُزم الأساسُ. **والعيبُ في الحارسِ باقٍ بعدَ انحلالِ حالتِه** — مُسجَّلٌ عنصرَ عملٍ `M0-20` في اللوحةِ بحلٍّ مقترحٍ لا مُنفَّذٍ؛ ولم يُصلَح في دفعتِه لأنّ `scripts/checks/` خارجُ نطاقِ `CLM-0025`، **ولأنّ إصلاحَ حارسٍ ليمرَّ قياسُ مَن يُصلحه هو عينُ ما تمنعه قاعدةُ صدقِ القياس.**

> **لمَ حُرِّرت `CLM-0024` وأُعيد الحجزُ بـ`CLM-0025` (2026-08-30):** بوابةُ M1-02 من طرفٍ إلى طرفٍ تحتاج أن تصلَ `telegram-adapter` بـ`services/identity` في مجموعةٍ واحدة، و`@wasla/channel-e2e` هي **الحزمةُ الوحيدةُ المسموحُ لها بذلك** ([ADR-007](../15-decisions/ADR-007-telegram-channel-adapter-isolation-and-stack.md)) — والبديلُ، وضعُ البوابةِ في `services/identity`، يمنعه [ADR-019](../15-decisions/ADR-019-human-session-lifecycle-and-init-data-verification.md) (القرار 1). فاحتاجَ النطاقُ `packages/channel-e2e/` و`pnpm-lock.yaml` (أضافت `services/identity` تبعيّةَ `@wasla/auth-sdk`) بعدَ بدءِ الكتابةِ. **وأوّلُ محاولةٍ كانت خاطئةً وأسقطتها البوابةُ:** أضفتُ `CLM-0025` **إلى جانبِ** `CLM-0024` بحُجّةِ «حجزٌ ثانٍ» في [`WORK_CLAIM_RULE.md` §6](../00-rules/WORK_CLAIM_RULE.md)، فرفضَ `validate-work-claims.sh` الدفعَ بنصِّه: «عنصر العمل «M1-02» محجوز مرتين في وقت واحد». والفحصُ محقٌّ وقراءتي كانت ناقصةً: الخانةُ تُجيز حجزاً ثانياً **لعنصرِ عملٍ ثانٍ** (كما `CLM-0022` لـ`M0-08`)، وتُجيز لعنصرٍ واحدٍ بديلاً آخرَ صريحاً هو «يُحرَّر الأولُ ويُعاد الحجزُ بنطاقٍ أوسعَ بعدَ التأكُّدِ من خلوِّه». فهذا ما جرى: نُقلت `CLM-0024` إلى المحرَّرةِ، و`CLM-0025` تحمل الاتّحادَ. ولم تُوسَّع `CLM-0024` في مكانِها حرفاً واحداً.

> **لمَ حجزانِ على فرعٍ واحدٍ لا واحدٌ موسَّعٌ (2026-08-30):** أوجب `M0-08` إعادةَ توليدِ `docs/12-testing/BASELINE.json` **معَ** هذه الدفعةِ لا بعدَها (أربعةُ عدّاداتٍ ساكنةٍ تغيّرت: الحزمُ 41→42 · ملفّاتُ الاختبارِ 249→252 · المخاطرُ 13→14 · وبصمةُ ملفِّ القفل). والملفُ خارجَ نطاقِ `CLM-0021`، **فرفضه الفحصُ الثاني فعلاً** لا افتراضاً. و[`WORK_CLAIM_RULE.md` §6](../00-rules/WORK_CLAIM_RULE.md) تمنع توسيعَ سطرٍ قائمٍ بعدَ بدءِ الكتابةِ وتأمر بـ«حجزٍ ثانٍ لعنصرِ عملٍ ثانٍ» — فهذا هو، ولم يُحرَّر عمودُ النطاقِ في `CLM-0021` حرفاً واحداً.

> **حالةُ `CLM-0020` عندَ نهايةِ جلسةِ 2026-08-29:** الحجزُ يبقى **`Active`**. أُغلق في هذه الجلسةِ أكبرُ بندٍ مُعلَنٍ في «ما لم يُتحقَّق منه»: **شُغِّلت مجموعةُ تكاملِ خدمةِ السوقِ على PostgreSQL 17 حقيقيٍّ — عشرةُ ملفّاتٍ · 118 اختباراً ناجحاً · 0 فاشلٍ**، بعدَ إحضارِ المحرّكِ بوصفةِ [LOCAL_POSTGRES_FOR_TESTS.md](../14-runbooks/LOCAL_POSTGRES_FOR_TESTS.md). والتشغيلُ **لم يُصدِّق ما كُتب بل أسقطَ عيباً حقيقيّاً** في ترتيبِ الصندوقِ (`RISK-0012`) أُصلح وثُبِّت بتأكيدٍ على سببِه. وتبقى **المراجعةُ 6/6** (بوّابةُ خروجِ الطورِ) فالحجزُ لا يُحرَّر.

> **تحريرُ `CLM-0020` — 2026-08-29 (إقفالُ الدورةِ، البروتوكول §8.1):** تحقَّق الشرطُ المكتوبُ في الملاحظةِ أدناه («لا يُنقَل إلى §3 حتى دمجِ طلبِ الدمجِ السادسِ»): دُمج [MR !112](https://gitlab.com/uxxxu/wasla/-/merge_requests/112) في `main`، فنُقل الصفُّ إلى §3 بدليلِه. **والبيتُ مقيسٌ لا مُستنتَجٌ:** شُغِّل `scripts/checks/validate-claim-freshness.sh` على شجرةِ `main` عندَ `0a21f29b` بعدَ تجسيدِ قائمةِ فروعِ المستودعِ الحقيقيّةِ (12 فرعاً، قُرئت من واجهةِ GitLab لأنّ git-over-HTTP لا يقبل التوثيقَ بتوكن حاملٍ في هذه البيئة، والمقيسُ **وجودُ الفرعِ لا محتواه**)، فأجاب الفحصُ: `CLM-0004` **حيٌّ** (`fix/m0-02-drivers-conformance-zone-seed` موجودٌ) و`CLM-0020` **بائتٌ** — أي أنّ `main` كانت **تُخفِق في بوّابتِها** (الفحصُ الرابعُ فاشلٌ، رمزُ الخروجِ 1) قبلَ هذا الإقفال، وهذه الدفعةُ هي ما يُعيدها خضراءَ. **وانحرافٌ يُذكَر ولا يُصحَّح رجعيّاً:** عمودُ `Branch` بقي `feat/m5-11-marketplace-outbox-inventory-sync` (فرعُ المراجعةِ 5/6) بينما جرت المراجعةُ 6/6 على `feat/m5-11-marketplace-exit-gate` — كلاهما محذوفٌ اليومَ (`404`)، والسجلُّ يُبقى على ما كُتب ويُعلَن الانحرافُ في صفِّ §3.

> **حالةُ `CLM-0020` عندَ نهايةِ جلسةِ 2026-08-29 (بعدَ المراجعةِ 6/6):** الحجزُ يبقى **`Active`** ولا يُنقَل إلى §3 حتى دمجِ طلبِ الدمجِ السادسِ. المراجعاتُ الستُّ كلُّها كُتبت وبوّابةُ الخروجِ خضراءُ محلّيّاً (**6/6 · 0 فاشلٌ**)، **لكنّ نقلَ `M5-11` إلى `Completed` قرارُ مالكِ البرنامجِ لا قرارُ عاملٍ** — فلا أُغلقه ولا أُطلق الحجزَ بحجّةِ «انتهى الطور». ويُذكَر ما يبقى مفتوحاً على هذا الطورِ بعدَ الدمج: `RISK-0012` (برهانُ الترتيبِ التامُّ بعدَّادٍ متزايدٍ — يمسّ شكلَ الصندوقِ المُوحَّدَ في خمسةِ أطوارٍ) و`RISK-0013` (تسلسلٌ قانونيٌّ لأجسامِ الإعادةِ أو عمودٌ `TEXT` — كلاهما يلزمه قرارٌ معماريٌّ)، وكلاهما **مُسجَّلٌ لا مُنفَّذٌ** بقصد.
>
> **حالةُ `CLM-0020` عندَ نهايةِ جلسةِ 2026-08-28:** الحجزُ يبقى **`Active`** ولا يُنقَل إلى §3 — العملُ المُنجَزُ هو **المراجعةُ 5/6 من ستٍّ**، والفرعُ حيٌّ للمراجعةِ 6/6 (إعادةُ تشغيلِ مسارِ HTTP وبوّابةُ خروجِ الطور). وتحريرُ الحجزِ إنّما يكون بعدَ الدمجِ **وانتهاءِ العملِ في نطاقِه**، لا بعدَ أوّلِ دمجٍ فيه — وإلّا أُقفِل نطاقٌ ما زال يُعمَل فيه أو فُتِح لغيرِ صاحبِه. والمقيسُ على الفرعِ: البوّابةُ **عشرةٌ ناجحةٌ · 0 فاشلٌ · تخطّيانِ مُعلَنانِ · رمزُ الخروجِ 0**.

> **توسيعُ نطاقِ `CLM-0018` (نفسُ اليومِ، نفسُ الجهةِ):** كُتب النطاقُ أوّلاً `scripts/checks/` ثمَّ وُسِّع إلى `scripts/` لأنّ تسجيلَ الفحصِ العاشرِ يقتضي تحديثَ تسميةِ البوّابةِ في [`scripts/verify.sh`](../../scripts/verify.sh) (9 → 10 فحوصٍ) وهو خارجَ `scripts/checks/`. ويُذكر التوسيعُ ولا يُخفى: تحريرُ النطاقِ صامتاً بعدَ بدءِ العملِ هو عينُ ما تمنعه القاعدةُ.

> **توسيعُ نطاقِ `CLM-0020` مرّةً ثانيةً 2026-08-29 (يُذكر ولا يُخفى):** أُضيف `packages/marketplace-e2e/` و`.gitlab-ci.yml` للمراجعةِ **6/6** — بوّابةِ خروجِ الطور. والسببُ مكتوبٌ في [PHASE11_EXIT_GATE_E2E.md §2](../12-testing/PHASE11_EXIT_GATE_E2E.md): البوّابةُ لا تسكن `services/marketplace/` لأنّ حارسَ نقاءِ الخدمةِ يقفل اعتمادَها على أربعةِ أسماءٍ حرفيّاً، ولأنّ بوّابةً تركض داخلَ `test:integration` مع 118 اختباراً يُنسَب فشلُها إلى وظيفةٍ تفحص شيئاً آخر. و`.gitlab-ci.yml` لازمٌ لا اختيارٌ: بوّابةٌ بلا وظيفةٍ تُشغِّلها ليست بوّابةً بل ملفٌّ يُتخطّى بغيابِ `DATABASE_URL` — والملاحظةُ في §73 أدناه («ولا يدخل `.gitlab-ci.yml`») كانت صادقةً للمراجعةِ 5/6 وبطلت بالمراجعةِ 6/6، فتُصحَّح هنا صراحةً لا تُحذَف. وسابقةُ `CLM-0018` تُتبَع حرفاً: التوسيعُ يُعلَن قبلَ الدفعِ لا يُكتشَف في المراجعة.

> **توسيعُ نطاقِ `CLM-0020` 2026-08-29 (يُذكر ولا يُخفى):** أُضيف `docs/07-security/` بعدَ بدءِ العمل. والسببُ ليس راحةً: أوّلُ تشغيلٍ لمجموعةِ التكاملِ على PostgreSQL حقيقيٍّ أسقطَ **عيباً في ترتيبِ صندوقِ الصادرِ** نطاقُه أوسعُ من الطورِ 11 — شكلُ الصندوقِ مُوحَّدٌ بينَ خمسِ خدماتٍ بقرارٍ مُعلَن. وقاعدةُ المستودعِ أنّ الدَينَ الذي يتجاوز نطاقَ الحجزِ **يُسجَّل خطراً بمالكٍ ومهلةٍ** لا يُكتب في سجلِّ عملٍ ويُنسى، فصار [`docs/07-security/RISK_REGISTER.md`](../07-security/RISK_REGISTER.md) لازماً (`RISK-0012`). وتحريرُ النطاقِ صامتاً بعدَ بدءِ العملِ هو عينُ ما تمنعه القاعدةُ — فسابقةُ `CLM-0018` أعلاه تُتبَع حرفاً.

> **نطاقُ `CLM-0020` ولمَ هو ثلاثةُ مساراتٍ لا واحد:** `services/marketplace/` محلُّ العملِ، و`docs/02-architecture/` لوثيقةِ الأحداثِ (سابقةُ `SUBSCRIPTION_EVENTS.md`)، و`docs/12-testing/` **لازمٌ لا اختيارٌ**: أيُّ ملفِّ اختبارٍ جديدٍ يُغيرُّ عدَّاد `test_files` في `BASELINE.json`، والبابُ الثاني من الفحصِ الحادي عشرَ (M0-08) **يُسقِط البوّابةَ** حتّى يُعاد توليدُ الأساسِ — فإقصاءُ المسارِ كان سيجعل العملَ غيرَ قابلٍ للدفعِ أصلاً. ~~ولا يدخل `.gitlab-ci.yml`: وظيفةُ `marketplace-db-integration` قائمةٌ من المراجعةِ 3/6.~~ **بطلَ هذا السطرُ في المراجعةِ 6/6** — البوّابةُ تحتاج وظيفةً جديدةً بقاعدةٍ مستقلّةٍ (`marketplace-exit-gate-e2e`)، والتفصيلُ في ملاحظةِ التوسيعِ الثانيةِ أعلاه.

> **`CLM-0002` حُرِّر بقرارِ مالكِ البرنامجِ 2026-08-27** ونُقل إلى §3 (رُصد بائتاً في `M0-15`، وأُغلق في `M0-17`). وفي المقابل `CLM-0004` (M0-02) **نشطٌ بحقٍّ**: فرعُه قائمٌ ومتقدِّمٌ على `main` بالتزامَين، وفيه فرقٌ حقيقيٌّ في `services/drivers/src/__tests__/repository.integration.test.ts` — أي عملٌ لم يُدمَج بعد.

---

## 3. الحجوزات المحرَّرة (Released)

| Claim ID | Work Item | Owner | Branch | Released | سبب التحرير |
| --- | --- | --- | --- | --- | --- |
| CLM-0034 | M0-22B | @uxxxu (agent:computer) | feat/m0-22b-remaining-jobs | 2026-09-02 | **دُمج في `main` عبر [PR #5](https://github.com/skyosv10-art/wasla/pull/5)** بقرارِ مالكِ البرنامجِ (`merged: true`، `merged_at: 2026-09-01T23:10:34Z`، الدمجُ `0a7524705bc06ac13b1a9077584bc369e6d3fa68`)، وأثمرَ على `main` التشغيلَ [`33569807797`](https://github.com/skyosv10-art/wasla/actions/runs/33569807797): **26 وظيفةً · 26 `started_at ≠ null` · 26 `success`** ([الدليلُ الخام](../12-testing/ci-evidence/2026-09-02T023000Z-m0-22b-main-run/README.md)). **وتُحقِّق من نتيجةِ الدمجِ لا من الفرعِ:** `git diff --stat 9afd9d5 0a75247` أجابَ **صفرَ ملفّاتٍ** — فما دُمج هو ما قِيس بعينِه. **تحريرٌ لأنّ الفرعَ انتهى، لا لأنّ العنصرَ اكتمل:** بوّابةُ [`M0-22B_GATE.md`](../12-testing/M0-22B_GATE.md) حكمُها **`BLOCKED`** ببندِها الثاني عشرَ (`required_status_checks` محجوبٌ بقيدِ خطّةِ حسابٍ في `M0-22A`)، فالعنصرُ يبقى `Ready for Gate` ولا يُرفَعُ إلى `GATED` ولا `Completed`. وبقاءُ السطرِ `Active` على فرعٍ مدموجٍ يُخالف البروتوكول §8.1 ويُصيِّر بياتَ الحجوزاتِ أحمرَ، فحُرِّر. والعملُ المتبقّي (وثيقةُ البوّابةِ ودليلُ `main`) محجوزٌ من جديدٍ بـ`CLM-0035`. |
| CLM-0033 | M0-22A | @uxxxu (agent:computer) | feat/m0-22a-branch-protection | 2026-09-02 | **دُمج في `main` عبر [PR #4](https://github.com/skyosv10-art/wasla/pull/4)** (`merged: true`، `merged_at: 2026-09-01T22:22:38Z`). كان السطرُ مُعلَّماً `Released` في §2 ولم يُنقَل، فنُقل هنا إلى موضعِه الصحيحِ — **نقلُ موضعٍ لا تغييرُ حكمٍ**. **والعنصرُ `M0-22A` يبقى `Blocked` لا محرَّراً منه:** ما دُمج هو **دليلُ الرفضِ** لا الحمايةُ — `PUT …/branches/main/protection` أجابَ `403 · Upgrade to GitHub Pro or make this repository public` ([الدليلُ الخام](../12-testing/ci-evidence/2026-09-01T220000Z-m0-22a-branch-protection/README.md))، و`RISK-0002` مفتوحٌ، ورفعُ الحجزِ قرارُ مالكٍ (ترقيةٌ · نشرُ المستودعِ · تأجيلٌ مُعلَنٌ). |
| CLM-0032 | M0-21 | @uxxxu (agent:computer) | feat/m0-21-ci-evidence | 2026-09-01 | **دُمج في `main` عبر [PR #3](https://github.com/skyosv10-art/wasla/pull/3)** (`merged: true`، الالتزامُ `2cc234a5c6f600d1de7a168cc5fb5d02019710a3`) بعدَ تشغيلٍ أخضرَ [`33501034356`](https://github.com/skyosv10-art/wasla/actions/runs/33501034356). وبه اكتملَ دليلُ `M0-21` **وأُغلق العنصرُ `Completed` بقرارِ مالكِ البرنامج**، فلم يبقَ على الفرعِ عملٌ ويُحرَّرُ الحجز. |
| CLM-0031 | M0-21 | @uxxxu (agent:computer) | feat/m0-21-github-actions-ci | 2026-09-01 | **دُمج في `main` عبر [PR #2](https://github.com/skyosv10-art/wasla/pull/2)** بقرارِ مالكِ البرنامجِ (`merged: true`، الالتزامُ `fce1f054083965ff533be284948e4e1ecce6ec88`)، وأثمرَ التشغيلَ [`33498726052`](https://github.com/skyosv10-art/wasla/actions/runs/33498726052) على `main` بستِّ وظائفَ خضراءَ. فتُرك الفرعُ ولم يعُد يُكتَبُ عليه، والحجزُ يُحرَّرُ لئلّا يبقى `Active` على فرعٍ منتهٍ فيُصيِّر بياتَ الحجوزاتِ أحمرَ. |
| CLM-0027 | M1-03 | @uxxxu (agent:computer) | feat/m1-03-service-identity | 2026-08-31 | **دُمج في `main` عبر [MR !117](https://gitlab.com/uxxxu/wasla/-/merge_requests/117)** بقرارِ مالكِ البرنامجِ (`merged_by: uxxxu`، `merged_at: 2026-08-30T12:42:39.108Z`، رأسُ الطلبِ `2db0dc71`، الدمجُ `cd0d7409`) **وحُذف الفرعُ** — سُئل المستودعُ عنه فأجاب `404 Branch Not Found`. **ودُمج بلا دليلِ خطٍّ، وهذا يُكتب لا يُطوى:** لا وظيفةَ واحدةَ بدأت على هذا الطورِ كلِّه (`ci_quota_exceeded` · `started_at: null` · `RISK-0001`)، فالدمجُ تجاوزٌ واعٍ قرَّره مالكُ البرنامجِ لا خطٌّ أخضرُ. **تحريرُ نطاقٍ لا إغلاقُ عنصرٍ:** `M1-03` يبقى `Ready for Gate`، **وقد رُصدت فيه أربعُ فجواتٍ حقيقيّةٍ بعدَ الدمجِ** (إعادةُ الإرسالِ · تدويرُ المفاتيحِ · الإنفاذُ على النظامِ · دليلُ CI) تُعالَج في `CLM-0029` — فالدمجُ لم يكن إغلاقاً، والسجلُّ يقول ذلك بلا تجميل. |
| CLM-0028 | M0-08 | @uxxxu (agent:computer) | feat/m1-03-service-identity | 2026-08-31 | **دُمج في `main` عبر [MR !117](https://gitlab.com/uxxxu/wasla/-/merge_requests/117)** (نفسُ الدمجِ `cd0d7409`) **وحُذف الفرعُ** (`404 Branch Not Found`). والأساسُ `docs/12-testing/BASELINE.json` مُلتزَمٌ في `main`، وأيُّ زحفٍ لاحقٍ يُقاس منه — وقد زحفَ فعلاً في هذه الدفعةِ (ملفّاتُ اختبارٍ ومخاطرُ ووثائقُ قرارٍ)، فأُعيد الحجزُ بـ`CLM-0030` وأُعيد توليدُ الأساسِ بـ`scripts/baseline.sh` لا بيدٍ. **تحريرُ نطاقٍ لا إغلاقُ عنصرٍ:** `M0-08` يبقى `Ready for Gate`. |
| CLM-0025 | M1-02 | @uxxxu (agent:computer) | feat/m1-02-telegram-session-verification | 2026-08-30 | **دُمج في `main` عبر [MR !115](https://gitlab.com/uxxxu/wasla/-/merge_requests/115)** بقرارِ مالكِ البرنامجِ (`merged_by: uxxxu`، `merged_at: 2026-08-30T10:31:08.318Z`، رأسُ الطلبِ `fdb36a81`، الدمجُ `e44210a3`). **وتُحقِّق من نتيجةِ الدمجِ لا من الفرعِ:** قُوبل رأسُ الطلبِ بشجرةِ `main` عندَ `e44210a3` فكان الفرقُ **صفرَ ملفّاتٍ** — فما دُمج هو ما قِيس بعينِه. **ودُمج بلا دليلِ خطٍّ، وهذا يُكتب لا يُطوى:** خطُّ [`#2803202939`](https://gitlab.com/uxxxu/wasla/-/pipelines/2803202939) على الدمجِ نفسِه قِيس بعدَه فكان **26 وظيفةً كلُّها `ci_quota_exceeded` وكلُّها `started_at: null`**، وقبلَه خطُّ الفرعِ [`#2803170194`](https://gitlab.com/uxxxu/wasla/-/pipelines/2803170194) **23 وظيفةً بالحالِ نفسِه** — فلا وظيفةٌ واحدةٌ بدأت. فالدمجُ **تجاوزٌ واعٍ لـ§56 قرَّره مالكُ البرنامجِ وهو صاحبُ القرارِ فيه**، لا خطٌّ أخضرُ ولا دليلٌ يُقرأ كأنّه أخضرُ (`RISK-0001`). **تحريرُ نطاقٍ لا إغلاقُ عنصرٍ**، ونقلُ العنصرِ إلى `Completed` قرارُ مالكِ البرنامجِ (البروتوكول §9)، ويبقى **مشروطاً بدليلِ خطٍّ لم يوجد بعدُ**. `M1-02` يبقى `Ready for Gate` وإن دُمج. |
| CLM-0026 | M0-08 | @uxxxu (agent:computer) | feat/m1-02-telegram-session-verification | 2026-08-30 | **دُمج في `main` عبر [MR !115](https://gitlab.com/uxxxu/wasla/-/merge_requests/115)** بقرارِ مالكِ البرنامجِ (`merged_by: uxxxu`، `merged_at: 2026-08-30T10:31:08.318Z`، رأسُ الطلبِ `fdb36a81`، الدمجُ `e44210a3`). **وتُحقِّق من نتيجةِ الدمجِ لا من الفرعِ:** قُوبل رأسُ الطلبِ بشجرةِ `main` عندَ `e44210a3` فكان الفرقُ **صفرَ ملفّاتٍ** — فما دُمج هو ما قِيس بعينِه. **ودُمج بلا دليلِ خطٍّ، وهذا يُكتب لا يُطوى:** خطُّ [`#2803202939`](https://gitlab.com/uxxxu/wasla/-/pipelines/2803202939) على الدمجِ نفسِه قِيس بعدَه فكان **26 وظيفةً كلُّها `ci_quota_exceeded` وكلُّها `started_at: null`**، وقبلَه خطُّ الفرعِ [`#2803170194`](https://gitlab.com/uxxxu/wasla/-/pipelines/2803170194) **23 وظيفةً بالحالِ نفسِه** — فلا وظيفةٌ واحدةٌ بدأت. فالدمجُ **تجاوزٌ واعٍ لـ§56 قرَّره مالكُ البرنامجِ وهو صاحبُ القرارِ فيه**، لا خطٌّ أخضرُ ولا دليلٌ يُقرأ كأنّه أخضرُ (`RISK-0001`). **تحريرُ نطاقٍ لا إغلاقُ عنصرٍ**، ونقلُ العنصرِ إلى `Completed` قرارُ مالكِ البرنامجِ (البروتوكول §9)، ويبقى **مشروطاً بدليلِ خطٍّ لم يوجد بعدُ**. والأساسُ المُلتزَمُ `docs/12-testing/BASELINE.json` صارَ في `main` ببصمةِ `sha256:e922ce7bb72ab111…`، فأيُّ زحفٍ لاحقٍ يُقاس منه. |
| CLM-0021 | M1-01 | @uxxxu (agent:computer) | feat/m1-01-unified-principal-model | 2026-08-30 | **دُمج في `main` عبر [MR !114](https://gitlab.com/uxxxu/wasla/-/merge_requests/114)** (رأسُ الطلبِ `a285eaf8`، الدمجُ `dc9f3610`، `merged_at: 2026-08-30T05:23:02.734Z`) **وحُذف الفرعُ** — سُئل المستودعُ عنه فأجاب `404 Branch Not Found`. **وتُحقِّق من نتيجةِ الدمجِ لا من الفرع:** سُئلت واجهةُ المستودعِ عن الفرقِ بين رأسِ الطلبِ `a285eaf8` وشجرةِ `main` عندَ `dc9f3610` فأجابت **صفرَ ملفّاتٍ مختلفةٍ** — فما دُمج هو ما قِيس بعينِه. **ولا دليلَ من الخطِّ:** خطُّ [`#2802915822`](https://gitlab.com/uxxxu/wasla/-/pipelines/2802915822) على `main` عندَ الدمجِ نفسِه قِيس اليومَ فكان **26 وظيفةً كلُّها `ci_quota_exceeded` وكلُّها `started_at: null`** — لم تبدأ وظيفةٌ واحدةٌ، فالفشلُ فشلُ رصيدٍ لا كودٍ (`RISK-0001`، البروتوكول §8.1). **تحريرُ نطاقٍ لا إغلاقُ عنصرٍ**، ونقلُ العنصرِ إلى `Completed` قرارُ مالكِ البرنامجِ (البروتوكول §9). `M1-01` يبقى `Ready for Gate`. |
| CLM-0022 | M0-08 | @uxxxu (agent:computer) | feat/m1-01-unified-principal-model | 2026-08-30 | **دُمج في `main` عبر [MR !114](https://gitlab.com/uxxxu/wasla/-/merge_requests/114)** (رأسُ الطلبِ `a285eaf8`، الدمجُ `dc9f3610`، `merged_at: 2026-08-30T05:23:02.734Z`) **وحُذف الفرعُ** — سُئل المستودعُ عنه فأجاب `404 Branch Not Found`. **وتُحقِّق من نتيجةِ الدمجِ لا من الفرع:** سُئلت واجهةُ المستودعِ عن الفرقِ بين رأسِ الطلبِ `a285eaf8` وشجرةِ `main` عندَ `dc9f3610` فأجابت **صفرَ ملفّاتٍ مختلفةٍ** — فما دُمج هو ما قِيس بعينِه. **ولا دليلَ من الخطِّ:** خطُّ [`#2802915822`](https://gitlab.com/uxxxu/wasla/-/pipelines/2802915822) على `main` عندَ الدمجِ نفسِه قِيس اليومَ فكان **26 وظيفةً كلُّها `ci_quota_exceeded` وكلُّها `started_at: null`** — لم تبدأ وظيفةٌ واحدةٌ، فالفشلُ فشلُ رصيدٍ لا كودٍ (`RISK-0001`، البروتوكول §8.1). **تحريرُ نطاقٍ لا إغلاقُ عنصرٍ**، ونقلُ العنصرِ إلى `Completed` قرارُ مالكِ البرنامجِ (البروتوكول §9). **وتحريرُه يُفرِج `M0-08`** فصار `docs/12-testing/` قابلاً للحجزِ من جديدٍ (`CLM-0026`) — وهو ما كان الحاجزَ المُعلَنَ في دفعةِ `M1-02` قبلَ هذا الدمج. `M0-08` يبقى `Ready for Gate`. |
| CLM-0023 | M0-19 | @uxxxu (agent:computer) | feat/m1-01-unified-principal-model | 2026-08-30 | **دُمج في `main` عبر [MR !114](https://gitlab.com/uxxxu/wasla/-/merge_requests/114)** (رأسُ الطلبِ `a285eaf8`، الدمجُ `dc9f3610`، `merged_at: 2026-08-30T05:23:02.734Z`) **وحُذف الفرعُ** — سُئل المستودعُ عنه فأجاب `404 Branch Not Found`. **وتُحقِّق من نتيجةِ الدمجِ لا من الفرع:** سُئلت واجهةُ المستودعِ عن الفرقِ بين رأسِ الطلبِ `a285eaf8` وشجرةِ `main` عندَ `dc9f3610` فأجابت **صفرَ ملفّاتٍ مختلفةٍ** — فما دُمج هو ما قِيس بعينِه. **ولا دليلَ من الخطِّ:** خطُّ [`#2802915822`](https://gitlab.com/uxxxu/wasla/-/pipelines/2802915822) على `main` عندَ الدمجِ نفسِه قِيس اليومَ فكان **26 وظيفةً كلُّها `ci_quota_exceeded` وكلُّها `started_at: null`** — لم تبدأ وظيفةٌ واحدةٌ، فالفشلُ فشلُ رصيدٍ لا كودٍ (`RISK-0001`، البروتوكول §8.1). **تحريرُ نطاقٍ لا إغلاقُ عنصرٍ**، ونقلُ العنصرِ إلى `Completed` قرارُ مالكِ البرنامجِ (البروتوكول §9). `M0-19` يبقى `Ready for Gate`. |
| CLM-0024 | M1-02 | @uxxxu (agent:computer) | feat/m1-02-telegram-session-verification | 2026-08-30 | **تحريرٌ لإعادةِ حجزٍ بنطاقٍ أوسعَ على نفسِ الفرعِ، لا إغلاقُ عنصرٍ ولا دمجٌ.** احتاجت بوابةُ M1-02 مسارَ `packages/channel-e2e/` (الحزمةُ الوحيدةُ التي يُجيز لها [ADR-007](../15-decisions/ADR-007-telegram-channel-adapter-isolation-and-stack.md) وصلَ المحوَّلِ بالخدمة) و`pnpm-lock.yaml`، وكلاهما خارجَ نطاقِ هذا السطرِ. و[`WORK_CLAIM_RULE.md` §6](../00-rules/WORK_CLAIM_RULE.md) تمنع توسيعَ سطرٍ قائمٍ بعدَ بدءِ الكتابةِ، وتمنع البوابةُ حجزَ عنصرٍ واحدٍ مرّتَين في وقتٍ واحدٍ (رفضت `CLM-0025` بجانبِ هذا السطرِ فعلاً بنصِّ «محجوز مرتين في وقت واحد»)، فبقيَ البديلُ الثالثُ الذي تُجيزه الخانةُ نفسُها: التحريرُ وإعادةُ الحجز. **والنطاقُ لم يُترك بلا مالكٍ لحظةً**: `CLM-0025` نشطةٌ على نفسِ الفرعِ بنفسِ المالكِ وتشمل مساراتِ هذا السطرِ كلَّها. `M1-02` يبقى `Ready for Gate` لا `Completed`، والدمجُ قرارُ مالكِ البرنامج. |
| CLM-0020 | M5-11 | @uxxxu (agent:computer) | feat/m5-11-marketplace-outbox-inventory-sync (والمراجعةُ 6/6 على feat/m5-11-marketplace-exit-gate) | 2026-08-29 | دُمج في `main` عبر [MR !112](https://gitlab.com/uxxxu/wasla/-/merge_requests/112) (الالتزامُ `c461a2d6`، الدمجُ `0a21f29b`، `merged_at: 2026-08-29T11:08:30.552Z`)، **وحُذف الفرعانِ كلاهما** — سُئل المستودعُ عنهما فأجاب `404 Branch Not Found` لـ`feat/m5-11-marketplace-outbox-inventory-sync` ولـ`feat/m5-11-marketplace-exit-gate`، فكان الحجزُ يُقفِل ستَّةَ مساراتٍ (`services/marketplace/`، `packages/marketplace-e2e/`، `docs/02-architecture/`، `docs/07-security/`، `docs/12-testing/`، `.gitlab-ci.yml`) على لا أحدٍ. **والرَّصدُ آليٌّ هذه المرّةَ لا يدويٌّ:** الفحصُ الرابعُ نفسُه أسقطَ البوّابةَ على شجرةِ `main` عندَ `0a21f29b` بعدَ تجسيدِ قائمةِ الفروعِ الحقيقيّةِ من واجهةِ المستودعِ — فحرسَ `M0-16` نطاقَه فعلاً. **وتُحقِّق من نتيجةِ الدمجِ:** على شجرةِ `main` عندَ `0a21f29b` شُغِّلت البوّابةُ الموحَّدةُ فنجحت **عشرةُ فحوصٍ (وجودُ الوثائقِ الحاكمةِ + 1،2،3،5،6،7،9،10،11) · تخطٍّ مُعلَنٌ واحدٌ (8: `only_allow_merge_if_pipeline_succeeds` غيرُ مُفعَّلٍ) · وفشلٌ واحدٌ هو الرابعُ** لأجلِ هذا الحجزِ البائتِ بعينِه؛ وحزمةُ الإثباتِ **141 ناجحاً · 0 فاشلٌ** (بـ`pnpm@9.15.9` المطابقِ لـ`env` في `BASELINE.json`)، والفحصُ الحادي عشرَ ببصمةِ `sha256:6b0f2f59e35b4557…`. وخطُّ [`#2801872301`](https://gitlab.com/uxxxu/wasla/-/pipelines/2801872301) على `main` فشلَ بـ`ci_quota_exceeded` في **26 وظيفةً كلُّها بـ`started_at: null`** — فلا دليلَ من الخطِّ، والدليلُ محلّيٌّ (`RISK-0001`، البروتوكول §8.1). **تحريرُ نطاقٍ لا إغلاقُ عنصرٍ:** `M5-11` يبقى `In Progress`، ونقلُه إلى `Completed` قرارُ مالكِ البرنامجِ لا قرارُ عاملٍ (البروتوكول §9). ويبقى مفتوحاً على الطورِ: `RISK-0012` و`RISK-0013` — كلاهما يلزمه قرارٌ معماريٌّ، مُسجَّلٌ لا مُنفَّذٌ. |
| CLM-0019 | M0-08 | @uxxxu (agent:computer) | feat/m0-08-automated-baseline | 2026-08-28 | دُمج في `main` عبر [MR !108](https://gitlab.com/uxxxu/wasla/-/merge_requests/108) (الالتزامُ `e556670a`، الدمجُ `d132f787`، `merged_at: 2026-08-28T00:13:53Z`)، **وحُذف الفرعُ** — سُئل المستودعُ عنه فأجاب `404 Branch Not Found`، فالحجزُ كان يُقفِل أربعةَ مساراتٍ (`docs/12-testing/`، `scripts/`، `docs/00-rules/`، `docs/07-security/`) على فرعٍ لا وجودَ له — وهو عينُ الضررِ الذي يحرسه `M0-16`. **وتُحقِّق من نتيجةِ الدمجِ لا من الفرع:** أُنزلت شجرةُ `main` عندَ `d132f787` وشُغِّلت عليها البوّابةُ الموحَّدةُ: **عشرةُ فحوصٍ ناجحةٍ (وجودُ الوثائقِ الحاكمةِ + تسعةٌ مرقَّمةٌ: 1،2،3،5،6،7،9،10،11) · 0 فاشلٌ · تخطّيانِ مُعلَنانِ · رمزُ الخروجِ 0** (الرابعُ: لا وصولَ إلى الفروعِ من بيئةِ التشغيل · الثامنُ: `only_allow_merge_if_pipeline_succeeds` غيرُ مُفعَّلٍ)، والفحصُ **الحادي عشرَ** الذي أنشأه هذا العنصرُ يعمل على شجرةِ `main` فعلاً بأبوابِه الأربعةِ وبصمةِ `sha256:0b2364fbe84e81ea…` نفسِها. وخطُّ `#2798229977` على `main` فشلَ بـ`ci_quota_exceeded` في **25 وظيفةً كلِّها بـ`started_at: null`** — فلا دليلَ من الخطِّ، والدليلُ محلِّيٌّ (البروتوكول §8.1). تحريرُ نطاقٍ لا إغلاقُ عنصرٍ: `M0-08` يبقى `Ready for Gate`. |
| CLM-0018 | M0-07 | @uxxxu (agent:computer) | feat/m0-07-risk-register | 2026-08-27 | دُمج في `main` عبر [MR !106](https://gitlab.com/uxxxu/wasla/-/merge_requests/106) (الالتزامُ `41012d49`، الدمجُ `e3d1cc90`، `merged_at: 2026-08-27T21:14:05Z`)، وحُذف الفرعُ (`404 Branch Not Found`). وخطُّ `#2797920709` على `main` فشلَ بـ`ci_quota_exceeded` في **25 وظيفةً كلِّها بـ`started_at: null`** — فلا دليلَ من الخطِّ، والدليلُ محلّيٌّ (البروتوكول §8.1): البوّابةُ 8 ناجحةٌ / 0 فاشلٌ / 3 تخطّياتٍ مُعلَنةٍ · الحزمةُ 120 ناجحاً / 0 فاشلٌ · `scripts/verify.sh` رمزُ 0. |
| CLM-0017 | M0-06 | @uxxxu (agent:computer) | fix/m0-06-dependency-audit | 2026-08-27 | دُمج في `main` عبر [MR !104](https://gitlab.com/uxxxu/wasla/-/merge_requests/104) (الالتزامُ `ac258b33`، الدمجُ `97f327ea`، `merged_at: 2026-08-27T18:59:39Z`)، وحُذف الفرعُ (`404 Branch Not Found`). وخطُّ `#2797596008` على `main` فشلَ بـ`ci_quota_exceeded` في **25 وظيفةً كلِّها بـ`started_at: null`** — فلا دليلَ من الخطِّ، والدليلُ محلّيٌّ (البروتوكول §8.1). |
| CLM-0016 | M0-04 | @uxxxu (agent:computer) | fix/m0-04-unified-verify-command | 2026-08-27 | دُمج في `main` عبر [MR !102](https://gitlab.com/uxxxu/wasla/-/merge_requests/102) (الالتزامُ `39f302e2`، الدمجُ `0b2fffe8`، `merged_at: 2026-08-27T15:54:08Z`) **وحُذف الفرعُ** — سُئل المستودعُ عنه فأجاب `404 Branch Not Found`. **وتُحقِّق من نتيجةِ الدمجِ لا من الفرع:** أُنزلت شجرةُ `main` عندَ `0b2fffe8` وشُغِّلت عليها البوّابةُ الموحَّدةُ: **ستٌّ خضراءُ · 0 إخفاق · ثلاثُ تخطّياتٍ مُعلَنةٍ** (قاعدةُ التوثيق: لا مرجعَ `origin/main` في نسخةٍ من الأرشيفِ · بياتُ الحجوزات: لا وصولَ إلى الفروعِ من هذه البيئة · الفحصُ الثامنُ: جزئيٌّ بقصدٍ حتى يُفعَّل `only_allow_merge_if_pipeline_succeeds`)، والفحصُ الثامنُ الذي أنشأه هذا العنصرُ **يعمل على شجرةِ `main` فعلاً**: «مدخلٌ موحَّدٌ مُستدعىً · لا كتلةَ إلزامٍ مضمَّنةً · 1 استثناءَ إخفاقٍ مُعلَناً · 2 سكربتَ استكشافٍ مُعلَناً». `M0-04` يبقى `Ready for Gate` لا `Completed`: خطُّ `#2797139344` على `main` (نفسِ الدمجِ `0b2fffe8`) فشلَ بـ`ci_quota_exceeded` في **خمسٍ وعشرينَ وظيفةً كلِّها بـ`started_at: null`** — ومنها `verify` و`governance-guard` نفسُهما، أي أنّ الوظيفةَ التي تُثبت هذا العنصرَ لم تبدأ أصلاً. |
| CLM-0015 | M0-03 | @uxxxu (agent:computer) | fix/m0-03-integration-ddl-isolation | 2026-08-27 | دُمج في `main` عبر [MR !100](https://gitlab.com/uxxxu/wasla/-/merge_requests/100) (الالتزامُ `491b13ff`، الدمجُ `2b62aee6`، `merged_at: 2026-08-27T15:27:49Z`) **وحُذف الفرعُ** (`404 Branch Not Found`). **وتُحقِّق من نتيجةِ الدمجِ لا من الفرع:** أُنزلت شجرةُ `main` عندَ `2b62aee6` فلا فرقَ في ملفٍّ واحدٍ عن شجرةِ الفرعِ (`diff -rq` رمزُه 0)، والبوّابةُ عليها **ستٌّ خضراءُ** — وفيها الفحصُ السابعُ (11 خدمةً بالتسلسل) — باثنَين متخطَّيَين، والحزمةُ **54/0**. `M0-03` يبقى `Ready for Gate` لا `Completed`: خطُّ `#2797065926` على `main` فشلَ بـ`ci_quota_exceeded` في **24 وظيفةً كلِّها بـ`started_at: null`**، ومنها `governance-guard` و`db-integration` — أي أنّ الوظيفةَ التي تُثبت العزلَ في CI لم تبدأ أصلاً. |
| CLM-0014 | M0-17 | @uxxxu (agent:computer) | fix/m0-17-merged-target-guard | 2026-08-27 | دُمج في `main` عبر [MR !98](https://gitlab.com/uxxxu/wasla/-/merge_requests/98) (الالتزامُ `1dabfbc5`، الدمجُ `acb94a68`، `merged_at: 2026-08-27T14:26:46Z`) **وحُذف الفرعُ** (`404 Branch Not Found`). **وتُحقِّق من نتيجةِ الدمجِ لا من الفرع:** أُنزلت شجرةُ `main` عندَ `acb94a68` وقُوبلت بشجرةِ الفرعِ فلا فرقَ في ملفٍّ واحدٍ (`diff -rq` بلا مخرَج)، والبوّابةُ عليها خمسةٌ خضراءُ باثنَين متخطَّيَين والحزمةُ **45/0**. **والهدفُ كان `main` مباشرةً** — لا فرعاً مكدَّساً — إعمالاً للقاعدةِ التي أنشأها هذا العنصرُ نفسُه. `M0-17` يبقى `Ready for Gate` لا `Completed`: خطُّ `#2796872466` على `main` فشلَ بـ`ci_quota_exceeded` في **24 وظيفةً كلِّها بـ`started_at: null`** — ولا واحدةَ منها بدأت، ومنها `governance-guard` نفسُها. |
| CLM-0002 | M0-01 | @uxxxu | fix/m0-01-subscription-composition-guard | 2026-08-27 | **حجزٌ بائتٌ رُصد في `M0-15` وحُرِّر بقرارِ مالكِ البرنامجِ 2026-08-27** (لا من تلقاءِ الوكيلِ: المالكُ `@uxxxu` بلا وسمِ وكيلٍ، فالقرارُ ليس قرارَه). **الدليلُ:** الفرعُ `fix/m0-01-subscription-composition-guard` **غيرُ موجودٍ** في المستودعِ (`404 Ref Not Found`)، وعملُه مدموجٌ في [MR !82](https://gitlab.com/uxxxu/wasla/-/merge_requests/82) (`merged_at: 2026-08-25T17:56:01Z`، الدمجُ `0c48a9d7`) — أي أنّ الحجزَ كان يُقفل `services/subscriptions/` على لا أحدٍ منذ يومَين، وهو عينُ الضررِ الذي حرسه `M0-16`. تحريرٌ بحكمِ البروتوكول §8.1. `M0-01` يبقى `Ready for Gate` لا `Completed`: التحريرُ يُفرِج نطاقاً ولا يُنشئ دليلَ إغلاقٍ، ودليلُ الإغلاقِ بوّابةٌ خضراءُ في CI. |
| CLM-0013 | M0-16 | @uxxxu (agent:computer) | fix/m0-16-claim-freshness-guard | 2026-08-27 | دُمج عبر [MR !96](https://gitlab.com/uxxxu/wasla/-/merge_requests/96) (الالتزامُ `d8bc8432`، الدمجُ `dcfaf1e9`) **وحُذف الفرعُ تلقائياً** — فلم يبقَ له وجودٌ في المستودعِ البعيد. والتحريرُ هنا **ليس مجاملةً**: الحارسُ الذي أنشأه `M0-16` نفسُه شُغِّل على السجلِّ الحقيقيِّ مقابلَ حالةِ الفروعِ الفعليّةِ فرفض هذا الحجزَ بالاسم — فبقاؤه `Active` كان يُصيِّر بوّابةَ `main` حمراءَ لحظةَ دمجِ [MR !97](https://gitlab.com/uxxxu/wasla/-/merge_requests/97). وأوّلُ ما رفضه الحارسُ هو حجزُ صانعِه. `M0-16` يبقى `In Progress` لا `Completed`: لا خطَّ أنابيبَ أخضرَ (`ci_quota_exceeded`). |
| CLM-0012 | M0-15 | @uxxxu (agent:computer) | fix/m0-15-meaningful-filter-parity | 2026-08-27 | دُمج في `main` عبر [MR !94](https://gitlab.com/uxxxu/wasla/-/merge_requests/94) (الالتزامُ `2ea7e64f`، الدمجُ `704e2f3a`). وتُحقِّق من **نتيجةِ الدمجِ** لا من الفرع: أُنزلت شجرةُ `main` بعدَ الدمجِ وقُوبلت بشجرةِ العملِ فكانت **مطابقةً بلا فرقٍ** (`diff -r`)، ثمّ شُغِّلت الحزمةُ على تلك الشجرةِ نفسِها فجاءت **32 ناجح · 0 فاشل**. `M0-15` يبقى `Ready for Gate` لا `Completed`: خطُّ الأنابيبِ `#2796336446` على `main` فشلَ بـ`ci_quota_exceeded` في وظائفِه **الأربعِ والعشرينَ** كلِّها بـ`started_at: null`، وكذلك `#2796270457` على الطلبِ (**22** وظيفة) — لا وظيفةَ واحدةَ بدأت. |
| CLM-0010 | M0-10 | @uxxxu (agent:computer) | fix/m0-09-m0-10-renumber-and-land | 2026-08-27 | **حجزٌ بائتٌ رُصد في `M0-15`:** بقي `Active` بعدَ دمجِ فرعِه في [MR !91](https://gitlab.com/uxxxu/wasla/-/merge_requests/91) (الدمجُ `82eab091`، `merged_at: 2026-08-26T23:38:49Z`) وحذفِ الفرعِ — والفرعُ لم يعد موجوداً (`404 Ref Not Found` على نقطةِ الفروع). فالتحريرُ تطبيقٌ حرفيٌّ للبروتوكول §8.1 لا قرارٌ جديد. `M0-10` يبقى `Ready for Gate`. |
| CLM-0009 | M0-09 | @uxxxu (agent:computer) | fix/m0-09-m0-10-renumber-and-land | 2026-08-27 | **حجزٌ بائتٌ رُصد في `M0-15`:** نفسُ فرعِ `CLM-0010`، مدموجٌ في [MR !91](https://gitlab.com/uxxxu/wasla/-/merge_requests/91) (الدمجُ `82eab091`) والفرعُ محذوف. تحريرٌ بحكمِ §8.1. `M0-09` يبقى `Ready for Gate`. |
| CLM-0008 | M0-13 | @uxxxu (agent:computer) | chore/m0-13-closeout-and-drift-findings | 2026-08-27 | **حجزٌ بائتٌ رُصد في `M0-15`:** مدموجٌ في [MR !90](https://gitlab.com/uxxxu/wasla/-/merge_requests/90) (الدمجُ `8c4269c1`، `merged_at: 2026-08-26T22:55:53Z`) والفرعُ محذوف. تحريرٌ بحكمِ §8.1. `M0-13` يبقى `Ready for Gate`. |
| CLM-0011 | M0-14 | @uxxxu (agent:computer) | fix/m0-14-meaningful-docs-scope | 2026-08-27 | دُمج في `main` عبر [MR !92](https://gitlab.com/uxxxu/wasla/-/merge_requests/92) (الالتزامُ `fc34e708`، الدمجُ `084dbab8`). وتُحقِّق محليّاً من **نتيجةِ الدمجِ نفسِها**: أُنزلت شجرةُ `main` بعدَ الدمجِ وقُوبلت بالفرعِ فكانت **مطابقةً بلا فرق**، ثمّ `verify-governance.sh` خمسةٌ خضراءُ و`test-governance.sh` **30/30** على تلك الشجرة. `M0-14` يبقى `Ready for Gate` لا `Completed`: خطُّ الأنابيبِ `#2796132705` على `main` فشلَ بـ`ci_quota_exceeded` في وظائفِه الأربعِ والعشرينَ كلِّها، وكذلك `#2796070035` على الطلبِ (22 وظيفة) و`#2796065617` على الفرعِ (وظيفتان) — لا وظيفةَ واحدةَ بدأت. الفرعُ لم يُحذف. |
| CLM-0006 | M0-12 | @uxxxu (agent:computer) | fix/m0-12-governance-proof-isolation | 2026-08-26 | دُمج في `main` عبر [MR !88](https://gitlab.com/uxxxu/wasla/-/merge_requests/88) (الالتزامُ `04d90968`، الدمجُ `8b44b143`) وحُذف الفرع. `M0-12` يبقى `Ready for Gate` لا `Completed`: دليلُ الإغلاقِ بوّابةٌ خضراءُ في CI، وخطُّ الأنابيبِ `#2793103205` على `main` فشلَ بـ`ci_quota_exceeded` في الوظائفِ العشرينَ كلِّها. |
| CLM-0007 | M0-13 | @uxxxu (agent:computer) | fix/m0-13-agent-owner-normalization | 2026-08-26 | دُمج في `main` عبر [MR !89](https://gitlab.com/uxxxu/wasla/-/merge_requests/89) (الالتزامُ `74463452`، الدمجُ `0edc4ff5`) وحُذف الفرع. واستهدافُ الطلبِ نُقل إلى `main` تلقائيّاً عندَ دمجِ !88 — لم يُحتَج تدخُّلاً يدويّاً. |
| CLM-0003 | M0-11 | @uxxxu | fix/m0-11-governance-gate-sigpipe | 2026-08-26 | حُرِّر بقرارِ مالكِ البرنامج (`M0-12`): عملُ `M0-11` مدفوعٌ ومدموجٌ في الالتزامِ `183a7369`، وبقاءُ الحجزِ نشطاً كان يمنع `M0-12` من العملِ على `scripts/checks/` وهو نطاقُه المُعلَنُ على اللوحة. `M0-11` نفسُه يبقى `Ready for Gate` لا `Completed`: دليلُ الإغلاقِ بوّابةٌ خضراءُ في CI، والحصّةُ منتهية. |
| CLM-0005 | M5-11 | @uxxxu (agent:computer) | feat/phase11-mr4-marketplace-http | 2026-08-26 | حُرِّر لأنّ الفرعَ دُمج في `main` عبر [MR !87](https://gitlab.com/uxxxu/wasla/-/merge_requests/87) (الالتزامُ `38c73e4c`، الدمجُ `8f16670e`). رُصد في `M0-12` كحجزٍ نشطٍ لفرعٍ مدموج. `M5-11` يبقى `In Progress` — MR 4/6. |
| CLM-0001 | M0-05 | @uxxxu | chore/unified-roadmap-governance | 2026-08-25 | دُمج في `main` عبر [MR !81](https://gitlab.com/uxxxu/wasla/-/merge_requests/81). وبقي `M0-05` نفسُه `Ready for Gate` لا `Completed`: دليلُ إغلاقه بوابةٌ خضراء، والبوابةُ لم تُنفَّذ لنقصِ رصيدِ CI. |

يُنقل السطر إلى هنا **بعد الدمج**، مع رابط دليل قابل للتحقق (MR أو pipeline).

| Claim ID | Work Item | Owner | Branch | Released | Evidence |
| --- | --- | --- | --- | --- | --- |
| — | — | — | — | — | — |

---

## 4. ما يفرضه `validate-work-claims.sh`

| # | الفحص | سبب الرفض |
|---|---|---|
| 1 | بنية الجدول والأعمدة الثمانية | سجل غير قابل للقراءة آليًا = حوكمة معطّلة |
| 2 | `Claim ID` فريد في الملف كله (نشط ومحرَّر) | إعادة الاستخدام تخفي التاريخ |
| 3 | لا حجزان نشطان لنفس `Work Item` | جهتان تعملان على نفس العنصر |
| 4 | لا تقاطع بين نطاقات الحجوزات النشطة | جهتان تعملان على نفس الملفات |
| 5 | `Work Item` موجود في اللوحة | عمل خارج خارطة الطريق |
| 6 | `Expires` صالح ولم يمضِ ولا يتجاوز 14 يومًا | حجز مهجور يقفل النطاق على الآخرين |
| 7 | نطاق مكتوب بمسارات موجودة أو قابلة للإنشاء | نطاق فضفاض لا يمنع شيئًا |
| 8 | كل ملف مُغيَّر في الدفع داخل نطاق حجز الفرع | توسع صامت يصطدم بعمل الآخرين |

الفحص الثامن هو جوهر منع التكرار: **لا يكفي أن تعلن ما ستعمل عليه، بل لا تستطيع تقنيًا الدفع خارجه.**

---

## 5. حالات خاصة

- **عمل عاجل (Hotfix):** يُنشأ عنصر `M0-HF-<n>` في اللوحة وحجز مدته يومان، ويُوثَّق في `TASK_LOG` كباقي العمل. لا استثناء من الحجز.
- **حجز مهجور:** بعد `Expires` يجوز لأي منفّذ نقله إلى «محرَّرة» بحالة `Expired` وسبب مكتوب، ثم إنشاء حجز جديد.
- **تسليم بين جهتين:** يُحرَّر الحجز الأول ويُنشأ حجز جديد بمالك جديد. لا يُعدَّل حقل `Owner` لحجز نشط.
- **توسيع النطاق:** يُعدَّل `Scope Paths` **قبل** كتابة الكود، وبعد تشغيل `find-existing-work.sh` على المسار الجديد.
