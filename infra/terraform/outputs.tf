# ── Outputs ────────────────────────────────────────────────────────────
# Sensitive outputs are marked as such — they are not printed in console output.

output "environment" {
  description = "The deployment environment"
  value       = var.environment
}
