{
  "name": "@wasla/contracts-support",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WASLA Support contracts (Phase 16, ADR-049)",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "generate": "openapi-typescript ../../../services/support/contracts/api.openapi.yml -o src/api-types.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "openapi-typescript": "^7.4.4",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
