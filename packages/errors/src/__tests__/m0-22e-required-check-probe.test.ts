// ── مِجَسٌّ أحمرُ متعمَّدٌ · M0-22E ─────────────────────────────────────────
// هذا الملفُّ **يُخفِقُ بقصدٍ**، ووجودُه مؤقَّتٌ في فرعِ المِجَسِّ وحدَه.
// غايتُه إثباتُ دعوى واحدةٍ لا تُقبَلُ بلا برهانٍ: **أنّ إخفاقَ فحصٍ مطلوبٍ
// يمنعُ الدمجَ فعلاً** بعدَ توسيعِ `required_status_checks` من ستّةٍ إلى
// سبعةٍ وعشرينَ. ولا يُدمَجُ هذا الفرعُ أبداً، ويُغلَقُ طلبُه بعدَ قراءةِ
// نتيجةِ محاولةِ الدمجِ وتسجيلِها دليلاً.
import { describe, expect, it } from 'vitest';

describe('M0-22E · مِجَسٌّ أحمرُ متعمَّدٌ (لا يُدمَجُ)', () => {
  it('يُخفِقُ بقصدٍ ليُقاسَ أثرُ إخفاقِ فحصٍ مطلوبٍ على الدمجِ', () => {
    expect('deliberate-red-probe').toBe('this-assertion-must-fail');
  });
});
