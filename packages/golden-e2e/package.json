{
  "name": "@wasla/golden-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WASLA M4-02 — Golden cross-service E2E journeys against deployed staging. Drives real Render staging URLs over HTTP. Tests only: no runtime code and no exports.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "vitest": "^2.1.0"
  }
}
