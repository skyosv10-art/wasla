{
  "name": "@wasla/negotiation-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WASLA Phase 08 Exit Gate — the only suite allowed to wire Negotiations, the Order Engine, Dispatch, Matching and the Customer Core together over real HTTP through their production adapters (ADR-013). Tests only: no runtime code and no exports.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@wasla/service-auth": "workspace:*",
    "@types/pg": "^8.23.1",
    "@wasla/contracts-negotiation": "workspace:*",
    "@wasla/customers-service": "workspace:*",
    "@wasla/dispatch-service": "workspace:*",
    "@wasla/geography-service": "workspace:*",
    "@wasla/identity-service": "workspace:*",
    "@wasla/matching-service": "workspace:*",
    "@wasla/negotiations-service": "workspace:*",
    "@wasla/orders-service": "workspace:*",
    "pg": "^8.23.0",
    "vitest": "^2.1.0"
  }
}
