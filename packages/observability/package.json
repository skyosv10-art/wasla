{
  "name": "@wasla/observability",
  "version": "0.1.0",
  "description": "Shared observability package: Prometheus metrics, OpenTelemetry tracing, and Fastify instrumentation for WASLA services.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "prom-client": "^15.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "fastify": "^5.12.1",
    "vitest": "^2.1.0"
  },
  "peerDependencies": {
    "fastify": "^4.0.0 || ^5.0.0"
  }
}
