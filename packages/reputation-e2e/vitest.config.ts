/**
 * إعدادٌ واحد، ويُضمّن ملفَّ `.e2e.test.ts` **عن قصد**.
 *
 * كلُّ حزمةٍ أخرى في هذا المستودع تستثني `*.{integration,e2e}.test.ts` كي يبقى
 * `pnpm -r test` أخضرَ بلا قاعدةِ بيانات. وهذه الحزمة استثناءٌ مُعلَنٌ سابعُ أخواتها
 * (channel-e2e، customer-e2e، dispatch-e2e، driver-e2e، negotiation-e2e، order-e2e)
 * ولنفس السبب: فيها **بوابةُ خروج Phase 09**، وبوابةٌ يمكن تخطّيها ليست بوابة.
 *
 * فتركض هذه الحزمةُ في كلِّ `pnpm -r test` على مُهيئي الذاكرة للخدمتين — محرّكُ الطلب
 * والسمعة — ولا تحتاج قاعدةً ولا شبكةً خارجَ `127.0.0.1`.
 *
 * `fileParallelism: false` لأنّ كلَّ ملفٍ هنا يرفع مُستمعَينِ حقيقيّين على منفذَينِ
 * يمنحهما النظام (`port: 0`)؛ وملفٌّ ثانٍ يركض بالتوازي كان سيتنافس على مِقبضَي
 * العمليّة نفسِها لا على منفذٍ مكتوبٍ بيد — والعطبُ حينها يظهر كتقلّبٍ لا كخطأ.
 *
 * والمهلةُ 120 ثانية لا 30: البوابةُ تسوق طلباتٍ كاملةً عبر HTTP من `published` إلى
 * `completed` ثمّ تستهلك أحداثَها الفعليّةَ وتُصرِّف صفَّ الصادر. تركض في ثوانٍ على
 * الذاكرة، والهامشُ لحاويةِ CI الباردة.
 *
 * التفصيل في docs/12-testing/PHASE09_EXIT_GATE_E2E.md.
 */
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    include: ["src/__tests__/*.e2e.test.ts"],
    fileParallelism: false,
    testTimeout: 120_000,
    hookTimeout: 60_000,
  },
});
