/**
 * إعدادُ الوحدةِ لحزمةِ `@wasla/service-auth`.
 *
 * ولِمَ استثناءُ ملفّاتِ التكاملِ **إلزامٌ لا تفضيلٌ**: `pnpm -r test` يجري بلا
 * قاعدةِ بياناتٍ، وملفُّ `replay-postgres.integration.test.ts` يُلقي صراحةً حينَ
 * لا `DATABASE_URL` (ولا يتخطّى صامتاً بقصدٍ). فلو بقيَ داخلَ الافتراضيِّ لصارَ
 * الاختيارُ بينَ حمرةٍ كاذبةٍ في كلِّ آلةٍ أو تخطٍّ صامتٍ يُقرَأُ خُضرةً — وكلاهما
 * كذبٌ في القياسِ. فالفصلُ يُبقي البرهانَ حقيقيّاً في وظيفةِ `db-integration`.
 *
 * والاستثناءُ **مقصورٌ على `*.integration.test.ts` وحدَهُ**: ملفُّ
 * `http-server.e2e.test.ts` كانَ يجري في المجموعةِ الافتراضيّةِ قبلَ هذا الإعدادِ
 * ولا يحتاجُ قاعدةً، فإخراجُهُ منها كانَ سيكونُ إضعافاً للمجموعةِ لا فصلاً.
 */
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    exclude: [
      "**/node_modules/**",
      "**/dist/**",
      "**/__tests__/*.integration.test.ts",
    ],
  },
});
