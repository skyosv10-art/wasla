{
  "name": "@wasla/customers-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WASLA Customer Core service — pure domain, ports, use cases and in-memory adapters (Phase 04).",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@wasla/contracts-customer": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "vitest": "^2.1.0"
  }
}
