{
  "name": "@wasla/delivery-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WASLA Store Orders & Delivery service — fulfilment workflow state machine and contracts (Phase 13, ADR-026).",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@wasla/contracts-delivery": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "vitest": "^2.1.0"
  }
}
