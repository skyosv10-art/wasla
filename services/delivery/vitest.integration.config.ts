/**
 * إعدادُ Vitest لاختباراتِ تكاملِ خدمةِ التوصيلِ على PostgreSQL.
 *
 * يختار ملفّاتِ التكاملِ وحدَها ويمنع توازيَ الملفّات: كلُّ ملفٍّ يُعيد ضبطَ نفسِ
 * قاعدةِ الاختبارِ عبر `pg-harness`، فتوازيهما كان سيُنتج فشلاً متقطّعاً.
 * تتخطّى الاختباراتُ نفسها بلا `DATABASE_URL`.
 *
 * ## مهلةُ 30 ثانيةً — لأنَّ القاعدةَ ليست دائماً على المُضيفِ نفسِهِ (9/N)
 *
 * مهلةُ Vitest الافتراضيَّةُ (5 ثوانٍ) تكفي قاعدةً محلّيَّةً كما في CI، وتُسقِطُ
 * نفسَ الاختباراتِ الصحيحةِ على قاعدةٍ بعيدةٍ: ملفٌّ واحدٌ يقطعُ عشراتِ الرحلاتِ
 * ذهاباً وإياباً، وكلُّ رحلةٍ عشراتُ المِلِّي ثانيةٍ عبرَ القارّاتِ. والفشلُ حينَها
 * لا يقولُ «هذا عطبٌ» بل «مهلتُك قصيرةٌ»، وهوَ أسوأُ ما يُعلَّمُ للمُطوِّرِ: أن
 * يقرأَ الأحمرَ بيئةً لا سبباً. فالمهلةُ سخيَّةٌ والتوازي ممنوعٌ — والسرعةُ
 * ليستْ ما تُقاسُ به هذهِ الطبقةُ.
 */
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    include: ["src/__tests__/*.{integration,e2e}.test.ts"],
    fileParallelism: false,
    environment: "node",
    testTimeout: 30_000,
    hookTimeout: 60_000,
  },
});
