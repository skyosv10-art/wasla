{
  "name": "@wasla/dispatch-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WASLA Dispatch service — من يُعرَض عليه ومتى ينتهي وقته: آلة المهمّة/الموجة/العرض، والزمن من نبضة واحدة بساعة مُحقونة، ومحرّك الطلبات والمطابقة عبر منافذ (Phase 07 · MR 4/6: مجال نقي بلا قاعدة بيانات وبلا HTTP).",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@wasla/contracts-dispatch": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@wasla/orders-service": "workspace:*",
    "vitest": "^2.1.0"
  }
}
