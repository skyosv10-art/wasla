{
  "name": "@wasla/drivers-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WASLA Driver Core service — ملفّ دور السائق ومركباته ووثائقه، والأهليّة دالّةً مُشتقّة لا عموداً (Phase 05).",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@wasla/contracts-driver": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "vitest": "^2.1.0"
  }
}
