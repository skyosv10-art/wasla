/**
 * منابعُ اللاحتميّةِ الوحيدةُ في الخدمة — مُعلَنةً بالاسمِ في `purity.test.ts`.
 *
 * ## لماذا ملفٌّ واحدٌ لا سطرٌ متفرّق
 *
 * `new Date()` مبثوثاً في الخدماتِ يجعل كلَّ اختبارِ زمنٍ يحتاج تجميدَ الساعةِ عالميّاً، ثمّ
 * يجعل حارسَ النقاءِ عاجزاً عن قولِ «لا ساعةَ مخفيّة». وحصرُها في ملفٍّ **مُعلَنٍ في
 * `REAL_CLOCK_FILES`** يقلب القاعدةَ من نصيحةٍ إلى فحصٍ: أيُّ ملفٍّ آخر يقرأ الساعةَ يُسقط
 * الاختبار باسمه.
 *
 * ## ولماذا ليست هذه «تِكّة»
 *
 * القرار 2 في الطور 11 يمنع مرورَ الوقتِ أن يقرّر شيئاً في هذا الحدّ: لا اعتمادَ تلقائيّاً بعد
 * مُهلة، ولا انتقالَ حالةٍ يُشعله جدولٌ. والساعةُ هنا تُسمّي لحظةَ قرارٍ **صنعه إنسانٌ**
 * (`decided_at` · `added_at` · `occurred_at`) ولا تصنع قراراً. ولا مُولِّدَ مُعرِّفاتٍ هنا
 * أصلاً: كلُّ `uuid` في هذه الخدمةِ من `gen_random_uuid()` في القاعدة، فالمُعرِّفُ يُولَّد في
 * المعاملةِ التي تكتبه.
 */

import type { Clock } from "../domain/time.js";

/** ساعةُ النظام: `now()` نصُّ ISO لأنّ المجالَ لا يعرف `Date`. */
export const systemClock: Clock = Object.freeze({
  now(): string {
    return new Date().toISOString();
  },
});
