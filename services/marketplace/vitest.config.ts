/**
 * إعدادُ vitest الافتراضيّ — اختباراتُ مجالٍ نقيّة وحدها.
 *
 * مجالُ السوقِ في المراجعة 2/6 بلا قاعدةٍ وبلا HTTP وبلا نبضة، فالحزمةُ كلُّها تركض في أجزاءٍ
 * من الثانية على جهازٍ لا Postgres فيه — بما فيها اشتقاقُ الحالةِ من دفترِ المراجعاتِ وطيُّ
 * دفترِ المخزون. واستثناءُ `*.integration.test.ts` و`*.e2e.test.ts` مكتوبٌ من الآن كي لا
 * تُدسّ اختباراتُ المراجعة 3/6 في المسار السريع فتصير كلُّ دفعةٍ محتاجةً قاعدةً حاضرة، كما
 * في الأطوار 04 و06 و07 و08 و09 و10.
 */
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    exclude: ["**/node_modules/**", "**/dist/**", "**/__tests__/*.{integration,e2e}.test.ts"],
  },
});
