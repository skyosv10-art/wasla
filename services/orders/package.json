{
  "name": "@wasla/orders-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WASLA Order Engine service — the order lifecycle: transition table, domain, ports, use cases, in-memory adapters (Phase 06).",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@wasla/contracts-order": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "vitest": "^2.1.0"
  }
}
