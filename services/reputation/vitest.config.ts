/**
 * إعدادُ vitest الافتراضيّ — اختباراتُ نواةٍ نقيّة وحدها.
 *
 * مجالُ السمعة في المراجعة 2/6 بلا قاعدةٍ وبلا HTTP، فالحزمةُ كلُّها تركض في أجزاءٍ من
 * الثانية على جهازٍ لا Postgres فيه — بما فيها اختباراتُ التلاشي والنبضة، وهي تدفع ساعةً
 * بيدها ولا تنتظر زمناً حقيقياً. واختباراتُ التكامل تأتي مع مُهيئات Drizzle في المراجعة
 * 3/6 مع وظيفة CI الخاصة بها، كما في الأطوار 04 و06 و07 و08.
 */
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    exclude: ["**/node_modules/**", "**/dist/**", "**/__tests__/*.{integration,e2e}.test.ts"],
  },
});
