{
  "name": "@wasla/search-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WASLA Marketplace Search service — derived read model, query normalization, bilingual ranking (Phase 12).",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:integration": "vitest run --config vitest.integration.config.ts"
  },
  "dependencies": {
    "@wasla/contracts-marketplace": "workspace:*",
    "@wasla/contracts-search": "workspace:*",
    "fastify": "^5.12.1",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/pg": "^8.11.0",
    "vitest": "^2.1.0"
  }
}
