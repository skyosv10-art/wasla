{
  "name": "@wasla/subscriptions-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WASLA Subscriptions & Referrals service — الحالةُ مُشتقّةٌ من دفتر مُدَدٍ لا حقلٌ يُكتب، والمكافأةُ مدةٌ لا رصيد، ولا عمودَ مالٍ واحدٍ هنا (Phase 10 · MR 3/6: استمراريّةٌ على Postgres — مُهاجرةٌ تُطبّق العقد، وبذرةُ خطّةٍ من `plans.ts` نفسِها، ودفترُ إضافةٍ فقط بلا `UPDATE` على حالة).",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    },
    "./db": {
      "types": "./src/db/index.ts",
      "import": "./src/db/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "db:migrate": "node --import tsx src/db/migrate-cli.ts"
  },
  "dependencies": {
    "@wasla/contracts-subscription": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "vitest": "^2.1.0",
    "@types/pg": "^8.23.1",
    "tsx": "^4.23.12"
  }
}
