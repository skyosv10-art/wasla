{
  "name": "@wasla/subscriptions-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WASLA Subscriptions & Referrals service — الحالةُ مُشتقّةٌ من دفتر مُدَدٍ لا حقلٌ يُكتب، والمكافأةُ مدةٌ لا رصيد، ولا عمودَ مالٍ واحدٍ هنا (Phase 10 · MR 2/6: طبقةُ مجالٍ نقيّةٌ وحدها — بلا Postgres وبلا HTTP وبلا مؤقّت).",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@wasla/contracts-subscription": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "vitest": "^2.1.0"
  }
}
