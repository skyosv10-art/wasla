{
  "name": "@wasla/support-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WASLA Support & Escalation service (Phase 16, ADR-049)",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "db:migrate": "node --loader tsx src/db/migrate-cli.ts"
  },
  "dependencies": {
    "@wasla/contracts-support": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8",
    "@types/pg": "^8.11.0"
  }
}
